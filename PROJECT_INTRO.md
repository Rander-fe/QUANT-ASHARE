# A股量化机器学习选股项目：详细说明

> 本文档以当前仓库代码、配置和本地已有产物为准，面向需要完整理解项目的人，而不是只提供一组运行命令。
>
> 核对日期：2026-08-24。固定配置以 `config/settings.py` 和各模块配置文件为最终准绳；本文中的数据规模是当前本地产物快照，不应被理解为永久不变的常量。

---

## 1. 项目到底在做什么

这是一个面向 A 股日频横截面选股的完整研究工程。它把 Qlib 日线行情、Tushare 财务与估值数据加工成 Point-in-Time（当时可知）的样本，构建约270个候选因子，先通过统计检验和经济逻辑筛选出25个人工核心因子，再以这25个因子为基准进行 DeepSeek 增量因子挖掘，最终冻结 25 个人工因子和 5 个准入增量因子，组成30个因子的 LightGBM 主模型。模型预测未来5日收益后，再交给低频 TopK/Buffer/Dropout 组合模块进行调仓回测。

项目不是“训练一次模型后直接看测试收益”，而是把研究过程拆成六个前后衔接、相互隔离的阶段，并在最后增加额外分析：

1. 数据准备：生成当时真实可见的行情、财务、估值和交易状态数据。
2. 270因子库构建：按技术、估值、质量、成长、风险、流动性等经济逻辑形成候选特征。
3. 270→58→25：先按 IC/ICIR 和缺失率筛选，再去冗余和做分组检验，确定人工25因子。
4. DeepSeek因子挖掘：生成候选后经过 DSL、时点、缺失率、RankIC、增量IC、冗余和10bps周频回测准入。
5. 30因子LightGBM：用25个人工因子加5个冻结增量因子做滚动样本外预测；Ridge和MLP作为额外对照。
6. 组合回测：把预测分数转成 TopK/Buffer/Dropout 低频组合，次日开盘执行并扣除交易成本。
7. 额外分析：比较 Ridge/LightGBM/MLP，以及5日、10日、20日预测期限和稳定性、成本、风险归因。

一句话数据流：

```text
Qlib 行情 + Tushare 财务/估值
  -> PIT 合并与股票状态清洗
  -> 约270个候选因子
  -> 人工25因子 + DeepSeek准入增量因子
  -> 冻结30因子
  -> 横截面去极值/中性化/标准化
  -> Purged Walk-Forward 滚动训练
  -> 模型预测分数
  -> 输出端再中性化 + TopK/Buffer/Dropout
  -> 次日开盘成交的组合回测
```

### 1.1 当前明确支持的研究对象

- 频率：日频。
- 市场：以沪深 A 股为主；数据拉取可包含北交所，但若干下游模块会排除 `BJ`，详见第 12 节。
- 主预测目标：`label_ret_5`；`label_ret_10`、`label_ret_20` 为额外周期敏感性分析。
- 主要模型：LightGBM。
- 对照模型：Ridge 线性模型、PyTorch MLP。
- 组合基准：沪深 300，代码 `SH000300`。
- 默认组合：Top50，低频调仓（当前配置 `rebalance_days=20`），Buffer=30，单次最多主动换出5只；报告中应以实际配置说明“周频”与20日实现的区别。

### 1.2 本项目不是什么

- 不是实盘交易系统：没有券商下单、盘中行情、订单回报和实盘风控接入。
- 不是分钟级高频系统：因子、标签和回测均以日线为基础。
- 不是保证收益的策略产品：现有结果是历史研究结果，仍受样本偏差、成本假设和市场结构变化影响。
- 不是“一键命令覆盖所有研究步骤”的完全自动化平台；`main.py all` 只覆盖默认主链，具体边界见第 10 节。

## 1.3 全项目主流程（按真实运行前后顺序）

本文不再把关键说明作为附录，而是按照程序实际运行顺序理解整个项目。完整主链如下：

```text
第一阶段 数据准备
  -> 第二阶段 构建270个候选因子库
  -> 第三阶段 从270个因子筛选58个，再确定25个人工核心因子
  -> 第四阶段 以人工25因子为基准，准备并运行DeepSeek增量因子挖掘
  -> 对DeepSeek候选进行合法性、缺失率、RankIC、增量IC、冗余和经济逻辑检查
  -> 通过10bps成本下的周频准入回测，注册并冻结5个增量因子
  -> 第五阶段 用25+5=30个因子训练最终LightGBM模型
  -> 第六阶段 将预测分数交给TopK/Buffer/Dropout组合回测
  -> 额外分析：Ridge/LightGBM/MLP模型对比；5日/10日/20日标签对比
```

每一阶段的“上一阶段产物”都是下一阶段的输入，不能跳过：数据准备产生可用的行情、财务和状态数据；270因子库产生候选特征；人工筛选产生 `human_core_25_v1.json`；DeepSeek阶段只在这25个因子之外寻找增量信息；最终模型再冻结25个人工因子和5个准入增量因子。主流程中的详细说明、文件、阈值、经济逻辑和面试问答，分别按上述阶段编排在本文后续对应章节中。

---

## 2. 最重要的研究纪律

### 2.1 固定时间区间

时间划分来自 `config/settings.py`：

| 区间 | 日期 | 用途 |
|---|---|---|
| 训练集 | 2016-01-01 至 2023-04-30 | 历史拟合、因子基础统计、滚动训练窗口 |
| 验证集 | 2023-05-01 至 2025-01-01 | 标签、因子、模型和超参数选择 |
| 测试集 | 2025-01-02 至 2026-08-13 | 参数冻结后的最终一次评估 |
| 配置的数据总区间 | 2016-01-01 至 2026-12-31 | 拉取/加工允许覆盖的边界；不代表数据源已经更新到该日 |

注意：训练、验证、测试是“研究用途”的分区；滚动模型内部还会对每一步构造局部 `train / valid / test` 窗口。两者不是同一个概念。局部 `test` 指当前滚动步的样本外预测段，不等同于全局最终测试集。

主研究纪律是：270因子筛选、25因子确定、DeepSeek候选准入、LightGBM模型选择和组合参数选择全部只能使用训练集和验证集；测试集只在所有方案冻结后做一次最终报告。模型层通常采用最近750个交易日训练、60个交易日局部验证、20个交易日向前预测的 Purged Walk-Forward；另有年度5+1+1滚动分析和多折 Purged Walk-Forward 调参，它们是不同用途，不能混称。

### 2.2 防未来函数的四层措施

1. 行情因子只使用当日及以前的数据。所有滚动计算必须按 `symbol` 分组并按日期排序。
2. 标签使用未来价格，例如 `close.shift(-20) / close - 1`，且任何 `label_ret_*` 列都禁止进入特征集合。
3. 财务数据按公告日 `ann_date` 向后匹配，而不是按报告期 `end_date` 直接匹配。
4. 滚动训练按标签周期设置 purge gap。例如 20 日标签要求训练末端与后续窗口至少隔离 20 个交易日，避免训练样本的未来收益区间穿入验证或预测区间。
5. DeepSeek 因子不能直接写进最终模型；必须先经过候选公式校验、训练期 `label_ret_5` 统计、与人工25因子去重，以及统一10bps成本下的周频/低频组合准入。

### 2.3 测试集审计

- Ridge、MLP、LightGBM 的测试段评估都要求显式确认参数。
- 模型对比默认只比较验证集；比较测试集必须显式传入确认参数。
- 组合回测默认只跑验证集。
- 最终测试组合回测成功后会写入 `reports/portfolio/final_test_audit.json`。该文件是一次性锁，后续测试集回测会被拒绝。
- 2026-08-24 首次最终测试后发现“开盘成交使用全天涨跌停状态”的实现错误。纠错时没有删除或覆盖原审计锁，而是使用独立入口 `backtest/run_test_correction.py`，把新结果写入 `reports/portfolio/test_corrections/open_limit_v2/`，并记录原/新配置哈希和 `parameters_retuned=false`。
- 这个锁只能约束统一组合回测入口，不能阻止研究者直接打开 Parquet 或修改代码，因此真正的纪律仍依赖流程管理。

### 2.4 应如何理解“样本外”

模型对某个 20 日预测段的预测，来自该段之前的训练和局部验证数据，因此是滚动样本外预测。但如果反复依据全局验证期的结果调整模型，它仍属于“验证集研究结果”，不能当作最终不可见测试结果。

---

## 3. 仓库结构与职责

```text
QUANT-ASHARE/
├─ main.py                         流水线命令分发与默认执行顺序
├─ config/settings.py              路径、日期和数据源的集中配置
├─ scripts/                        数据拉取、清洗、因子构建和 Qlib 基线入口
├─ factors/
│  ├─ registry.py                  自研因子注册表
│  ├─ operators.py                 通用时序算子
│  └─ base/                        9 个实现文件、7 个已启用因子类别
├─ preprocessing/
│  ├─ preprocess_factors.py        正式模型特征预处理
│  ├─ neutralize.py                独立中性化诊断/替代产物
│  └─ build_features.py            辅助特征构建逻辑
├─ analysis/                       IC、冗余、分层、标签与模型比较
├─ quantmind_integration/          DeepSeek/QuantMind候选解析、评估、比较和周频准入
├─ models/
│  ├─ rolling.py                   Ridge/MLP 共用滚动工具
│  ├─ lightgbm/                    LightGBM 数据、训练、评估、Optuna
│  ├─ ridge/                       Ridge 基线
│  └─ mlp/                         PyTorch MLP
├─ backtest/
│  ├─ config.py                    冻结的组合参数
│  ├─ alpha_signal.py              输出分数中性化与选股
│  ├─ engine.py                    次日开盘成交回测引擎
│  └─ run_backtest.py              验证/测试回测入口和审计锁
├─ data/raw/                       原始行情和财务数据
├─ data/processed/                 中间表、因子、特征、标签和预测
├─ reports/                        因子、模型和组合报告
│  ├─ quantmind_trials/             每个DeepSeek候选的公式、指标和失败原因
│  └─ factor_mining_memory.json      挖掘记忆与历史失败/准入信息
├─ mlruns/                         MLflow 本地实验记录
├─ tests/                          时间隔离、信号、回测和模型形状测试
└─ knowledge/                      因子、组合、评估和风险知识说明
```

`PROJECT_BLUEPRINT.md` 更偏目标蓝图，`README.md` 更偏运行速查；本文档负责解释当前实现本身。代码与配置发生冲突时，应优先相信代码和 `config/settings.py`。

---

## 4. 数据层：运行索引

本节保留为快速索引；数据准备的文件清单、时点逻辑、经济含义、面试问答和优化建议见第41节，避免同一环节出现两套口径。

### 4.1 `fetch_daily`：从 Qlib 导出日线

入口：`scripts/fetch_daily.py`。

数据源默认位于 `~/.qlib/qlib_data/cn_data`，股票清单默认使用 Qlib 的 `all` instruments。脚本按股票分批读取并按交易所前缀落盘：

- `data/raw/daily/daily_sh.parquet`
- `data/raw/daily/daily_sz.parquet`
- `data/raw/daily/daily_bj.parquet`

主要字段包括 `open/high/low/close/volume/amount/vwap/factor/adjclose`。命令可指定起止日期、股票数量、批大小和输出目录。

当前本地原始行情文件约为：沪市 170 MB、深市 223 MB、北交所 12 MB。文件大小只用于理解规模，不是质量保证。

### 4.2 `fetch_financial`：拉取财务报表并生成 PIT 事件表

入口：`scripts/fetch_financial.py`。

数据来自 Tushare，token 读取顺序为环境变量 `TUSHARE_TOKEN`，其次是项目根目录 `.env`。原始文件包括利润表、资产负债表、现金流量表和财务指标表。

脚本只保留配置要求的合并报表口径，并支持断点续传、定期 checkpoint、失败重试和请求间隔。真正供行情合并的是 `data/processed/financial/financial_events.parquet`。

该表以 `(symbol, ann_date)` 表示“某股票在某公告日开始可知的最新财务状态”。同一股票、同一公告日的重复记录会按脚本规则保留最后一条。若公告日缺失而回退到报告期，会带来潜在未来函数风险，应查看运行警告并审计数据覆盖。

### 4.3 `merge_data`：日线和财务事件的时点合并

入口：`scripts/merge_data.py`。

核心语义相当于：

```python
merge_asof(daily, financial_events,
           left_on="date", right_on="ann_date",
           by="symbol", direction="backward")
```

也就是在每个交易日只取得 `ann_date <= date` 的最近一份财务状态。当前实现合并沪深日线，不把北交所文件纳入该底表，因此“原始数据含北交所”不等于“主研究样本含北交所”。输出为 `data/processed/merged_data.parquet`。

停牌日行情缺失不会被随意填成正常交易价格；财务缺失也不会在此阶段统一填零。

### 4.4 `fetch_stock_basic`：上市日和历史名称

入口：`scripts/fetch_stock_basic.py`。

输出 `stock_basic.parquet` 和 `namechange.parquet`。历史名称表用于按当日真实名称识别 ST 和退市状态，避免拿今天的股票名称反推过去。上市日期优先从名称历史推断，Qlib instruments 提供兜底。

### 4.5 `clean_data`：股票状态和基础样本清洗

入口：`scripts/clean_data.py`。主要处理顺序为：

1. 按 `(symbol, date)` 去重。
2. 排除指数代码。
3. 使用历史名称区间识别当日 ST/退市状态；没有历史名称表时才退回较弱的当前名称判断。
4. 排除上市不足 60 天的次新股。
5. 保留停牌导致的行情 NaN，不虚构交易。
6. 财务字段在股票内部向前填充；从未披露的数据仍为 NaN。
7. 根据板块规则标记涨跌停与一字板。

涨跌停规则考虑主板 10%、科创板 20%、创业板 2020-08-24 前后 10%/20%、北交所 30%。正式预处理阶段还能获得 Tushare 官方 `pct_chg`，普通涨跌停标记会在那里再校正一次；一字板标记仍依赖清洗底表中的高低价判断。

输出：`data/processed/basic_cleaned.parquet`。

### 4.6 `fetch_daily_basic`：估值、市值、行业和官方涨跌幅

入口：`scripts/fetch_daily_basic_by_date.py`。

这个步骤必须在 `clean_data` 之后执行，因为它读取已清洗股票池和交易日。脚本按交易日请求 Tushare，补充总/流通市值、`pe_ttm/pb/ps_ttm`、换手率、官方 `pct_chg` 和行业。

输出 `basic_cleaned_with_extra_by_date.parquet`。当前该表有 10,427,948 行、130 列，约 2.65 GB。

---

## 5. 因子层：270 个候选特征快速索引

本章原有的因子说明已统一收敛到第46节“270因子库构建详解（面试版）”。本章只保留数量和目录索引；因子类别、计算机制、经济逻辑、面试回答和优化方向，以第46节为唯一详细口径。

### 5.1 自研因子注册机制

每个自研因子通过 `factors/registry.py` 注册。`scripts/build_factors.py` 导入全部因子模块后遍历注册表计算，当前实测总数为 112：

| 类别 | 数量 | 典型含义 |
|---|---:|---|
| reversal_momentum | 22 | 反转、动量、跳空、隔夜收益、最大回撤 |
| volatility | 18 | 收益波动、下行波动、ATR、偏度、波动率的波动 |
| liquidity | 18 | 换手、Amihud、量价相关、资金流、成交趋势 |
| technical | 32 | RSI、MACD、KDJ、CCI、布林带、均线乖离 |
| value | 3 | EP、BP、SP |
| quality | 10 | ROE、ROA、ROIC、利润率、偿债和周转 |
| growth | 9 | 收入、利润、现金流及单季成长 |
| composite | 0 | 注册表预留，当前没有启用 |

`price_structure.py` 和 `volume_price.py` 是实现文件，不是额外注册类别；其中因子仍归入上述类别。因此不能按文件数推导类别数。

### 5.2 通用算子约束

`factors/operators.py` 提供收益、均值、标准差、极值、时序排名、相关、决定系数、斜率、线性衰减和历史引用等工具。滚动算子要求完整窗口，窗口不足时返回 NaN，避免用不完整历史制造不可比信号。

因子实现应先按股票、日期排序；安全处理除零；尽量去量纲；财务因子只使用 PIT 字段；横截面排名和标准化必须按交易日进行。

### 5.3 Alpha158

入口：`scripts/build_alpha158.py`。

脚本调用 Qlib `Alpha158.get_feature_config()` 得到 158 个表达式，再由 `D.features` 分批计算，包括 9 个 K 线形态、4 个价格相对特征，以及 29 类滚动特征乘以 5/10/20/30/60 日窗口。

股票池取项目底表和 Qlib all instruments 的交集，并排除指数与 B 股。输出 `alpha158.parquet` 当前有 10,876,393 行、160 列，约 5.9 GB。

### 5.4 `build_factors` 的实际合并逻辑

入口：`scripts/build_factors.py`。执行步骤：

1. 优先读取带 daily_basic 的宽底表；若不存在则退回基础清洗表。
2. 只投影因子真正依赖的底表字段，降低内存。
3. 生成 5/10/20 日未来收益标签。
4. 计算 112 个自研因子。
5. 如 `alpha158.parquet` 存在，按列分批对齐合并；同名时保留自研因子。
6. 写出 `factors.parquet`。

当前 `factors.parquet` 有 10,427,948 行、275 列：2 个主键 + 3 个标签 + 270 个因子，约 12.5 GB。

一个关键点：`build_factors` 在 Alpha158 文件不存在时仍会成功，只是产物只有自研因子。当前 `main.py all` 的顺序是先 `build_factors`、后 `build_alpha158`；如果从空目录首次运行，当次 `factors.parquet` 不会包含 Alpha158，需要再次运行 `build_factors`。这是当前编排的已知顺序问题。

---

## 6. 特征预处理与独立中性化

### 6.1 正式模型输入：`preprocess_factors`

入口：`preprocessing/preprocess_factors.py`。它读取 `factors.parquet` 和带行业、市值、涨跌停信息的宽底表，对每个交易日依次执行：

1. 1%/99% 分位数缩尾。
2. 因子对行业哑变量和 `log(total_mv)` 回归，取残差。
3. 横截面 Z-Score 标准化。
4. 标准差接近零的常数列置 0。

普通涨跌停标记会用官方 `pct_chg` 按板块阈值重算。`pct_chg` 只用于生成标记，不进入模型。

产物：

- `features.parquet`：主键、行业、涨跌停标记、`log_mv` 和 270 个处理后特征；
- `labels.parquet`：主键和三个未经标准化的未来收益标签。

当前两表均为 10,427,948 行；`features.parquet` 有 278 列，约 16.35 GB，`labels.parquet` 有 5 列，约 135 MB。脚本按 Parquet row group 流式处理。`--max-rg` 只用于调试，其产物不是完整训练数据。

### 6.2 独立 `neutralize` 环节的正确定位

入口：`preprocessing/neutralize.py`。它可单独做行业、市值或联合中性化，用于暴露诊断、方法比较或替代研究产物。

当前模型加载的是 `features.parquet`，不是 `neutralized_both.parquet`。默认链条因此是：

```text
factors.parquet -> preprocess_factors -> features.parquet -> models

原始因子/特征 -> neutralize -> 独立诊断或替代研究产物
```

如果未来要让模型使用独立中性化结果，需要显式修改模型数据入口，不能只把 `neutralize` 放进运行顺序就认为已经生效。

---

## 7. 因子评价和筛选漏斗

### 7.1 RankIC 评价

`analysis/evaluate_factors.py` 对每个交易日计算因子与目标收益的 Spearman 横截面相关，再汇总均值、标准差、ICIR、正 IC 比例和统计显著性。当前 `daily_rankic.parquet` 有 1,781 个日期和 270 个因子序列。

RankIC 为负不等于无效，只表示方向相反，可在构造多空组合时翻转方向。

### 7.2 冗余剔除

`analysis/remove_redundant_factors.py` 先过滤弱信号，再根据日频 IC 序列之间的相关性去冗余。这里比较的是因子效果随时间的共同变化，不是原始因子值相关性。

当前报告快照：初始 270 个因子，`|IC| < 0.01` 剔除 32 个，相关阈值 0.70，最终保留 58 个，清单写入 `selected_factor_cols.json`。

### 7.3 五分组检验

`analysis/factor_quantile.py` 每日把因子分为五组，按训练期 IC 符号统一方向，评价高低组收益、年化收益、Sharpe、最大回撤、换手和单调性。当前报告采用 5 日调仓。

通过规则写入 `passed_factor_cols.json`。模型优先读取这份清单，而不是直接读取 `selected_factor_cols.json`：前者是分组检验后的建模候选，后者是 IC 与冗余筛选结果。

### 7.4 最终入选的 25 个因子

最终建模清单来自 `data/processed/passed_factor_cols.json`。这里的“最终入选”是指通过当前代码定义的因子筛选漏斗，并不等于已经通过最终测试集确认。

当前筛选配置为：

| 配置 | 当前值 | 含义 |
|---|---:|---|
| 初始候选 | 58 | 已经过 IC 强度、稳定性和冗余筛选的因子 |
| 最终通过 | 25 | 写入 `passed_factor_cols.json` 并供模型优先读取 |
| 标签 | `label_ret_5` | 未来 5 个交易日收益 |
| 持有期 | 5 日 | 单因子分组组合的调仓/持有周期 |
| 分组数 | 5 | 每日横截面分为 Q1–Q5 |
| 收益门槛 | 训练期对齐后年化多空收益 > 0 | 因子方向按训练期 IC 正负统一 |
| 单调性门槛 | 训练期 `|monotonic_corr| > 0.8` | 代码使用严格大于；JSON 中的 `0.8` 是记录阈值 |
| 方向来源 | 仅训练期 | 防止用验证期收益决定因子正负方向 |

下面的“方向”表示进入分组分析时根据训练期 IC 采用的方向：`+` 表示因子原值越大越看多，`-` 表示将原始方向反转后使用。“训练年化”和“验证年化”均为 IC 方向对齐后的单因子多空年化收益，是研究统计而非最终组合收益。

| 序号 | 因子 | 来源/类别 | 方向 | 训练年化 | 训练单调性 | 验证年化 | 验证单调性 |
|---:|---|---|:---:|---:|---:|---:|---:|
| 1 | `LOW_TURNOVER` | 自研·流动性 | + | 44.70% | 1.00 | 36.56% | 1.00 |
| 2 | `LN_TURNOVER` | 自研·流动性 | - | 41.98% | -1.00 | 36.82% | -0.90 |
| 3 | `CORR20` | Alpha158·量价相关 | - | 27.68% | -1.00 | 25.59% | -1.00 |
| 4 | `VOL_CV20` | 自研·流动性 | + | 26.04% | 1.00 | 24.31% | 1.00 |
| 5 | `RET_AMT_CORR20` | 自研·流动性 | + | 24.26% | 1.00 | 14.81% | 1.00 |
| 6 | `KLOW` | Alpha158·K线结构 | - | 24.15% | -1.00 | 17.65% | -0.40 |
| 7 | `CONSEC_UP5` | 自研·反转/动量 | + | 23.93% | 0.90 | 27.49% | 0.10 |
| 8 | `CORD60` | Alpha158·收益量相关 | - | 21.15% | -1.00 | 11.60% | -1.00 |
| 9 | `RET_VOL_CORR5` | 自研·流动性 | + | 19.19% | 1.00 | 14.23% | 1.00 |
| 10 | `OBV_CHG_20` | 自研·技术指标 | - | 19.16% | -0.90 | 17.66% | 0.00 |
| 11 | `CORD5` | Alpha158·收益量相关 | - | 18.87% | -1.00 | 14.89% | -1.00 |
| 12 | `AMIHUD_20` | 自研·流动性 | + | 18.33% | 1.00 | 6.73% | 0.70 |
| 13 | `VOL_OF_VOL_20` | 自研·波动率 | + | 16.47% | 1.00 | 17.45% | 1.00 |
| 14 | `CORR60` | Alpha158·量价相关 | - | 15.14% | -1.00 | 14.82% | -1.00 |
| 15 | `VOL_PRICE_DIVERGENCE` | 自研·流动性 | + | 15.01% | 1.00 | 9.21% | 0.90 |
| 16 | `BP` | 自研·价值 | + | 12.43% | 1.00 | 12.40% | 1.00 |
| 17 | `EP` | 自研·价值 | + | 11.96% | 1.00 | 10.37% | 1.00 |
| 18 | `CNTD10` | Alpha158·涨跌天数 | - | 10.96% | -0.90 | 17.22% | -0.90 |
| 19 | `IMAX60` | Alpha158·高点位置 | - | 10.43% | -0.90 | 9.60% | -0.30 |
| 20 | `WVMA30` | Alpha158·量价波动 | - | 9.74% | -1.00 | 14.21% | -0.90 |
| 21 | `MAX_DD60` | 自研·反转/动量 | - | 9.42% | -1.00 | 11.00% | -0.30 |
| 22 | `CNTD60` | Alpha158·涨跌天数 | - | 8.83% | -1.00 | 6.04% | -0.40 |
| 23 | `Q_SALES_YOY` | 自研·成长 | + | 7.52% | 0.90 | 1.49% | 0.30 |
| 24 | `VSTD5` | Alpha158·成交量波动 | - | 6.99% | -1.00 | 6.09% | -0.70 |
| 25 | `NEG_SKEW_20` | 自研·波动率 | + | 6.57% | 0.90 | 13.76% | 1.00 |

#### 7.4.1 25 个因子的结构分布

| 来源/类别 | 数量 | 因子 |
|---|---:|---|
| Alpha158 | 10 | `CORR20`、`KLOW`、`CORD60`、`CORD5`、`CORR60`、`CNTD10`、`IMAX60`、`WVMA30`、`CNTD60`、`VSTD5` |
| 自研·流动性 | 7 | `LOW_TURNOVER`、`LN_TURNOVER`、`VOL_CV20`、`RET_AMT_CORR20`、`RET_VOL_CORR5`、`AMIHUD_20`、`VOL_PRICE_DIVERGENCE` |
| 自研·反转/动量 | 2 | `CONSEC_UP5`、`MAX_DD60` |
| 自研·波动率 | 2 | `VOL_OF_VOL_20`、`NEG_SKEW_20` |
| 自研·价值 | 2 | `BP`、`EP` |
| 自研·技术指标 | 1 | `OBV_CHG_20` |
| 自研·成长 | 1 | `Q_SALES_YOY` |

最终清单明显偏向流动性和量价类信号：10 个 Alpha158 中大多数也是量价相关、成交量波动或价格路径位置特征；加上 7 个自研流动性因子，25 个因子中至少 17 个与价格—成交行为直接相关。这符合 A 股日频横截面中交易行为信息较强的经验，但也意味着组合可能对流动性状态和小盘风格较敏感，必须在输出端继续做市值中性化，并补充市场状态稳定性分析。

#### 7.4.2 每个入选因子的研究含义

- `LOW_TURNOVER`：低换手信号，原方向为正。它可能捕捉低关注、低拥挤或流动性溢价，也可能隐含小盘和低流动性风险。
- `LN_TURNOVER`：换手率的对数变换，训练期 IC 为负，使用时反向。它与 `LOW_TURNOVER` 经济含义接近，但非线性变换后的横截面分布不同。
- `CORR20/CORR60`：Alpha158 的中长期量价相关特征，当前方向为负，表示较低或更负的价格—成交量相关关系在样本内更有利。
- `VOL_CV20`：成交量变异系数，刻画成交活跃度相对于均值的波动。
- `RET_AMT_CORR20`：20 日收益与成交额的相关关系，试图识别价格变化是否获得资金成交确认。
- `KLOW`：下影线相对 K 线结构。当前使用反向方向；验证期收益仍为正，但五组单调性降至 -0.40。
- `CONSEC_UP5`：近 5 日连续上涨状态。当前训练方向为正，但验证单调性仅 0.10，说明收益可能集中在极端组而非稳定逐组递增。
- `CORD5/CORD60`：Alpha158 中收益变化与成交量变化的短期/中期相关特征，均按负方向使用。
- `RET_VOL_CORR5`：5 日收益和成交量相关，关注短期量价配合或背离。
- `OBV_CHG_20`：20 日 OBV 变化，衡量按涨跌方向累积的成交量趋势。当前反向使用，验证期单调性降为 0。
- `AMIHUD_20`：单位成交额对应的绝对收益冲击，数值越高通常越不流动。它可能获得流动性风险补偿，但容量和冲击成本风险也更高。
- `VOL_OF_VOL_20`：波动率自身的波动，刻画风险状态是否稳定。该因子训练和验证单调性均为 1.00。
- `VOL_PRICE_DIVERGENCE`：成交量趋势与价格趋势的背离，试图捕捉资金与价格走势不一致后的修复。
- `BP`：账面市值比，代表价值暴露；当前原方向为正。
- `EP`：盈利收益率，代表单位价格对应的盈利能力；需注意负利润和异常估值处理。
- `CNTD10/CNTD60`：Alpha158 的上涨与下跌天数差异类特征，当前均反向使用，体现价格路径持续性或反转。
- `IMAX60`：60 日窗口中高点出现位置的归一化特征，反映近期是否刚创新高以及高点距离当前有多远。
- `WVMA30`：成交量加权的收益波动类特征，当前反向使用，倾向较低的量价联合波动。
- `MAX_DD60`：60 日最大回撤，当前反向使用。解释方向时必须结合代码定义：若原值本身为负回撤，乘以负方向后可能对应偏好更大或更小回撤，不能只凭名称判断。
- `Q_SALES_YOY`：单季度销售收入同比成长，使用公告日 PIT 数据。训练期通过，但验证年化仅 1.49%、单调性 0.30，是明显衰减的基本面因子。
- `VSTD5`：5 日成交量标准差类特征，当前反向使用，偏好较稳定的短期成交量状态。
- `NEG_SKEW_20`：20 日负偏度或下行不对称风险特征。训练、验证收益和单调性均保持正向，但仍需确认具体实现的符号定义。

#### 7.4.3 对这 25 个因子的正确解读

这份清单有三个面试中值得主动说明的特点：

1. **它是训练规则筛出的清单**。通过条件只使用训练期收益和训练期单调性，验证列用于观察外推情况，没有参与当前 `passed` 判定。
2. **验证期收益全部仍为正，但并非全部保持单调**。`KLOW`、`CONSEC_UP5`、`OBV_CHG_20`、`IMAX60`、`MAX_DD60`、`CNTD60`、`Q_SALES_YOY` 等验证单调性明显下降，说明“方向仍有收益”与“横截面五组稳定单调”是不同结论。
3. **存在经济含义重叠**。例如 `LOW_TURNOVER` 与 `LN_TURNOVER`、`CORR20` 与 `CORR60`、`CORD5` 与 `CORD60`、`CNTD10` 与 `CNTD60` 都是同类信号的不同变换或窗口。前序冗余筛选基于 0.70 阈值仍允许它们同时存在，模型阶段还应观察增量贡献和稳定性。

模型读取这 25 个因子时不会按表中 IC 方向手工乘以 `+1/-1`；`passed_factor_cols.json` 只保存名称，LightGBM、Ridge 或 MLP 会直接从预处理后的原始方向特征中学习系数或非线性关系。IC 方向对齐主要用于单因子分组筛选和结果解释。对于 Ridge，预期系数方向应与训练期 IC 大致相容，但受共线性影响不一定逐项一致。

### 7.5 正确研究顺序

```text
evaluate_factors -> remove_redundant -> factor_quantile -> preprocess/models
```

当前 `main.py all` 没有执行前两个步骤，却会执行 `factor_quantile`，因此它依赖已有评估产物；从空白状态运行并不能自动完成整个筛选漏斗。

---

## 8. 模型层：LightGBM运行机制与超参数优化快速索引

本章保留模型结果速查；LightGBM 的详细运行机制、经济逻辑、超参数含义和面试回答统一见第43节，以第43节为当前实现的唯一详细口径。

### 8.1 数据入口与特征安全

模型从 `features.parquet` 和 `labels.parquet` 按主键合并。特征选择会排除主键、行业、市值、涨跌停状态和所有标签列。如果 `passed_factor_cols.json` 存在且有效，则优先只使用其中因子，否则使用全部合法特征。

### 8.2 Purged Walk-Forward 窗口

当前 LightGBM 主训练入口默认训练长度 750 个交易日，局部验证长度 60 日，滚动步长 20 日；purge 长度从标签名解析，5/10/20 日标签分别隔离对应交易日数。另有独立的 `scripts/backtest_walkforward.py` 使用年度 5+1+1 窗口，不能把两者混称。

```text
[750日训练] --purge-- [60日局部验证] --purge-- [20日样本外预测]
```

每一步都重新训练。局部验证用于当前滚动模型的早停或监控，全局验证集才用于方案选择。

### 8.3 LightGBM

入口：`models/lightgbm/train.py`。默认回归目标、最多 1000 轮、早停 50 轮，默认标签为 `label_ret_20`。训练样本默认剔除普通涨跌停日，也可改为只剔一字板或不剔除。

每步模型写入 `models/lightgbm/<label>/step_xxx/lgb_model.txt`，预测写入 `data/processed/predictions/lgb_pred_<label>.parquet`。

### 8.4 Optuna

`models/lightgbm/optuna_search.py` 默认执行 30 次 trial，以验证集 RankIC 选参数，结果按标签存入 `models/lightgbm/best_params.json`。

当前快照：

| 标签 | trial 数 | 最优验证 RankIC |
|---|---:|---:|
| 20 日 | 30 | 0.120692 |
| 10 日 | 30 | 0.117672 |
| 5 日 | 30 | 0.108693 |

这些是调参目标值，不是最终组合收益，也不是独立测试结论。

### 8.5 Ridge 与 MLP

`models/ridge/train.py` 是线性基线，默认 `alpha=10`。它用来判断复杂模型是否真的提供非线性增益。

`models/mlp/train.py` 默认 20 个 epoch、早停耐心 4、batch size 4096、学习率 `1e-3`、权重衰减 `1e-4`、dropout 0.15，并限制训练/验证最大样本数以控制成本。

两者复用 `models/rolling.py` 的严格滚动工具。

### 8.6 模型比较

`analysis/compare_models.py` 在同一标签、同一验证区间比较 Ridge、LightGBM 和 MLP。实际选择不能只机械按 RankIC 第一名：本次 MLP 的 RankIC 略高，但 LightGBM 的 Pearson IC、多空收益和统一组合转化更好，因此最终将 `selected_model.json` 冻结为 LightGBM。

当前 `label_ret_20` 验证集比较结果：

| 模型 | IC | RankIC | RankICIR | Top-Bottom |
|---|---:|---:|---:|---:|
| Ridge | 0.082064 | 0.091403 | **18.189793** | 0.023892 |
| LightGBM | **0.092976** | 0.096664 | 16.948402 | **0.026998** |
| MLP | 0.081718 | **0.098246** | 15.781224 | 0.022757 |

选择逻辑是：Ridge证明线性因子稳定有效；MLP证明更复杂非线性能够略微改善全截面排序；LightGBM虽然不是每项第一，但头部/尾部收益分离最好、实际组合转化最好，同时工程稳定性和训练成本优于MLP。

统一组合回测的 `--model auto` 会读取该文件。如果它不存在，说明还没有完成统一模型选择。

### 8.7 LightGBM完整运行机制

LightGBM 在本项目中的任务不是预测某只股票的精确价格，而是利用约 30 个最终因子，对每个交易日的股票做横截面排序，输出 `pred` 分数，再交给 TopK 组合模块选股。涉及文件：`models/lightgbm/train.py`、`models/lightgbm/config.py`、`models/lightgbm/data.py`、`models/lightgbm/evaluate.py`、`models/rolling.py` 和 `data/processed/features.parquet`、`labels.parquet`。

输入特征排除 `symbol/date`、行业、市值、涨跌停状态和所有标签列；如果存在 `passed_factor_cols.json`，优先使用其中通过分层检验的因子。标签可以是 `label_ret_5`、`label_ret_10` 或 `label_ret_20`。训练时默认剔除涨停和跌停样本，因为封板股票可能无法买入或卖出，把它们当作普通可交易样本会制造虚假信号。

### 8.8 滚动训练和防泄漏

`models/rolling.py` 和 `train.py` 默认使用：

```text
750日训练 → purge → 60日局部验证 → purge → 20日样本外预测
```

每一步模型都会重新训练，窗口向前移动。滚动训练体现市场状态会变化，较近的数据可能比很久以前的数据更有参考价值。20 日是模型更新步长，不是组合调仓周期；周频调仓由回测配置单独控制。

Purged Walk-Forward 根据标签周期隔离边界：5/10/20 日标签分别隔离相应交易日数。原因是 t 日标签会使用 t+h 的未来价格，若训练和验证边界太近，未来收益窗口可能重叠，导致隐性泄漏。

### 8.9 每个滚动窗口怎样训练

每一步依次完成：

1. 按日期取训练、局部验证和预测区间；
2. 删除标签缺失、非有限值和不可交易封板样本；
3. 构造 LightGBM Dataset；
4. 最多训练 1000 轮；
5. 局部验证连续 50 轮没有改善时早停；
6. 用最佳迭代轮数预测下一段日期；
7. 保存模型和训练/验证边界。

模型保存到 `models/lightgbm/<label>/step_xxx/lgb_model.txt`，预测保存到 `data/processed/predictions/lgb_pred_<label>.parquet`。预测文件不保存真实未来收益，只保存股票、日期、分数、区间和模型边界，避免答案随预测文件泄漏给下游。

### 8.10 超参数与经济含义

配置文件：`models/lightgbm/config.py`。

`learning_rate` 控制每棵树学习的步幅；`num_leaves` 和 `max_depth` 控制非线性复杂度；`min_data_in_leaf` 防止模型为少量股票噪声建立规则；`lambda_l1/lambda_l2` 抑制过拟合；`feature_fraction` 和 `bagging_fraction` 通过随机抽取因子和样本降低相关性与模型方差；`num_boost_round` 设定最大树数，早停决定实际训练多久。

这些参数背后的思想不是“模型越复杂越好”，而是在捕捉因子非线性关系的同时，限制模型记忆某些日期、股票或偶然市场阶段。最终关注的是排序、头部收益、换手和组合表现，而不是单纯训练误差最低。

### 8.11 Optuna 超参数优化

文件：`models/lightgbm/optuna_search.py`。Optuna 不读取最终测试集，也不是用全部滚动窗口反复试验。它在测试集之前取一个快速窗口：最近 750 日训练、隔离标签周期、最近 60 日验证。每次 trial 用 TPE 采样参数，训练 LightGBM 并早停，以验证窗口平均 RankIC 为最大化目标。

搜索范围包括：学习率 0.005—0.1、叶子数 8—256、深度 3—12、最小叶样本 5—100、L1/L2 正则 1e-4—100、特征采样和样本采样 0.4—1.0。默认 30 次 trial，`--quick` 的 3 次仅用于检查流程。最优参数按标签写入 `models/lightgbm/best_params.json`，再由 `train.py` 用于完整滚动训练。

为什么优化 RankIC：本项目最终是排序选股，RankIC 比 MSE 更接近因子把强弱股票排对的能力；但它仍只是模型层指标，最终还必须用 TopK、交易成本、回撤和容量验证。

### 8.12 面试回答模板

> 我使用 LightGBM 学习人工核心因子和通过准入的增量因子与未来收益之间的非线性关系。为了防止时间泄漏，我采用 Purged Walk-Forward，每次用最近 750 个交易日训练、60 个交易日早停验证，再预测未来 20 个交易日。Optuna 只在测试集之前的验证窗口以 RankIC 为目标搜索学习率、树复杂度和正则参数。最终模型不是按 MSE 直接决定，而是结合验证 RankIC、头部收益、换手、成本和 TopK 组合表现选择。

面试官若问“为什么不用随机 K 折”，回答：股票市场有时间顺序，随机切分会让未来状态进入训练集；若问“为什么不用最复杂的模型”，回答：复杂度只有在样本外排序和扣成本组合中带来稳定增益才有价值。

### 8.13 当前优化方向

项目同时保留 `models/lightgbm/optuna_search_v2.py` 的多折 Purged 搜参路径，以及 `scripts/backtest_walkforward.py` 的年度 5+1+1 独立回测路径；`models/lightgbm/train.py` 的生产滚动训练仍是 750/60/20 日窗口。后续应比较这三种时间结构，并明确最终报告采用哪一种，不能把 v2 搜参折数、年度 5+1+1 回测和生产滚动窗口写成同一个流程。

### 8.14 LightGBM及最终组合评分标准

项目评分分为三层，最终以最终30因子 LightGBM 模型及其组合结果为准，不能只看单因子或单一数字：

```text
第一层：模型有没有预测能力
第二层：因子/模型信号是否稳定且有增量
第三层：转成真实组合后是否可交易、能否扣成本赚钱
```

本项目主评分对象固定为最终30因子 LightGBM 模型，主评分口径固定为 `label_ret_5`，即 t 日模型分数预测未来 5 个交易日收益，并与周频调仓保持一致。`label_ret_10` 和 `label_ret_20` 只用于额外周期敏感性分析，不用于修改最终30因子模型、25个人工因子、DeepSeek准入标准或最终组合参数。

#### 8.14.0 基准与年化超额收益口径

正式组合回测的基准是沪深 300 指数，代码标识为 `SH000300`，配置位置为 `backtest/config.py` 的 `PortfolioConfig.benchmark`。回测通过 `backtest/run_backtest.py` 从 `data/raw/daily/daily_sh.parquet` 读取该指数收盘价，构造基准净值；策略和基准从同一回测起点归一化为 1 后比较。

代码中的年化收益为：

```text
策略年化收益 = (期末策略净值 / 期初策略净值)^(1/年数) - 1
基准年化收益 = (期末基准净值 / 期初基准净值)^(1/年数) - 1
年化超额收益 = 策略年化收益 - 基准年化收益
```

例如策略年化 8%，沪深 300 年化 5%，项目报告的年化超额就是 3 个百分点。它不是“策略净值减去基准净值后再简单年化”，也不是单日收益差的简单平均；代码使用两者各自的复合年化收益之差。信息比率则使用每日策略收益减去每日基准收益后的主动收益序列，再按年化因子计算。

面试时应明确说：沪深 300 是相对收益参照，不代表策略必须复制沪深 300 的行业权重；当前组合有自己的 TopK、行业上限和交易约束。因此评价最终30因子 LightGBM 时，年化超额收益必须和绝对收益、Sharpe、最大回撤、换手、成本及行业暴露一起看。

#### 8.14.1 第一层：模型预测能力

由 `models/lightgbm/evaluate.py` 和 `analysis/compare_models.py` 计算：

| 指标 | 含义 | 使用方式 |
|---|---|---|
| Pearson IC | 预测分数和未来收益的线性相关 | 观察收益幅度预测能力 |
| RankIC | 预测排名和未来收益排名的相关 | 主要排序指标 |
| ICIR / RankICIR | IC均值除以IC标准差，并按年化口径报告 | 衡量稳定性，不单独决定入选 |
| IC positive ratio | 每日IC为正的比例 | 判断方向是否经常反转 |
| Top group mean | 预测最高分组的平均未来收益 | 观察真正买入区域 |
| Top-Bottom spread | 最高组减最低组收益 | 观察分层区分度 |
| HAC t值 | 对重叠多日标签自相关进行修正后的显著性 | 作为谨慎统计参考 |

模型比较时优先看 `label_ret_5` 验证集 RankIC 和 Top-Bottom，再看 ICIR、稳定性和组合转化；测试集只在最终冻结后报告，不能拿来选模型。

#### 8.14.1.1 LightGBM分数本身如何计算与判断

LightGBM输出的 `pred` 是模型对“未来5个交易日收益”的回归预测分数，不是概率、不是百分比收益承诺，也不是最终组合收益。模型先根据30个因子在每个交易日学习非线性关系和因子交互，再为当日每只股票输出一个连续分数：

```text
30个因子
  -> LightGBM多棵决策树逐步拟合残差
  -> 每棵树的叶节点输出累加
  -> 得到股票横截面pred分数
  -> 按pred从高到低排序
  -> TopK/Buffer/Dropout组合选股
```

分数的判断分为三步：

1. **方向检查**：同一天内，分数较高的股票是否平均具有更高的未来5日收益。若 RankIC 长期为负，需要检查标签方向、因子方向和实现口径，不能只因为某一段收益好就手动翻转 `pred`。
2. **排序能力检查**：计算每日 Pearson IC、RankIC、RankICIR、正IC比例、Top组收益和 Top-Bottom 多空收益。由于组合按排名选股，最终30因子 LightGBM 以验证期 RankIC 和 Top-Bottom 为主要模型分数依据；MSE只是训练目标，不是最终投资评分。
3. **组合转化检查**：将 `pred` 交给组合层，在同一 TopK、调仓、行业约束、涨跌停、停牌和10bps成本下回测。只有当高分股票的排序优势能够转化为扣成本后的年化超额、Sharpe和可接受回撤，模型分数才具有实际投资意义。

项目没有把 RankIC、Sharpe 和年化超额强行加权成一个人为综合分数，因为三者量纲和含义不同。实际选择顺序是：**先看验证期预测能力，再看稳定性与增量性，最后看扣成本组合结果**。最终30因子模型至少应记录以下评分信息：`label_ret_5`、验证区间、Pearson IC、RankIC、RankICIR、Top-Bottom、净Sharpe、年化净超额、最大回撤、双边换手和年化成本率。测试集只用于参数冻结后的最终报告，不能参与分数权重或模型选择。

#### 8.14.2 第二层：因子信号评分（5日主口径）

单因子进入人工筛选或模型输入前，至少检查：

```text
训练期 |IC均值| >= 0.01
训练期 |ICIR| > 0.05
日频IC相关性阈值：相关性 > 0.70 的同质因子去冗余
五分组IC符号对齐后的年化多空收益 > 0
五分组收益单调性绝对值 > 0.8
```

这些是研究漏斗标准，不是保证未来赚钱的承诺。负 IC 因子使用绝对值筛选，并在分组回测时翻转方向，避免误删稳定反向信号。

#### 8.14.3 第三层：组合交易评分（5日预测、周频调仓）

由 `backtest/run_backtest.py`、`backtest/engine.py` 和 `backtest/weekly_factor_engine.py` 负责。最终组合至少报告：

| 指标 | 关注点 |
|---|---|
| 年化收益 | 策略长期收益水平 |
| 基准年化收益 | 是否只是跟随市场 |
| 年化超额收益 | 相对基准的增量收益 |
| Sharpe | 收益相对于波动的效率 |
| 信息比率 | 超额收益相对于跟踪误差的效率 |
| 最大回撤 | 最坏历史亏损区间 |
| 平均双边换手 | 交易活跃程度和成本压力 |
| 最大再平衡换手 | 单次调仓冲击 |
| 年化成本率 | 交易成本对收益的侵蚀 |
| 行业/规模暴露 | 收益是否来自隐藏风格押注 |
| TopK持仓重合度 | 组合稳定性和换手来源 |

候选因子的 10bps 训练期周频准入门槛来自 `config/quantmind_admitted_registry.json`：净 Sharpe 至少 0.5、年化超额收益不低于 0、最大回撤绝对值不超过 0.5。该标准只用于候选准入，不能替代最终组合完整回测。

#### 8.14.4 评分优先级

面试时可以这样解释：

> 我不会把模型选择简化为 RankIC 排名。第一步看验证期横截面排序能力，第二步看 ICIR、正 IC 比例、分层单调性和增量信息，第三步把预测分数转成 TopK 组合，加入涨跌停、停牌、换手、佣金和印花税后，再比较超额收益、Sharpe、最大回撤和容量。只有三层证据方向一致，才认为模型值得进入最终方案。

#### 8.14.5 不同指标不能直接横向混比

- 5 日、10 日、20 日标签的 IC 和 ICIR 不能不加说明直接比较；
- 重叠多日收益的普通 ICIR 可能偏乐观，要同时看 HAC 和非重叠口径；
- 五分组收益没有完整交易约束，不能直接当作 TopK 净收益；
- 验证 RankIC 高不代表扣成本组合一定好；
- 测试期收益不能参与任何评分权重、阈值或模型选择。

---

## 9. 从预测到可交易组合

### 9.1 输出端再次中性化

非线性模型可能从已中性化特征中重新组合出行业或市值暴露。因此 `backtest/alpha_signal.py` 对每个调仓截面的预测分数再次做 1%/99% 缩尾、行业与 `log(circ_mv)` 回归、残差 Z-Score 和降序排名。

这是输入端和输出端两道不同的控制。

### 9.2 默认组合参数

| 参数 | 默认值 | 含义 |
|---|---:|---|
| topk | 50 | 目标持股数 |
| buffer | 30 | 原持仓跌出前 80 名后才考虑主动移除 |
| max_drop | 5 | 单次主动换出的最多股票数 |
| rebalance_days | 20 | 调仓间隔 |
| initial_cash | 100,000,000 | 初始资金 |
| buy_cost | 0.0005 | 买入费率 |
| sell_cost | 0.0015 | 卖出费率 |
| min_commission | 5 | 单笔最低费用 |
| lot_size | 100 | 交易手数 |
| winsor_quantile | 0.01 | 模型输出分数的横截面缩尾比例 |
| max_industry_weight | 0.20 | 单行业股票数上限比例 |
| use_mean_variance_optimizer | False | 默认等权；开启后使用风险优化权重 |
| max_stock_weight | 0.04 | 优化器模式的单票权重上限 |
| covariance_lookback | 120 | 协方差估计历史交易日数 |
| covariance_min_observations | 40 | 启用完整收缩协方差所需最少收益观察数 |
| alpha_scale | 0.025 | 优化器内预测 Alpha 的缩放系数 |
| risk_aversion | 0.50 | 优化器风险惩罚强度 |
| turnover_penalty | 0.003 | 优化器换手惩罚强度 |
| target_annual_volatility | 0.35 | 目标年化波动率 |
| volatility_lookback | 60 | 已实现组合波动率回看期 |
| minimum_exposure | 0.60 | 最低目标风险仓位 |
| max_rebalance_turnover | 0.15 | 非首次建仓的单次双边成交额预算 |
| drawdown_tiers | -20%/-30%/-40% | 对应仓位上限90%/75%/60% |
| benchmark | SH000300 | 沪深 300 |

缓冲区和 max_drop 用于降低边界换手。不可交易或不在候选截面的股票仍可能造成实际持仓偏离目标。

### 9.3 回测时序与约束

`backtest/engine.py` 按 T 日收盘信号、T+1 开盘成交执行。回测入口先用复权因子还原真实开盘价和前收盘价，只用开盘时已知信息判断开盘是否触及涨跌停：开盘涨停不能买，开盘跌停不能卖，不使用当日收盘后才能知道的 `pct_chg/high/low` 判断开盘成交。

每个交易日先根据此前组合净值计算波动率目标和回撤降仓比例；调仓日再生成股票名单和目标权重。默认采用目标风险仓位内等权，只有 `use_mean_variance_optimizer=True` 时才使用预测 Alpha、收缩协方差和换手惩罚求权重。成交顺序为先卖后买，按100股取整；资金不足、开盘价无效或成交量无效时减少或拒绝订单。停牌持仓使用最近有效收盘价估值。

当前实现没有显式维护 T+1 当日买入不得卖出的持仓批次，但默认20日调仓通常不发生同日往返；若改成日频交易，需要补充该约束。波动和回撤目标每日计算，但交易只在预设调仓日发生，因此非调仓日不会为了刚触发的风险阈值立即卖出。

### 9.4 输出与指标

输出 `nav.parquet`、`trades.parquet`、`holdings.parquet` 和 `summary.json`。净值表还记录目标仓位、已实现波动率和交易前回撤。指标包括年化收益、基准年化、年化超额、波动、Sharpe、信息比率、最大回撤、平均日换手、平均仓位、最大再平衡换手和总成本。

成本模型仍是简化模型：没有成交量参与率、冲击成本和滑点曲线，也未逐项拆分印花税等费用；`sell_cost` 是合并费率假设。

### 9.5 周频调仓策略的经济逻辑、面试解释与优化方向

#### 9.5.1 这套策略到底在做什么

LightGBM 负责回答“哪些股票未来5个交易日相对更强”，组合层负责回答“买哪些、买多少、什么时候换”。因此，模型分数不是直接买入信号，而是每天对股票进行横截面排序；到了预设调仓日，才把排名转换成组合。

当前默认配置是：持有 Top50，20 个交易日调仓一次，原持仓使用 30 名缓冲区，单次最多主动换出 5 只，次日开盘成交。研究上称为周频或低频调仓时，应以实际配置和报告中的 `rebalance_days` 为准；本项目当前代码默认值是20个交易日，不能把“周频”口头描述与20日实现混为一谈。

#### 9.5.2 TopK：集中信号但控制个股风险

TopK 是把预测分数最高的 K 只股票作为目标股票池。选择排名靠前的股票，体现的是相对强弱和有限资金优先配置高置信度信号的思想；不买入全市场，则避免大量低分股票稀释 Alpha。K=50 又提供了基本分散化，降低单只股票偶然事件对组合的影响。

TopK 不是越小越好：K 太小，信号更集中但个股、行业和流动性风险变大；K 太大，风险更分散但模型优势可能被稀释。应在验证集比较不同 K 的扣成本年化超额、Sharpe、最大回撤、换手和容量，不能根据测试期最优值反向选择。

#### 9.5.3 Buffer：解决排名边界抖动

如果严格按照 Top50，每次第50名和第51名只因微小分数变化就互换，会产生大量不必要交易。Buffer=30 表示原持仓只要仍在前80名内，通常可以继续持有；只有明显跌出缓冲区，才优先卖出。这体现了“信号变化必须足够大才值得支付交易成本”的经济思想，相当于给持仓设置了交易惯性。

#### 9.5.4 Dropout/max_drop：限制组合冲击

`max_drop=5` 限制单次主动换出的股票数量。它不是模型预测规则，而是组合执行层的风险控制：即便排名变化很大，也避免一次性卖出过多股票造成成交冲击、现金错配和组合风格突变。代价是部分已经变弱的股票会暂时保留，因此策略可能牺牲一部分理论收益，换取更低换手和更好的可执行性。

#### 9.5.5 行业、市值与交易约束的作用

预测分数在输出端还会进行缩尾、行业和 `log(circ_mv)` 中性化，再排序选股。这样做的目的是减少模型把“行业上涨”或“大市值股票更稳定”误认为个股 Alpha。回测执行时还必须处理停牌、开盘涨跌停、100股整手、资金不足和成交价格无效等情况。先卖后买、次日开盘成交和交易成本模型，则把研究信号转换为更接近实际交易的结果。

#### 9.5.6 为什么采用周频调仓

这里的“周频”不是说每天都根据分数完全换仓，而是大约每周更新一次投资决策；实际代码中的调仓间隔必须以 `rebalance_days` 和回测报告为准，当前默认实现为20个交易日，因此面试时应准确说“低频定期调仓/当前配置为20日调仓”，不要把概念性周频与代码参数混为一谈。

采用周频或低频调仓，核心是认为模型预测的不是当天的确定性收益，而是未来一段时间的相对强弱。每天排名都会受到市场噪声、买卖盘变化和个别极端收益影响，如果每天都交易，就可能把这些短期扰动误认为新的 Alpha。延长持有时间，可以让真正稳定的因子信号有机会兑现，同时减少无效交易。

具体经济逻辑有以下几层：

1. **信号与持有期匹配。** 主标签 `label_ret_5` 描述未来5个交易日收益，周频决策能够让模型预测的短期相对强弱在组合中得到一定兑现，而不是刚预测就频繁换仓。
2. **降低交易成本。** 每次换仓都要支付佣金、卖出费用、买卖价差和潜在冲击成本。调仓越频繁，毛收益越容易被成本侵蚀，尤其是高换手、低流动性股票。
3. **过滤排名噪声。** 第50名与第51名的分数往往非常接近，日频排名变化不一定代表经济信息变化。低频调仓配合 Buffer，可以只在信号变化足够大时交易。
4. **提高策略容量和可执行性。** 低频交易减少同时产生的订单量，有利于使用较大资金规模，也降低开盘成交不充分、涨跌停无法成交和冲击成本带来的偏差。
5. **减少风格追涨杀跌。** 如果模型短期暴露于动量、流动性或市场情绪，日频完全换仓可能放大风格追逐；低频和换出限制能够抑制组合风格突然跳变。
6. **符合研究目标。** 本项目不是分钟级或日内交易系统，因子主要来自日线行情、财务和估值信息。周频或低频调仓与数据更新频率和研究定位更匹配。

周频并不意味着一定优于日频、10日或20日。它是信号反应速度和交易成本之间的折中：日频响应快但噪声和成本高；10日调仓更平滑但可能错过信号衰减前的收益；20日调仓成本更低但对新信息反应慢。正确做法是在验证集固定模型和因子后，比较不同调仓间隔的净年化超额、Sharpe、最大回撤、换手、容量和不同市场状态表现，再冻结参数，不能根据最终测试期最优结果反向选择。

还要区分三个容易混淆的周期：`label_ret_5` 是预测目标期限，模型 Walk-Forward 的20日步长是重新训练和产生预测的频率，组合的 `rebalance_days` 是实际交易调仓频率。三者可以相关，但不是同一个参数，报告中必须分别写清楚。

#### 9.5.7 面试时的完整回答

> 我先用 LightGBM 对股票进行横截面排序，再通过 TopK 选择高分股票。为了避免第K名附近的微小排名变化造成过度交易，我加入 Buffer；为了限制单次交易冲击，又设置 max_drop。组合层还会做行业和市值中性化，并处理停牌、涨跌停、整手交易、次日开盘成交和交易成本。最终不是只看模型 RankIC，而是看这些分数能否转化为扣除10bps成本后的年化超额、Sharpe、最大回撤、换手和容量。这个设计体现的是：保留有效信号，同时控制交易成本、风格暴露和组合风险。

#### 9.5.8 当前不足与后续优化

当前主要不足是成本模型仍较简化，缺少成交量参与率、分股票冲击成本、实时滑点和更精细的印花税拆分；`T+1` 买入当日不可卖出的批次约束也没有完整维护；波动率和回撤降仓只在预定调仓日成交，突发风险下可能反应偏慢。

后续可在验证集完成以下优化：

- 对 TopK、Buffer、max_drop 和调仓周期做组合参数网格，寻找扣成本有效前沿；
- 按股票流动性和成交量估计冲击成本，做乐观、中性、悲观压力测试；
- 引入动态调仓阈值，让分数变化超过经济意义阈值时才交易；
- 增加行业、规模、风格和个股权重的显式约束，并做容量分析；
- 补充真实 T+1、最小成交量、停牌复牌和涨跌停无法成交规则；
- 用滚动验证评估周频、10日和20日调仓，但最终主模型仍以 `label_ret_5` 评分口径为准；
- 将组合参数、成本假设、成交规则和结果哈希写入 manifest，保证每次比较可复现。

因此，周频调仓不是简单的“每周买前K名”，而是一个将预测排名、交易成本、风险暴露、成交可行性和持仓稳定性统一起来的组合决策层。

---

## 10. `main.py` 如何使用，以及它没有做什么

### 10.1 正确命令形式

当前入口是位置参数，不是旧版的 `--stage`：

```powershell
python main.py list
python main.py build_factors
python main.py train_lgb --label label_ret_10 --quick
python main.py all
```

推荐使用项目兼容环境：

```powershell
C:/Users/haoran/miniconda3/envs/rqalpha/python.exe main.py list
```

### 10.2 当前注册阶段

```text
fetch_daily              fetch_daily_basic
fetch_financial          build_financial_events
merge_data               fetch_stock_basic
clean_data               build_factors
build_alpha158           evaluate_factors
remove_redundant         factor_quantile
preprocess_factors       neutralize
train_lgb                optuna_search
lgb_baseline             portfolio_backtest
train_ridge              train_mlp
compare_models
model_comparison_all
tune_portfolio
```

其中 `model_comparison_all` 是串行训练 Ridge、MLP 并与已有 LightGBM 预测做验证集比较的便捷入口；它不是 `all` 默认主链的一部分。

### 10.3 `all` 的真实顺序

```text
fetch_daily -> fetch_financial -> merge_data -> fetch_stock_basic
-> clean_data -> fetch_daily_basic -> build_factors -> build_alpha158
-> preprocess_factors -> neutralize -> factor_quantile
-> optuna_search -> train_lgb -> lgb_baseline
```

必须知道的边界：

- 不执行 `evaluate_factors` 和 `remove_redundant`；
- 不训练 Ridge 和 MLP；
- 不执行 `compare_models` 和统一 `portfolio_backtest`；
- `neutralize` 的产物不会自动替换模型输入；
- 首次空目录运行时，`build_factors` 早于 `build_alpha158`，会漏合并 Alpha158；
- 某阶段失败后会继续尝试后续阶段，最终统一返回失败，不是遇错立即停止。

所以 `all` 是维护中的默认主链，不是从零生成最终研究结论的完整按钮。

### 10.4 更稳妥的全量研究顺序

```powershell
# 数据
python main.py fetch_daily
python main.py fetch_financial
python main.py merge_data
python main.py fetch_stock_basic
python main.py clean_data
python main.py fetch_daily_basic

# 因子：必须先生成 Alpha158，再合并总因子表
python main.py build_alpha158
python main.py build_factors
python main.py evaluate_factors
python main.py remove_redundant
python main.py factor_quantile
python main.py preprocess_factors

# 模型：验证集研究
python main.py optuna_search --label label_ret_20
python main.py train_lgb --label label_ret_20 --eval-segments valid
python main.py train_ridge --label label_ret_20 --eval-segments valid
python main.py train_mlp --label label_ret_20 --eval-segments valid
python main.py compare_models --label label_ret_20 --segment valid

# 组合：先验证集
python main.py portfolio_backtest --label label_ret_20 --model auto --segment valid
```

只有因子、标签、模型和组合参数全部冻结后，才应使用各模块要求的 `--confirm-final-test` 执行最终测试。

---

## 11. 当前本地状态与最终结果快照

| 文件 | 当前规模 | 作用 |
|---|---:|---|
| `basic_cleaned_with_extra_by_date.parquet` | 10,427,948 × 130 | 带估值、行业和交易状态的底表 |
| `alpha158.parquet` | 10,876,393 × 160 | 158 个 Qlib 因子 |
| `factors.parquet` | 10,427,948 × 275 | 270 因子 + 3 标签 + 主键 |
| `features.parquet` | 10,427,948 × 278 | 正式模型特征和元数据 |
| `labels.parquet` | 10,427,948 × 5 | 三个未来收益标签 |
| `daily_rankic.parquet` | 1,781 × 271 | 日频因子 RankIC 序列 |

Alpha158 行数多于主因子表，是因为主因子合并以基础底表为左表，Alpha158 独有行会被丢弃。这是左连接语义，不一定是行数错误。

当前冗余报告显示 270 个候选中保留 58 个，其中 25 个进一步通过当前五分组训练规则并写入最终建模清单。分层报告里的高年化多空收益是单因子研究结果，不等同于最终可交易组合收益。

三模型比较、统一组合验证、首次最终测试和一次实现错误纠错复算均已完成。当前应引用 `execution_rule_version=open_limit_v2` 的结果，而不是旧成交规则结果。

| 区段 | 年化收益 | 基准年化 | 年化超额 | 波动 | Sharpe | IR | 最大回撤 | 日均换手 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 验证集（修正后） | 2.27% | -1.48% | 3.75% | 16.39% | 0.219 | 0.209 | -21.34% | 0.98% |
| 最终测试纠错复算 | 21.70% | 12.11% | 9.59% | 14.15% | 1.461 | 0.500 | -13.73% | 1.01% |

测试区间实际可用预测截至 2026-08-03，共383个交易日；测试年化明显高于验证集，必须解释为市场阶段依赖，不能外推为长期稳定收益。测试是带20日 purge 的滚动再训练样本外模拟，不是2025年初冻结单一模型并持有到底。

---

## 12. 重要限制、风险与已知问题

### 12.1 样本池口径并非完全统一

原始行情可导出沪深北，但 `merge_data` 当前只合沪深，模型配置也排除 `BJ`。报告若称“全 A 股”，应注明实际下游样本口径。

### 12.2 财务覆盖

财务字段缺失不会填零，这是保守处理，但会造成部分质量和成长因子缺失。应使用 `analysis/check_fin_coverage.py` 审计年份、股票和字段覆盖。

### 12.3 行业历史口径

当前行业字段未必是完整的历史 Point-in-Time 行业变更序列。股票历史行业迁移可能带来轻微时点偏差，中性化结果对此应保持敏感。

### 12.4 标签与可实现收益不同

标签按未来收盘价构造，而组合在次日开盘成交并受停牌、涨跌停影响。训练样本过滤和组合交易约束只能降低偏差，不能让二者完全等价。

### 12.5 多重检验

270 个因子、三个标签、三个模型和多轮 Optuna 会显著提高偶然有效概率。应重视跨时期稳定性、换手成本、市场状态分层和复杂模型相对 Ridge 的真实增益。

### 12.6 回测简化

- 未建模成交量参与率、冲击成本和滑点；
- 停牌估值使用最近收盘价；
- 费用没有逐项拆分；
- 行业上限按股票数量近似，而非成交后实际市值；
- 没有完整实盘 T+1 持仓批次账本；
- 没有公司行动和真实撮合队列模型。

### 12.7 配置漂移

日期、窗口、模型参数和成本最终由代码决定。正式实验应保存配置哈希、Git 提交、输入校验信息和运行时间，不能只依赖本文。

---

## 13. 验证与故障排查

### 13.1 轻量检查

```powershell
python main.py list
python -m unittest discover -s tests -p "test_*.py"
python main.py train_lgb --quick --eval-segments valid
```

现有测试覆盖标签周期、purge 边界、标签不进入特征、输出中性化、行业持股上限、次日开盘成交和成本，以及 Ridge/MLP 基础行为。它们不替代全量数据质量检查。

### 13.2 常见问题

**因子只有 112 个左右**：检查 `alpha158.parquet`，然后重新运行 `build_factors`。

**模型没有使用 58 个因子**：模型优先读取 `passed_factor_cols.json`，检查其中 `factors` 字段和训练日志，不要把 58 个冗余筛选结果与最终分组通过数混为一谈。

**`--model auto` 报错**：先训练同标签模型并运行 `compare_models` 生成 `selected_model.json`。缺失模型会被比较脚本跳过。

**预处理内存高**：正式脚本已按 row group 流式处理。`--max-rg` 可冒烟，但产物不完整。

**财务因子异常好**：先审计 `ann_date`、公告日缺失回退和修订记录。

**测试集被锁**：查看 `reports/portfolio/final_test_audit.json`。不要为了继续调参而删除锁；若发现数据事故，应记录原因并废弃整套测试结论。

---

## 14. 建议阅读顺序

1. `config/settings.py`：日期、路径和数据边界。
2. `main.py`：入口和默认链条局限。
3. `scripts/merge_data.py`、`clean_data.py`：PIT 和股票状态。
4. `scripts/build_factors.py`、`factors/registry.py`：因子构建。
5. `preprocessing/preprocess_factors.py`：正式模型输入。
6. `models/lightgbm/data.py`、`models/rolling.py`：purge 与滚动窗口。
7. 三个模型的 `train.py`：训练差异。
8. `backtest/alpha_signal.py`、`engine.py`：信号到交易。
9. `tests/`：关键设计意图。
10. `knowledge/`：经济含义、组合方法和风险。

---

## 15. 总结

项目已经具备数据、因子、滚动建模和可交易组合回测的主体骨架，并在公告日对齐、标签隔离、输出中性化和最终测试锁方面做了明确设计。当前最值得优先处理的是修正默认编排顺序、补齐三模型统一验证与组合报告、审计行业历史口径和财务覆盖，再用冻结配置完成一次真正独立的最终测试。

阅读任何指标时都应追问四件事：数据在当时是否可知、预测是否样本外、选择是否只看验证集、收益是否考虑可交易性和成本。四个问题都清楚，结果才有研究意义。

---

## 16. 面试场景下应如何介绍这个项目

这一节不是另一份简历，而是把前面的工程细节转化为量化研究面试官关心的研究逻辑。面试时不要从目录结构开始背，也不要一上来罗列 270 个因子。更好的顺序是：研究问题、数据与偏差控制、信号构造、验证方法、组合落地、结果与反思。

### 16.1 30 秒版本

> 我做了一个 A 股日频多因子机器学习选股系统。项目把 Qlib 行情和 Tushare 财务数据按公告日做 Point-in-Time 对齐，构造了 112 个自研因子和 158 个 Alpha158 因子，并做横截面去极值、行业市值中性化和标准化。模型采用带 purge gap 的滚动训练，比较 Ridge、LightGBM 和 MLP，验证集负责选因子和模型，测试集设置一次性审计锁。最后把预测分数再次中性化，通过 TopK、缓冲区和涨跌停约束，在下一交易日开盘执行组合回测。

这个版本要让面试官立刻捕捉到五个关键词：A 股横截面、PIT、因子、滚动样本外、可交易组合。

### 16.2 90 秒版本

> 这个项目研究的是 A 股日频横截面选股，而不是指数择时。样本从 2016 年开始，训练、验证和最终测试严格分开。行情来自 Qlib，财务和估值来自 Tushare。财务数据最容易出现未来函数，所以我没有按报告期直接合并，而是把财报整理成以公告日生效的事件表，再对每日行情做 backward as-of join。
>
> 特征一共 270 个，包括 112 个自研的反转、动量、波动、流动性、技术、价值、质量和成长因子，以及 Qlib Alpha158。每天对因子做 1%/99% 缩尾，回归行业和对数市值取残差，再做横截面标准化。因子先看日频 RankIC 和 ICIR，再基于 IC 序列相关性去冗余，最后做五分组单调性和换手检验。
>
> 模型不是随机切分，而是 750 日训练、60 日局部验证、20 日预测的 Purged Walk-Forward，并按照标签周期留出 5、10 或 20 日隔离带。我用 Ridge 判断线性基线，用 LightGBM 建模非线性，也保留 MLP 对照。组合层对模型分数再次做行业和市值中性化，用 50 股等权、20 日调仓、TopK buffer 和最大换出限制降低换手，信号在下一交易日开盘成交，并处理停牌和涨跌停。
>
> 这个项目最重要的不是某一个回测数字，而是建立了一套能防数据泄漏、能比较模型增益、能从预测落到可交易组合的研究流程。目前验证调参中 20 日标签的最好 RankIC 约为 0.121，但我会明确说明它是验证目标值，不是最终测试结论。

### 16.3 3 分钟展开版本的结构

如果面试官说“详细讲讲”，建议按下面六段展开，每段控制在 20–35 秒：

1. **问题定义**：每天在可交易股票横截面上预测未来收益排序，而不是精确预测绝对收益。
2. **数据治理**：行情、财务、估值、股票名称和上市日如何合并，重点讲公告日 PIT。
3. **因子研究**：因子类别、经济直觉、预处理以及 IC—冗余—分层漏斗。
4. **模型验证**：为什么不能随机切分，如何构造 purge 的滚动样本外预测。
5. **组合落地**：为什么模型分数还要再中性化，如何控制换手和不可交易状态。
6. **结论与不足**：验证结果、目前尚未完成的最终统一测试，以及下一步怎样提高可信度。

---

## 17. 研究问题的数学定义

### 17.1 横截面预测目标

对股票 \(i\) 在交易日 \(t\)，项目定义未来 \(h\) 日收益标签：

\[
y_{i,t}^{(h)} = \frac{P_{i,t+h}}{P_{i,t}} - 1, \quad h \in \{5,10,20\}
\]

模型学习映射：

\[
\hat y_{i,t}^{(h)} = f_\theta(X_{i,t})
\]

其中 \(X_{i,t}\) 只能由 \(t\) 日及以前可见的信息构成。尽管训练目标是收益回归，实际评价更关心同一天股票之间的排序，因此主要使用 RankIC：

\[
\mathrm{RankIC}_t = \mathrm{corr}_{\mathrm{Spearman}}
\left(\hat y_{i,t}, y_{i,t}\right)
\]

这解释了为什么项目使用回归模型，却主要按 RankIC 选模型：组合只需要可靠地区分相对强弱，不要求预测值在绝对数值上完全校准。

### 17.2 IC、ICIR 和显著性

时间区间内平均 RankIC 为：

\[
\overline{IC} = \frac{1}{T}\sum_{t=1}^{T} IC_t
\]

常见的年化 ICIR 可写为：

\[
ICIR_{ann} = \frac{\overline{IC}}{\sigma(IC_t)}\sqrt{252}
\]

但不同代码库可能使用未年化 ICIR，因此面试中引用数字前必须说明项目函数的具体口径。IC 高表示平均预测方向强，ICIR 高表示跨日更稳定；两者不能互相替代。

IC 的 t 统计量近似为：

\[
t = \frac{\overline{IC}}{s(IC)/\sqrt{T}}
\]

不过日频 IC 往往存在自相关和异方差，朴素 t 值可能偏乐观。更严谨的扩展应采用 Newey–West 标准误或 block bootstrap。

### 17.3 为什么标签周期和调仓周期要匹配

默认 LightGBM 标签为未来 20 日收益，滚动预测和组合调仓也默认每 20 个交易日一次。这样模型目标与持有期大体一致，降低“训练预测一个月收益、组合却每天翻仓”的目标错位。

但二者并非必须严格相等。如果 5 日标签的 RankIC 更强，也可研究 5 日信号配合较长持有期的信号重叠或集成；这需要重新定义预测重叠、换手和成本，而不能只改一个命令参数。

### 17.4 排序预测与收益预测的区别

项目当前 LightGBM 目标函数是 MSE 回归，而最终选择指标是 RankIC。这存在目标函数和评价函数不完全一致的问题：MSE 会受极端收益影响，RankIC 只关心顺序。

面试中可以将其表述为一个明确的改进方向：

- 对标签做横截面 rank 或标准化后再回归；
- 尝试 Huber、Fair 等稳健损失；
- 使用 pairwise/listwise 排序目标；
- 比较原始收益、去行业收益、市场中性收益作为标签；
- 在验证集同时观察 RankIC、分组收益和换手，避免只优化一个统计量。

---

## 18. 数据工程部分的面试深挖

### 18.1 为什么财务数据必须按公告日

例如某公司 2023 年年报的报告期是 2023-12-31，但可能到 2024-03-25 才公告。若把这份数据从 2023-12-31 开始赋给每日样本，模型在 2024 年一季度提前知道了尚未披露的信息，形成严重未来函数。

项目把财务记录转为以 `ann_date` 为生效时间的事件，再为交易日 \(t\) 查找最近的：

\[
\max\{a: a \le t\}
\]

这是 `merge_asof(..., direction="backward")` 的实际含义。

面试官可能继续追问修订公告。当前同一 `(symbol, ann_date)` 保留最后记录，能处理同日重复，但更严格的版本需要同时保留数据版本时间、更新时间和报告类型，确保修订数据只从修订披露日起生效，而不是覆盖修订前历史。

### 18.2 为什么不能把财务缺失填零

零有明确经济含义。例如 ROE 为 0 表示企业有权益但没有产生净利润；缺失表示没有可用披露或字段不适用。把缺失填零会把“未知”伪装成“经营表现为零”，引入系统性偏差。

当前策略是保留原始缺失，并在公告后对财务状态向前延续。更进一步应设置财务数据最大有效期，避免多年未更新的旧报表无限沿用，并增加“距上次披露天数”作为信息陈旧度特征。

### 18.3 为什么历史 ST 状态不能用当前名称判断

一家公司今天为 ST，不代表五年前也是 ST；反之亦然。用当前名称过滤整个历史会同时造成未来信息泄漏和样本选择偏差。项目用 `namechange` 的生效区间判断每个交易日的名称状态，是 Point-in-Time 股票池构造的一部分。

### 18.4 停牌数据为什么不直接前向填价格

若停牌期间把价格向前填充，会产生一串人为的 0 收益和 0 波动，导致低波、反转、流动性因子受到污染。复牌跳空也可能被错误摊平。项目保留停牌价格缺失，在回测时把停牌持仓按最近有效价格估值，但不允许成交。

### 18.5 复权口径需要怎样回答

因子研究通常使用复权价格以消除拆分、分红和配股造成的机械跳跃；交易限制判断则更适合官方未复权涨跌幅。项目因此在两个场景使用不同口径：因子来自 Qlib 复权行情，普通涨跌停在预处理时用 Tushare 官方 `pct_chg` 校正。

需要注意，回测成交价若使用复权价格，现金和股数的绝对经济含义会受到复权体系影响。对收益曲线通常仍可工作，但更严格的撮合系统应使用未复权真实成交价，并单独处理公司行动和持仓股数调整。

### 18.6 幸存者偏差如何处理、还剩什么风险

项目使用 Qlib `all` instruments 和历史名称信息，目标是包含历史股票而非只使用当前上市公司，从而降低幸存者偏差。但是否完全消除还取决于本地 Qlib 数据是否包含退市证券的完整历史、Tushare 财务抓取是否覆盖已退市代码，以及数据清洗是否误删退市前样本。

面试中不要简单说“已经消除幸存者偏差”，更准确的说法是“在股票池设计上显式降低了幸存者偏差，但仍需对退市股覆盖做专项审计”。

---

## 19. 因子研究部分的面试深挖

### 19.1 因子类别背后的经济直觉

**反转与动量**：短期价格可能因散户交易、流动性冲击或过度反应出现反转；中期动量可能反映信息扩散。A 股中短期反转和交易约束尤其重要。

**低波动**：低风险股票可能获得经风险调整后更高收益，也可能只是行业、市值和质量暴露，因此必须中性化后再看有效性。

**流动性**：低换手、Amihud 非流动性和量价关系可能捕捉流动性溢价、拥挤交易和资金行为，但高收益往往伴随更差可交易性。

**价值**：EP、BP、SP 把利润、净资产和收入相对于市场价格归一化。价值因子容易受到负利润、会计口径和行业结构影响。

**质量与成长**：ROE、ROIC、利润率和同比成长反映基本面，但数据频率低、披露滞后，且更容易暴露行业和大盘风格。

**技术指标**：多个技术指标常是同一价格路径的不同变换，因此数量多不代表独立信息多，这正是冗余剔除的重要原因。

### 19.2 为什么先缩尾、再中性化、最后标准化

顺序不是任意的：

1. 先缩尾，避免极端异常值主导 OLS 中性化系数。
2. 再中性化，移除行业和市值的线性暴露。
3. 最后标准化，让不同因子的横截面尺度可比，便于正则模型和神经网络训练。

如果先标准化再缩尾，最终方差不再严格统一；如果先中性化但极值很大，回归可能被少数样本拉偏。

### 19.3 中性化回归的含义

对某日某因子 \(x_i\)，项目近似拟合：

\[
x_i = \sum_k \beta_k I_{i,k} + \gamma\log(MV_i) + \epsilon_i
\]

使用残差 \(\epsilon_i\) 作为中性化因子。它表示在同一行业和相近市值条件下，股票相对横截面的异常因子值。

这里的中性化只是去除线性暴露。行业分类误差、行业内市值非线性和风格因子交互仍可能残留，因此组合输出端还会再次控制暴露。

### 19.4 为什么基于 IC 序列相关性去冗余

两个原始因子值可能相关，但预测效果出现的时间不同；也可能原始值不太相关，却在相同市场环境中同时有效。项目比较每日 IC 序列，意图保留“效果来源和时序表现”更不同的因子。

该方法也有局限：只基于单因子 IC，无法判断两个因子联合进入非线性模型后是否互补。可扩展为：

- 原始因子值横截面相关；
- IC 序列相关；
- 条件 IC 或增量 IC；
- 特征聚类后每簇选代表；
- 模型消融和 permutation importance；
- 稳定性选择或带组结构的正则化。

### 19.5 为什么还要做分组回测

IC 是相关系数，无法直接告诉研究者收益是否集中在极端组、是否单调、是否伴随高换手。例如一个因子可能总体 IC 不错，但只有最差组异常，或者中间组完全无序。五分组能检查：

- Q1 到 Q5 是否大致单调；
- 多空收益来自哪一端；
- 收益是否被少数日期主导；
- 组合换手是否过高；
- 训练期方向到验证期是否保持。

### 19.6 如何看待当前“270 保留 58”

这不应被包装成“成功找到了 58 个 alpha”。更准确的是：按当前训练期 IC 阈值和 0.70 的 IC 序列相关阈值，270 个候选中有 58 个被选为较少冗余的代表，后续还需经过分组和模型验证。

筛选阈值本身也是超参数。如果根据验证集反复调整 0.70、IC 门槛和单调性门槛，同样会产生验证集过拟合。

---

## 20. 机器学习部分的面试深挖

### 20.1 为什么时间序列不能随机 K 折

随机切分会把未来样本放入训练集，并让相邻日期的高度相似横截面同时出现在训练和验证中。更隐蔽的问题是标签重叠：若 4 月 1 日样本的标签使用未来 20 日收益，而验证从 4 月 10 日开始，训练标签已经包含验证期价格。

项目通过按日期切分和标签周期 purge 同时解决特征时间顺序与标签区间重叠问题。

### 20.2 为什么使用滚动窗口而不是不断扩张窗口

750 日固定窗口强调较新的市场结构，避免过旧数据永久占据训练权重，适合风格和微观结构变化较快的市场。扩张窗口样本更多、估计更稳定，但可能稀释近期制度和交易行为。

两种方案没有绝对优劣，应在验证集比较：

- 固定滚动窗口；
- expanding window；
- 时间衰减样本权重；
- 不同市场状态下的条件训练。

### 20.3 三个模型各自承担什么角色

| 模型 | 项目角色 | 优势 | 主要风险 |
|---|---|---|---|
| Ridge | 线性基准 | 稳定、快、可解释、抗共线性 | 无法捕捉复杂交互 |
| LightGBM | 主模型 | 擅长非线性、缺失和特征交互 | 易过拟合、参数多、重要性可能误导 |
| MLP | 非线性对照 | 可学习平滑复杂函数、便于扩展表示学习 | 数据与调参成本高、稳定性和解释性较弱 |

面试中的关键判断不是“LightGBM 一定最好”，而是复杂模型必须在同一滚动验证框架中稳定超过 Ridge，并且增益能转化为扣成本后的组合表现。

### 20.4 LightGBM 参数如何解释

- `learning_rate` 与树数共同控制拟合速度和复杂度。
- `num_leaves`、`max_depth` 控制单棵树表达能力。
- `min_data_in_leaf` 防止叶节点只拟合少量股票。
- `colsample_bytree` 和 `subsample` 注入随机性，降低过拟合。
- `lambda_l1/lambda_l2` 正则化叶权重。
- 早停使用每个滚动步中预测段之前的局部验证窗口。

当前 Optuna 得出的最优参数按标签分别存储。面试中应强调：超参数是用全局验证框架选择的，最终测试不参与搜索。

### 20.5 为什么不能只看模型的 MSE

横截面收益噪声极大，绝对收益预测误差可能很高，但排序仍有投资价值。MSE 还会被极端收益主导。模型应至少同时看：

- RankIC 均值和 ICIR；
- 日度 IC 分布及不同年份稳定性；
- Top-Bottom 分组收益；
- 覆盖率和缺失样本偏差；
- 输出换手与成本；
- 行业、市值和风格暴露；
- 组合回撤和超额稳定性。

### 20.6 特征重要性应该怎样回答

LightGBM split/gain importance 只能说明模型如何使用特征，不等同于经济意义或因果贡献。高度相关因子会互相分摊重要性，某个特征也可能因为缺失模式而获得重要性。

更可靠的解释框架包括：

- 滚动窗口内重要性的时间稳定性；
- permutation importance；
- SHAP 的横截面和时间聚合；
- 按因子类别做消融；
- 加入因子后的验证集增量 RankIC；
- 对重要因子重新做单因子分组和经济逻辑核查。

---

## 21. 组合与回测部分的面试深挖

### 21.1 为什么高 IC 不一定有高组合收益

从预测到收益还隔着多个环节：

- IC 可能来自大量中间排名，而 Top50 区域区分度不强；
- 高 IC 因子可能换手很高；
- 信号最强股票可能频繁涨停买不进；
- 行业集中会造成风险暴露而非纯 alpha；
- 预测周期和实际持有周期不匹配；
- 交易成本、冲击成本和容量会侵蚀收益。

因此项目把模型评价和组合评价分开，不用单一 IC 代替投资结果。

### 21.2 TopK、Buffer 和 Dropout 的关系

TopK 规定目标持股数。Buffer 允许原持仓排名略微跌出 TopK 后继续持有，例如 Top50 加 30 名缓冲意味着原持仓在前 80 名内通常不因边界波动卖出。`max_drop=5` 又限制一次主动淘汰数量。

这是一种有滞后的换仓规则：牺牲少量信号及时性，换取更低换手和更稳定的持仓。最优 buffer 不能只按毛收益选，应按扣成本后的验证表现和容量共同决定。

### 21.3 为什么 T 日信号在 T+1 开盘成交

如果因子包含 T 日收盘价、成交量或当日财务状态，最早只能在收盘后完整计算。用 T 日收盘价成交会默认自己能在看到收盘数据后仍以收盘价交易，是经典的执行未来函数。项目将交易推迟到下一交易日开盘，保证信号和成交的时间顺序。

更保守的版本还应加入开盘滑点、涨跌停排队、成交量参与率和信号计算延迟。

### 21.4 行业约束当前如何实现

选股函数把行业上限转换为股票数量上限：`floor(topk * max_industry_weight)`。Top50、20% 上限对应单行业最多约 10 只。

这不是严格的行业市值权重约束，因为股票虽目标等权，实际成交后会受到整手、资金不足和不可交易持仓影响。更严格的做法是使用优化器直接约束成交后权重：

\[
|B w - b^{benchmark}| \le c
\]

其中 \(B\) 是行业暴露矩阵，\(w\) 是组合权重，\(b^{benchmark}\) 是基准行业权重。

### 21.5 应该补充哪些风险指标

当前引擎已有年化收益、波动、Sharpe、信息比率、最大回撤和换手。面试中可提出下一步补充：

- Beta、Alpha 和跟踪误差；
- 行业、风格和市值暴露；
- 下行波动、Sortino、Calmar；
- 月度胜率、超额回撤持续时间；
- VaR/CVaR；
- 单票、行业和因子风险贡献；
- 容量、成交额参与率和分层冲击成本；
- 不同市场状态下的绩效归因。

### 21.6 如何做容量分析

当前一亿元初始资金只是回测参数，不代表策略真实容量。容量分析至少需要：

1. 用未复权真实成交额计算订单占当日成交额比例；
2. 设置单票最大参与率，例如不超过日成交额的 5%；
3. 使用随参与率和波动率上升的冲击成本模型；
4. 在不同资金规模下重新回放订单；
5. 观察收益、换手、无法成交比例和行业偏离如何变化。

---

## 22. 项目结果应怎样诚实表达

### 22.1 可以明确说的结果

- 当前完整因子表包含 270 个因子和三个标签，共约 1043 万个股票—交易日样本。
- 自研因子注册表实测为 112 个，Alpha158 为 158 个。
- 当前 IC 与冗余筛选快照从 270 个候选中保留 58 个代表因子，其中 25 个通过五分组训练规则并进入 `passed_factor_cols.json`。
- 三个标签均完成过 30 次 Optuna 搜索；当前记录的验证目标 RankIC 分别约为 0.109、0.118 和 0.121。
- 已实现严格滚动窗口、purge gap、验证/测试分段和统一组合测试锁。
- 已有关键时间隔离、信号和回测逻辑的自动化测试。
- 三模型统一验证比较已经完成，LightGBM因最高IC、最高多空收益和更好的组合转化被选为主模型。
- `open_limit_v2` 修正后验证集年化2.27%、年化超额3.75%、Sharpe 0.219、最大回撤-21.34%、日均换手0.98%。
- 最终测试纠错复算年化21.70%、年化超额9.59%、Sharpe 1.461、最大回撤-13.73%；这是383个交易日的滚动再训练样本外结果，不能视为长期收益承诺。

### 22.2 不应直接说的结果

- 不应说“策略 RankIC 是 0.121”，而应说“20 日标签 Optuna 搜索中的最好验证目标 RankIC 是 0.120692”。
- 不应把单因子五分组年化收益当作最终策略收益。
- 不应说“稳定年化21.7%”或“已经达到实盘水平”；测试窗口只有约1.5年，且尚未建模冲击成本和容量。
- 不应说“完全消除了未来函数、幸存者偏差和交易偏差”；更合理的是说明采取了哪些控制，仍有哪些残余风险。
- 不应以已有模型文件数量代表模型稳定性。

### 22.3 面试官问“最终赚了多少”时

可以这样回答：

> 修正成交时序错误后，验证集年化2.27%、超额3.75%、Sharpe 0.219；约1.5年的滚动样本外测试年化21.70%、超额9.59%、Sharpe 1.461。测试期明显好于验证期，因此我不会把21.7%当成长期预期，而会同时展示两个区段，并说明它是每20日滚动再训练、包含基础交易成本但尚未包含冲击成本的研究回测。

这个回答不会削弱项目，反而体现对研究有效性和回测过拟合的理解。

---

## 23. 高频面试问题与回答要点

### 23.1 “你为什么选 A 股日频多因子？”

回答要点：A 股股票数量多，适合横截面学习；日频数据比高频更易获得完整历史，交易成本和制度约束仍足够有研究价值；多因子能结合价格、成交和基本面信息。代价是信号噪声高、市场风格变化快、涨跌停和停牌影响明显。

### 23.2 “你在项目中最重要的工作是什么？”

不要回答“写了很多因子”。更好的回答是：把 PIT 数据治理、滚动无泄漏验证和可交易组合连接成完整闭环；其中最需要谨慎的是财务公告日、标签重叠 purge 和信号到次日成交的时间顺序。

### 23.3 “最难的技术问题是什么？”

可以从两类问题回答：

- 研究正确性：财务 PIT、历史 ST、标签重叠和测试集隔离。
- 工程规模：1043 万行乘 270 特征造成 10 GB 级 Parquet，必须列投影、float32、按 row group 流式处理和分批合并 Alpha158。

### 23.4 “为什么不直接用深度学习？”

表格型因子数据中，树模型通常是强基线；样本行数虽大，但有效独立时间样本远少于行数，因为同日股票和相邻日期高度相关。先用 Ridge 和 LightGBM 建立可信基线，再判断 MLP 是否提供稳定增益，比直接堆复杂网络更合理。

### 23.5 “你如何防止过拟合？”

回答应覆盖多层：

- 时间切分而非随机切分；
- purge 标签重叠；
- 验证集选方案、测试集一次性评估；
- 因子弱信号与冗余筛选；
- Ridge 作为低复杂度基准；
- LightGBM 正则、采样和早停；
- 组合层检查换手、成本和暴露；
- 承认多重检验仍需更严格统计校正。

### 23.6 “为什么做两次行业市值中性化？”

输入端中性化防止单个因子的原始行业和规模暴露进入模型；输出端中性化防止非线性模型通过特征组合重新生成这些暴露。前者控制特征，后者控制最终投资信号。

### 23.7 “负 IC 因子为什么保留？”

只要方向稳定，负 IC 可以乘以 -1 后成为正向信号。筛选时更关注绝对强度、稳定性和经济合理性。分组回测用训练期 IC 符号对齐，避免偷看验证期方向。

### 23.8 “如果市场风格切换怎么办？”

固定 750 日滚动窗口会逐步遗忘旧数据，但响应仍有限。可进一步做滚动 IC 动态权重、市场状态分类、时间衰减、在线更新和分状态模型。不过 regime 定义本身也必须只用当时信息，并防止状态划分过拟合。

### 23.9 “为什么选 50 只股票？”

50 只是当前冻结默认值，在分散化、信号集中度、交易容量和成本之间取折中。更少持仓可能提高 alpha 浓度但增加个股风险，更大持仓降低风险但稀释信号。应在验证集上画不同 TopK 的扣成本有效前沿，而不是声称 50 是理论最优。

### 23.10 “项目有什么 bug 或设计问题？”

应主动说出已识别和已修复问题：最初开盘成交错误地使用了当天完整 `pct_chg` 生成的涨跌停状态，修正为真实昨收和开盘价后，验证年化从旧口径5.78%下降到最终可复现的2.27%；同时发现 `set` 遍历顺序使持仓保留次序不确定，修正为按模型排名和代码排序后连续两次结果完全一致。仍未解决的主要问题包括复权价格成交账本、冲击成本、历史行业PIT、容量和严格T+1批次。

### 23.11 “如果给你更多时间，你先做什么？”

优先级建议：

1. 修正流水线依赖图并让每个产物带 manifest、配置哈希和数据校验。
2. 建立未复权真实价格、分红送转和持仓股数账本，彻底统一收益与成交口径。
3. 加入成交额参与率、冲击成本和严格行业/风格风险约束。
4. 做跨年份、牛熊、大小盘和流动性状态稳定性分析。
5. 采用合成扰动、成本压力测试和纸面仿真继续检验，但不再用已查看测试集调参。
6. 最后才考虑新增更多因子、LSTM或更复杂模型。

---

## 24. 面试官可能要求现场推导的内容

### 24.1 从 IC 到预期主动收益

经典基本法则常写为：

\[
IR \approx IC \times \sqrt{Breadth} \times TC
\]

其中 Breadth 是有效独立决策次数，TC 是 transfer coefficient，反映交易约束后信号能转化为持仓的程度。

在本项目中，不能把“每日股票数 × 交易日数”直接当 Breadth，因为股票收益相关、相邻标签重叠、20 日调仓也降低独立决策数。涨跌停、行业上限、buffer 和成本都会降低 TC。因此验证 RankIC 不能机械换算成很高 IR。

### 24.2 换手与成本

若单次双边换手为 \(TO_t\)，线性费率为 \(c\)，粗略成本为：

\[
Cost_t \approx c \cdot TO_t
\]

真实冲击通常是非线性的，可近似依赖订单占成交量比例和波动：

\[
Impact \propto \sigma \sqrt{\frac{Q}{ADV}}
\]

其中 \(Q\) 是订单规模，\(ADV\) 是平均成交量或成交额。当前项目只有线性费用，尚未实现该冲击模型。

### 24.3 行业市值中性化与组合优化

因子层采用回归残差中性化；组合层当前使用规则选股。更完整的组合优化可写为：

\[
\max_w \quad \alpha^T w - \lambda w^T\Sigma w - \eta\|w-w_{prev}\|_1
\]

约束：

\[
\mathbf{1}^T w = 1, \quad 0 \le w_i \le w_{max}
\]

\[
|B^T w - b_{benchmark}| \le c
\]

其中第一项追求预测收益，第二项惩罚风险，第三项惩罚换手，\(B\) 表示行业和风格暴露。这个框架是当前规则组合向风险模型组合升级的自然方向。

---

## 25. 可展示的工程能力

### 25.1 大规模数据处理

项目处理约 1043 万行、270 个特征。为避免内存峰值，已经使用：

- Parquet 列投影，只读当前环节所需字段；
- 数值列尽量存为 float32；
- Alpha158 按股票和列分批计算、合并；
- 预处理按 Parquet row group 流式读取和写出；
- 主键对齐采用 `(symbol, date)`；
- 中间对象及时释放，避免宽表反复深拷贝。

面试时可强调：量化研究不仅是建模，数据布局和内存策略会直接决定实验能否运行和能否复现。

### 25.2 模块化与可测试性

项目把配置、数据、因子、预处理、训练、评价、信号和回测分开。核心时间窗口、特征泄漏、行业约束和次日成交都有单元测试。不同模型复用滚动工具，减少模型之间验证口径漂移。

### 25.3 实验审计

已有 MLflow 本地记录、标签级最优参数 JSON、模型逐步落盘、模型选择文件、组合配置哈希和最终测试审计锁。尚可增强的部分是统一 run manifest：记录 Git commit、输入文件哈希、Python 依赖、随机种子、配置和所有输出路径。

---

## 26. 一份适合简历的项目表述

以下内容应根据真实求职简历篇幅删减；测试结果必须注明滚动再训练、测试长度和研究回测口径：

> **A 股多因子机器学习选股研究系统**
>
> - 搭建从数据准备、因子构建、模型训练到组合回测的日频横截面研究管线，整合 Qlib 行情与 Tushare 财务/估值数据，以公告日事件表和 backward as-of join 实现财务数据 Point-in-Time 对齐。
> - 构建约270个候选因子，覆盖价值、质量、成长、动量、反转、波动、流动性和规模等经济逻辑，经过 RankIC/ICIR、缺失率、冗余和五分组检验，形成25个人工核心因子。
> - 以人工25因子为去重基准，接入 DeepSeek 生成增量因子假设；通过公式合法性、防泄漏、增量IC、冗余和统一10bps低频组合准入，冻结5个增量因子，组成最终30因子输入。
> - 实现带标签周期 purge/embargo 的 Purged Walk-Forward 滚动训练，对比 Ridge、LightGBM 与 PyTorch MLP；以验证期 RankIC、头尾收益、稳定性和扣成本组合转化综合选择 LightGBM，主标签为未来5日收益。
> - 将预测分数转换为行业/市值约束下的 TopK 组合，通过 Buffer、Dropout/max_drop、低频调仓、次日开盘成交、整手交易、停牌/涨跌停处理和交易成本核算提高可执行性。
> - 建立训练/验证/测试隔离、测试集一次性审计、因子挖掘记忆和实验产物记录机制，并针对数据时点、成交时序和可复现性设计自动化检查。

简历若空间有限，优先保留：PIT数据对齐、270→25→30因子漏斗、DeepSeek增量挖掘、Purged Walk-Forward、LightGBM模型选择、低频组合约束和测试集隔离。除非能同时说明样本区间、成本、基准和测试审计，否则不要只放一个漂亮的收益数字。

---

## 27. 面试前的项目自检清单

### 27.1 必须能脱离文档解释

- 为什么报告期不能作为财务数据生效日。
- 为什么随机 K 折会泄漏。
- purge gap 如何由标签周期决定。
- IC、RankIC、ICIR、分组收益的区别。
- 为什么负 IC 因子可以有价值。
- 为什么输入和输出都要中性化。
- 为什么 T 日信号不能在 T 日收盘成交。
- TopK、buffer、max_drop 如何影响换手。
- LightGBM验证RankIC 0.096664与Optuna单窗口目标0.120692为什么不是同一个口径。
- 为什么测试年化21.70%不能视为长期预期。
- 哪些结论已经完成测试，哪些风险仍未被测试解决。

### 27.2 面试前建议准备的真实产物

- 一张完整数据流图。
- 一张滚动窗口和 purge gap 示意图。
- 因子类别与筛选漏斗表。
- 三模型统一验证集对比表。
- 验证集净值、超额净值和回撤图。
- 行业、市值暴露前后对比。
- 年度或市场状态分解表。
- 换手—成本—收益敏感性分析。
- 一页“已知限制与下一步”，主动控制面试官预期。

### 27.3 面试时的底线

- 不把验证集叫测试集。
- 不把 Optuna trial 最优值叫最终模型表现。
- 不把理论标签收益叫可实现收益。
- 不声称完全消除所有偏差。
- 不用因子数量代替因子质量。
- 不回避已知代码编排问题。
- 对任何结果都能说清数据区间、股票池、调仓频率、成本和基准。

做到这些，这个项目展示的就不只是 Python 和 LightGBM，而是一套接近真实量化研究岗位要求的研究判断力。

---

## 28. 25 个最终因子的可复现定义与真实执行逻辑

本节以当前 Python 实现和本地 Qlib 0.9.7 返回的 Alpha158 表达式为准。它比因子名称或注册注释优先级更高。若未来修改因子代码但没有重新生成 `factors.parquet`、`features.parquet` 和筛选文件，磁盘产物仍代表旧公式，必须全链重算。

### 28.1 统一符号

下表使用以下记号：

- \(C_t, O_t, H_t, L_t\)：当日收盘、开盘、最高、最低价；
- \(V_t, A_t\)：成交量、成交额；
- \(T_t\)：Tushare 换手率 `turnover_rate`；
- \(r_t=C_t/C_{t-1}-1\)：日收益；
- \(\Delta V_t=V_t/V_{t-1}-1\)、\(\Delta A_t=A_t/A_{t-1}-1\)；
- \(\operatorname{Corr}_N\)、\(\operatorname{Mean}_N\)、\(\operatorname{Std}_N\)：截至当日、只使用过去数据的 N 日滚动统计；
- \(\epsilon\)：防除零极小量，代码中通常为 `1e-12`；
- `方向 +/−` 仍指训练期 IC 方向，不表示公式最前面是否已经带负号。

### 28.2 15 个自研入选因子的代码定义

| 因子 | 当前实际公式/实现 | 最短历史 | 原始值越高的代码含义 | 关键注意事项 |
|---|---|---:|---|---|
| `LOW_TURNOVER` | \(-T_t\) | 1日 | 换手越低 | 缺失换手率先填 0，因此缺失样本得到 0，可能高于正常负值并被误认为低换手 |
| `LN_TURNOVER` | \(\log(T_t+10^{-6})\) | 1日 | 换手越高 | 缺失同样先填 0，得到约 -13.82；训练 IC 为负，分组时反向使用 |
| `VOL_CV20` | \(-\operatorname{Std}_{20}(V)/[\operatorname{Mean}_{20}(V)+\epsilon]\) | 早期允许1日 | 成交量相对波动越低 | `min_periods=1`，窗口初期统计与完整20日不可完全比较 |
| `RET_AMT_CORR20` | \(-\operatorname{Corr}_{20}(r,\Delta A)\) | 完整20日 | 收益与成交额变化越背离 | 成交额突变、除权和极端值会影响相关系数 |
| `CONSEC_UP5` | \(-\sum_{j=0}^{4} r_{t-j}\mathbf{1}(r_{t-j}>0)\) | 早期允许1日 | 近5日正收益累计越小 | 名称和注释称“连续上涨”，但代码实际是正收益滚动和，不是连续天数计数；这是命名—实现偏差 |
| `RET_VOL_CORR5` | \(-\operatorname{Corr}_{5}(r,\Delta V)\) | 完整5日 | 短期量价越背离 | 5日相关估计噪声较大，对单日异常成交敏感 |
| `OBV_CHG_20` | 先算 \(OBV_t=\sum_{s\le t}\operatorname{sign}(\Delta C_s)V_s\)，再算 \((OBV_t-OBV_{t-20})/(|OBV_{t-20}|+\epsilon)\) | 20日 | OBV相对过去增强 | OBV 是从股票样本起点累计，分母接近0时比率可能极端，依赖后续缩尾 |
| `AMIHUD_20` | \(\operatorname{Mean}_{20}(|r_t|/(A_t+\epsilon))\) | 早期允许1日 | 价格冲击更高、流动性更差 | 成交额单位只会缩放因子但不改排序；停牌/零成交处理和真实容量高度相关 |
| `VOL_OF_VOL_20` | \(-\operatorname{Std}_{20}[\operatorname{Std}_{20}(r)]\) | 内外层均至少5个值 | 波动率状态更稳定 | 实际有效观察期可接近40日，初期大量缺失 |
| `VOL_PRICE_DIVERGENCE` | 先令 \(s_t=1\) 当 `sign(r)==sign(ΔV)`，否则 \(-1\)；因子为 \(-\operatorname{Mean}_5(s)\) | 早期允许1日 | 量价方向越背离 | 函数注释提到 Spearman，但实际为了性能使用符号一致性，不是滚动秩相关 |
| `BP` | 当 \(PB_t>0\) 时为 \(1/PB_t\)，否则为0 | 1日 | 账面价值相对价格更高 | 负净资产和缺失均变成0，不是 NaN；0 会参与后续横截面中性化与排序 |
| `EP` | 当 \(PE_{TTM,t}>0\) 时为 \(1/PE_{TTM,t}\)，否则为0 | 1日 | 盈利收益率更高 | 亏损、PE为0和缺失都编码为0，混合了不同经济状态 |
| `MAX_DD60` | \([\max_{60}(C)-\min_{60}(C)]/[\max_{60}(C)+\epsilon]\) | 完整60日 | 60日价格振幅越大 | 名称叫最大回撤，但实现没有保持峰谷先后顺序，严格说是窗口高低振幅；训练 IC 为负，实际偏好较小振幅 |
| `Q_SALES_YOY` | 直接读取 PIT 对齐后的 `q_sales_yoy` | 财报可用后 | 单季销售同比更高 | 低频财务值会在公告后延续；验证期衰减明显，且受行业周期影响 |
| `NEG_SKEW_20` | \(-\operatorname{Skew}_{20}(r)\)，至少5个收益 | 约6日后可计算 | 左偏/负偏度越强 | 高值可能是近期尾部下跌后的反转，也可能代表持续风险，需结合交易约束解释 |

这里有三个值得立即审计的缺失值编码：

1. `LOW_TURNOVER` 把缺失换手率填 0 后取负，缺失值会落在高因子区域附近；
2. `LN_TURNOVER` 把缺失编码成极小换手率的对数，可能形成极端低换手信号；
3. `BP/EP` 把无效或缺失估值编码为 0，把“未知”“亏损/负净资产”和“中性估值”混在一起。

虽然后续有缩尾、中性化和标准化，但这些步骤不会恢复丢失的缺失语义。更严谨的方案是保留 NaN，并增加缺失指示变量或在股票池层明确排除不可比较样本。

### 28.3 10 个 Alpha158 入选因子的精确表达式

以下表达式由当前本地 Qlib `Alpha158.get_feature_config()` 实际返回：

| 因子 | Qlib 表达式 | 解释 |
|---|---|---|
| `KLOW` | `(Less($open, $close)-$low)/$open` | `Less` 取开收盘较小值，衡量下影线长度相对开盘价；当前训练 IC 方向为负 |
| `IMAX60` | `IdxMax($high, 60)/60` | 60日最高价在窗口中的相对位置；具体索引方向由 Qlib `IdxMax` 定义 |
| `CORR20` | `Corr($close, Log($volume+1), 20)` | 20日价格水平与对数成交量相关 |
| `CORR60` | `Corr($close, Log($volume+1), 60)` | 60日价格水平与对数成交量相关 |
| `CORD5` | `Corr($close/Ref($close,1), Log($volume/Ref($volume,1)+1), 5)` | 5日价格比率与对数成交量比率相关 |
| `CORD60` | `Corr($close/Ref($close,1), Log($volume/Ref($volume,1)+1), 60)` | 60日价格比率与对数成交量比率相关 |
| `CNTD10` | `Mean($close>Ref($close,1),10)-Mean($close<Ref($close,1),10)` | 10日上涨日比例减下跌日比例 |
| `CNTD60` | `Mean($close>Ref($close,1),60)-Mean($close<Ref($close,1),60)` | 60日上涨日比例减下跌日比例 |
| `VSTD5` | `Std($volume,5)/($volume+1e-12)` | 5日成交量标准差除以当日成交量；不是除以窗口均量 |
| `WVMA30` | `Std(Abs($close/Ref($close,1)-1)*$volume,30)/(Mean(Abs($close/Ref($close,1)-1)*$volume,30)+1e-12)` | 绝对收益乘成交量序列的30日变异系数 |

这些定义揭示出几处仅看名称无法发现的差异：

- `CORR` 使用价格**水平**和对数成交量水平，可能受趋势和非平稳性影响；`CORD` 使用价格/成交量相对变化，更接近量价变化关系。
- `VSTD5` 的分母是当日成交量，不是常见的滚动平均成交量，因此当日缩量时数值会被放大。
- `WVMA30` 名称容易被理解为“加权移动平均”，实际是“绝对收益×成交量”的滚动变异系数。
- `KLOW` 除以开盘价而不是完整 K 线长度，属于下影线的价格尺度归一化。

### 28.4 因子从原始值到模型矩阵的完整变换

对任一入选因子，实际经历以下步骤：

```text
原始行情/估值/PIT财务字段
  -> 按 symbol 的原始因子公式
  -> factors.parquet
  -> 每日1%/99%缩尾
  -> 每日 因子 ~ 行业哑变量 + log(total_mv) 的OLS残差
  -> 每日横截面Z-Score
  -> features.parquet
  -> 只读取 passed_factor_cols.json 中25列
  -> 残余 NaN 原地填0
  -> 与5/10/20日原值标签按(symbol,date)内连接
  -> 按symbol/date排序
  -> Purged Walk-Forward模型训练
```

因此，表中公式是原始因子定义，模型看到的是“当日缩尾、中性化、标准化后的残差 Z 分数”。例如模型中的 `BP=1` 不再表示账面市值比等于1，而表示该股票当日 BP 在剔除行业和规模后约高于横截面均值一个标准差。

模型数据加载还有两项重要保护：

- `industry/log_mv/pct_chg/limit_*` 被列为非特征，仅作为元数据或过滤条件；
- 合并表虽同时携带三个未来收益标签，`get_feature_cols` 会排除全部 `label_ret_5/10/20`，防止把其他周期标签误当特征。

### 28.5 270 → 58 的第一轮筛选逻辑

`selected_factor_cols.json` 当前记录：

| 步骤 | 条件 | 数量变化 |
|---|---|---:|
| 初始候选 | 自研112 + Alpha158 | 270 |
| IC强度过滤 | `|IC| >= 0.01` | 剔除32，剩238 |
| ICIR过滤 | 配置阈值0.05 | 当前记录剔除0，仍238 |
| 冗余过滤 | 日频IC序列相关性阈值0.70 | 再剔除180，剩58 |

最终58个因子的 JSON 顺序来自筛选程序的保留顺序，不应被解释为综合重要性排名。相关性去冗余的结果依赖：

- 使用哪个标签计算 IC；
- 训练区间和股票池；
- 缺失日如何处理；
- 相关阈值0.70；
- 同一相关簇中优先保留哪个因子。

任何一项变化都可能改变保留名单，所以 `selected_factor_cols.json` 应和生成它的数据版本一起管理。

### 28.6 58 → 25 的五分组筛选算法

当前代码逐日执行：

1. 读取已经中性化和标准化的58个因子与 `label_ret_5`。
2. 在全部可用交易日上先建立统一的 `all_dates[::5]` 调仓网格，再分别截取训练期和验证期，避免两个区间从各自首日重新计数造成调仓相位变化。
3. 每个调仓日剔除当前因子或标签为 NaN 的股票。
4. 使用 `rank(method="first")` 打破相同因子值，再用 `qcut` 等数量分成5组；G1为低因子组，G5为高因子组。
5. 计算每组股票未来5日收益的等权平均。
6. 从训练期 `factor_evaluation.parquet` 读取 `ic_mean` 符号。原始多空为 `G5-G1`，最终多空序列为：

\[
LS_t=\operatorname{sign}(\overline{IC}_{train})\cdot(R_{G5,t}-R_{G1,t})
\]

7. 五组单调性为组号 \((0,1,2,3,4)\) 与全期五组平均收益之间的 Spearman 相关。
8. 只有训练期同时满足 `ann_return > 0` 且 `abs(monotonic_corr) > 0.8` 才写入25因子清单。注意代码是严格 `>`，正好等于0.8不会通过。
9. 验证期使用相同因子方向单独计算，只展示、不参与 `passed` 判断；测试集完全不进入该脚本。

持有期多空净值和指标计算为：

\[
NAV_k=\prod_{j=1}^{k}(1+LS_j)
\]

\[
AnnualReturn=NAV_T^{252/(5T)}-1
\]

\[
AnnualVol=\operatorname{Std}(LS)\sqrt{252/5}
\]

当前代码的 Sharpe 定义为 `AnnualReturn / AnnualVol`，而不是更常见的 `mean(LS)/std(LS)*sqrt(252/5)`。两者在收益较大时可能不同，面试和报告引用时必须明确口径。

换手率也不是传统的“成交额/组合市值”，而是相邻调仓日 Top 和 Bottom 组成员的 Jaccard 换手：

\[
Turnover(S_t,S_{t+1})=1-\frac{|S_t\cap S_{t+1}|}{|S_t\cup S_{t+1}|}
\]

最后取 Top、Bottom 两侧换手的平均。因此分组报告中的 turnover 不能直接乘组合费率推算真实交易成本。

### 28.7 当前最重要的周期口径不一致

25因子清单明确使用 `label_ret_5` 和5日持有期筛选；但 `models/lightgbm/config.py` 的默认模型标签是 `label_ret_20`，默认滚动步长和组合调仓周期也是20日。

这意味着默认运行：

```text
用5日预测能力筛选25因子
  -> 用这些因子预测20日收益
  -> 20日调仓组合
```

这个设计不一定必然错误：短周期有效因子可能也包含中期信息，模型可以重新学习权重；但它没有证明“对5日有效的25因子就是20日目标的最佳特征子集”。它会带来以下风险：

- 短期反转和量价信号在20日内衰减或反向；
- 适合20日标签的价值、质量、成长因子可能在5日筛选中被提前淘汰；
- Optuna只优化模型参数，无法恢复已经被5日筛选删除的20日有效因子；
- 模型比较结果混合了“模型能力”和“5日特征选择对20日目标的适配程度”。

更严谨的方案应按标签分别生成：

```text
selected_factor_cols_label_ret_5.json
passed_factor_cols_label_ret_5.json
selected_factor_cols_label_ret_10.json
passed_factor_cols_label_ret_10.json
selected_factor_cols_label_ret_20.json
passed_factor_cols_label_ret_20.json
```

然后确保目标标签、分组持有期、滚动步长、预测文件和组合调仓周期在实验 manifest 中明确绑定。也可以把25因子作为统一基线，再与“20日专属筛选”和“全270因子正则化建模”做验证集消融比较。

### 28.8 验证期稳定性分层

根据当前表中的验证年化和单调性，可把25个因子分为三类。这个分层仅用于诊断，不应反向修改当前清单，否则验证集会参与因子选择。

**训练与验证都保持强单调性（验证 `|monotonic_corr| >= 0.8`）**：

- `LOW_TURNOVER`、`LN_TURNOVER`；
- `CORR20`、`CORR60`；
- `VOL_CV20`、`RET_AMT_CORR20`、`RET_VOL_CORR5`；
- `CORD5`、`CORD60`；
- `VOL_OF_VOL_20`、`VOL_PRICE_DIVERGENCE`；
- `BP`、`EP`；
- `CNTD10`、`WVMA30`；
- `NEG_SKEW_20`。

**验证仍有正多空收益，但单调性中等（绝对值约0.4–0.7）**：

- `KLOW`、`AMIHUD_20`、`CNTD60`、`VSTD5`。

**验证收益仍为正但单调性较弱（绝对值不高于0.3）**：

- `CONSEC_UP5`、`OBV_CHG_20`、`IMAX60`、`MAX_DD60`、`Q_SALES_YOY`。

弱单调不等于无效，可能意味着收益集中于某个极端组；需要查看完整 G1–G5 收益、逐年表现和样本覆盖，而不是只看一个相关系数。

### 28.9 因子簇与潜在重复暴露

25个因子可以进一步按经济来源聚成若干簇：

| 因子簇 | 成员 | 共同暴露与风险 |
|---|---|---|
| 低换手/低活跃 | `LOW_TURNOVER`、`LN_TURNOVER`、`AMIHUD_20` | 低流动性、小盘、容量和冲击成本 |
| 量价相关/背离 | `CORR20/60`、`CORD5/60`、`RET_VOL_CORR5`、`RET_AMT_CORR20`、`VOL_PRICE_DIVERGENCE` | 交易行为、趋势/反转、异常成交 |
| 成交量稳定性 | `VOL_CV20`、`VSTD5`、`WVMA30` | 成交活跃状态、机构/情绪代理变量 |
| 价格路径与风险 | `KLOW`、`CNTD10/60`、`IMAX60`、`MAX_DD60`、`NEG_SKEW_20`、`VOL_OF_VOL_20`、`CONSEC_UP5` | 反转、趋势、低波和尾部风险 |
| 价值 | `BP`、`EP` | 银行/周期行业、盈利和净资产口径 |
| 基本面成长 | `Q_SALES_YOY` | 行业景气、财报覆盖和低频陈旧性 |
| 累积量能 | `OBV_CHG_20` | 长历史起点、成交量趋势和极端分母 |

即使每对因子的 IC 序列相关没有超过0.70，簇内因子仍可能在模型中表达相似信息。建议做四组验证集消融：逐因子删除、逐簇删除、只保留每簇代表、全25因子；比较增量 RankIC、换手、行业暴露和跨年稳定性。

### 28.10 复现这份25因子清单所需的最小产物链

```powershell
# 1. 确保 Alpha158 已存在，再构建270因子总表
python main.py build_alpha158
python main.py build_factors

# 2. 生成预处理特征和原值标签
python main.py preprocess_factors

# 3. 训练期IC评估、去弱信号和去冗余
python main.py evaluate_factors
python main.py remove_redundant

# 4. 对58因子做训练期五分组筛选，验证期只报告
python main.py factor_quantile
```

复现后至少核对：

- `factors.parquet` 是否为270个因子而不是仅112个；
- `factor_evaluation.parquet` 使用的标签和日期；
- `selected_factor_cols.json` 是否仍为58个；
- `passed_factor_cols.json` 的 `criteria/n_total/n_passed/factors`；
- 25个名称是否全部存在于 `features.parquet`；
- 训练日志是否打印“精选因子清单过滤: 25 -> 25”；
- 当前模型标签是否和因子筛选标签一致，若不一致是否有明确实验理由。

### 28.11 针对当前25因子的优先改进建议

1. 修正 `CONSEC_UP5` 的名称或实现：若保留当前公式，应改名为“近5日正收益和取负”；若要连续天数，应真正从当前日向前计数连续上涨日。
2. 将 `MAX_DD60` 改为保持时序先后的滚动最大回撤，而不是窗口最高—最低振幅；或者改名为 `RANGE_DD60`。
3. 让换手率和估值因子保留缺失语义，避免把缺失编码成强低换手或0估值信号。
4. 按5/10/20日标签分别筛选因子，消除当前5日筛选—20日默认模型错配。
5. 把分组 Sharpe 改为统一、明确的周期收益均值/标准差年化口径，并保留旧指标用于结果兼容。
6. 为分组收益加入交易成本、涨跌停可实现性和行业权重控制，避免单因子毛收益过度乐观。
7. 对验证期弱单调的5个因子重点查看极端组收益、年度稳定性和覆盖率，而不是立即删除。
8. 对流动性/量价大簇做消融，确认模型增益不是由多个近似信号重复放大。
9. 为每次筛选保存标签、数据日期、股票池、Git commit、输入文件哈希和代码参数。
10. 最终测试前冻结25因子名单；任何根据测试期表现增删因子的行为都应视为测试污染。

---

## 29. 模型训练：从数据矩阵到滚动样本外预测

### 29.1 三个模型共享的训练前提

Ridge、LightGBM 和 MLP 使用同一份 `features.parquet`、`labels.parquet` 和 `passed_factor_cols.json`。共同流程为：

1. 从精选清单读取25个因子；如果清单不存在则退化为全部合法特征，这会改变实验口径，必须关注日志警告。
2. 只投影主键、交易状态元数据和精选因子，避免加载整张270列宽表。
3. 剔除指数、B股和北交所前缀。
4. 将特征和三个标签按 `(symbol,date)` 内连接。
5. 排除主键、行业、市值、涨跌停标记及全部未来标签，确保没有其他周期标签混入特征。
6. 残余 NaN、正负无穷在模型矩阵中转换为0。由于特征已逐日标准化，0近似表示当日横截面中性值，但也会把“缺失”与“恰好等于均值”混在一起。
7. 按股票和日期排序，以交易日数组构造 Purged Walk-Forward 窗口。

三个模型都使用同一个标签周期解析器：`label_ret_5/10/20` 分别得到5/10/20日 purge。未知标签不会静默回退，而是直接报错。

### 29.2 滚动窗口是怎样生成的

默认参数为 `train_len=750`、`valid_len=60`、`step=20`。对每个预测段构造：

```text
750日训练 -> h日隔离 -> 60日局部验证 -> h日隔离 -> 最多20日预测
```

其中 \(h\) 是标签前瞻期。若使用20日标签，训练窗口最后一个样本的标签最远会使用未来20日价格，因此必须在训练和局部验证之间留20个交易日；局部验证和预测段之间同理。

每个窗口按“日期”切分，不按行数切分。当天所有股票只能同时属于同一集合，避免同一市场截面被拆到训练和验证两侧。窗口随后通过 `date.isin(...)` 从1043万行宽表中切出股票样本。

默认滚动步长20日不代表所有模型只预测每20日一个点，而是每个滚动模型连续预测随后最多20个交易日的全市场横截面，然后下一步重新训练。

### 29.3 训练样本过滤与抽样

LightGBM 默认在每个窗口的训练和局部验证样本中剔除普通涨停、跌停日；可选择：

- `all`：剔除 `limit_up/limit_down`，默认；
- `lock`：只剔除一字涨跌停；
- `none`：不做封板过滤。

Ridge 和 MLP 的共用 `valid_rows` 当前固定剔除普通涨停和跌停，没有暴露 `lock/none` 选项。因此三模型虽然使用同一窗口和特征，但封板过滤的可配置能力不完全一致。公平比较时 LightGBM 应保持默认 `all`。

Ridge 默认每步训练样本上限100万行，MLP为40万训练行和15万验证行。超过上限时使用随滚动步变化但可复现的随机种子抽样。抽样是在全窗口股票—日期行中均匀抽行，不保证每个日期、行业或股票等比例，可能改变横截面权重。LightGBM当前没有类似样本上限，使用过滤后的全量窗口。

### 29.4 Ridge 的实际训练逻辑

Ridge 解的是：

\[
\min_{\beta,b}\sum_i(y_i-b-X_i\beta)^2+\alpha\|\beta\|_2^2
\]

当前参数：

- `alpha=10.0`；
- `solver="lsqr"`；
- 每步最多100万训练样本；
- 不使用60日局部验证窗口，也没有早停；
- 每一步保存模型和25个特征名称到 `model.joblib`。

Ridge 的价值不是追求最高拟合能力，而是建立低方差、可解释的线性基线。如果复杂模型仅在训练或单一窗口超过 Ridge，却在完整验证期没有稳定增量，就应优先选择 Ridge 或重新检查复杂模型。

Ridge 参数 `alpha` 越大，系数越收缩。合理搜索应在对数尺度上进行，例如 `0.01,0.1,1,10,100,1000`。但不能为每个滚动步单独挑最优 alpha 后再拼接结果，否则会引入不可部署的后见选择；应该在统一验证规则下冻结一个 alpha。

### 29.5 LightGBM 的实际训练逻辑

每步使用 `lgb.train` 做回归：训练集拟合、60日局部验证集按 MSE 监控，最多1000棵树，连续50轮没有改善则早停。预测时使用该步 `best_iteration`。

默认基础参数：

| 参数 | 默认值 | 控制对象 |
|---|---:|---|
| objective/metric | regression/mse | 回归训练和早停指标 |
| learning_rate | 0.0421 | 每棵树的更新步幅 |
| num_leaves | 210 | 叶节点容量 |
| max_depth | 8 | 树深上限 |
| min_data_in_leaf | 20 | 叶节点最少样本 |
| colsample_bytree | 0.8879 | 每棵树特征采样比例 |
| subsample | 0.8789 | 行采样比例 |
| subsample_freq | 1 | 每棵树执行行采样 |
| lambda_l1 | 205.6999 | L1叶权重正则 |
| lambda_l2 | 580.9768 | L2叶权重正则 |
| num_threads | 8 | 线程数 |
| seed | 42 | 随机种子 |

如果 `best_params.json` 中存在当前标签记录，Optuna参数会覆盖上述默认值；不存在则回退默认参数。参数按标签存储，5/10/20日搜索不会互相覆盖。

每步模型保存为 `models/lightgbm/<label>/step_xxx/lgb_model.txt`。完整预测只保存 `symbol/date/pred/segment`，故意不携带真实未来收益，降低下游直接看到测试答案的风险。

### 29.6 MLP 的实际训练逻辑

当前网络结构：

```text
25维输入 -> Linear(25,64) -> ReLU -> Dropout
         -> Linear(64,32) -> ReLU -> Dropout
         -> Linear(32,1)
```

训练配置：

- AdamW优化器；
- 学习率 `1e-3`；
- weight decay `1e-4`；
- dropout 0.15；
- batch size 4096；
- 最多20个epoch；
- patience 4；
- CPU/GPU自动选择；
- Python、NumPy和PyTorch种子均设为42。

训练损失为 `HuberLoss(delta=0.05)`，降低极端收益对梯度的影响；但早停比较的是局部验证集 MSE，而不是 Huber loss 或 RankIC。也就是说 MLP存在“训练目标=Huber、早停目标=MSE、全局选择目标=RankIC”的三层目标不一致。这不是程序错误，但应通过验证实验确认是否影响排序效果。

每次验证MSE刷新至少 `1e-8` 才算改善；连续4轮未改善提前停止，并恢复最佳验证MSE对应参数。每步保存 `model.pt` 和包含特征名、最佳MSE、实际epoch数的 `metadata.json`。

### 29.7 三模型输出和公平比较

三个模型统一输出：

```text
data/processed/predictions/<model>_pred_<label>.parquet
```

`--quick` 或 `--max-steps` 产生的是带 `_smoke` 后缀的部分预测，不能进入正式三模型比较。`compare_models.py` 只接受无后缀完整预测，并要求 Ridge、LightGBM、MLP 三者都存在且目标区段非空；缺一个即返回失败，避免把“两模型比较”误写成完整结论。

比较指标包括每日 Pearson IC、Spearman RankIC、年化 ICIR/RankICIR、10分组 Top/Bottom 平均收益与多空差。验证期按 `rank_ic_mean` 降序选择模型，写入 `selected_model.json`。组合回测 `--model auto` 读取该文件。

当前“公平比较”仍有需要注意的差异：

- LightGBM使用全量训练样本，Ridge/MLP可能抽样；
- Ridge不使用局部验证窗口；
- MLP与LightGBM早停指标不同；
- 三者虽然预测同一日期，但模型容量和随机性不同。

公平不等于算法过程完全相同，而是数据、特征、标签、滚动边界和最终评价区间一致，并公开训练差异。

### 29.8 预测评估指标的准确口径

每天至少5只股票时计算：

\[
IC_t=Corr_{Pearson}(pred_{i,t},y_{i,t})
\]

\[
RankIC_t=Corr_{Spearman}(pred_{i,t},y_{i,t})
\]

代码中的年化 RankICIR 为：

\[
RankICIR=\frac{mean(RankIC_t)}{std(RankIC_t)}\sqrt{252}
\]

10分组评价每天按预测 rank 等量分组，报告 Top组、Bottom组未来标签均值以及 `Top-Bottom`。这些收益没有交易成本、T+1开盘执行和组合约束，因此只属于模型信号评价。

---

## 30. 参数调整：分层、顺序和防过拟合纪律

### 30.1 参数不能混在一次搜索里

项目存在五类参数：

1. **研究目标参数**：标签5/10/20日、股票池、基准。
2. **数据与因子参数**：缩尾比例、中性化方式、因子筛选阈值、25因子清单。
3. **时间验证参数**：训练长度、局部验证长度、purge、滚动步长。
4. **模型参数**：Ridge alpha、LightGBM树参数、MLP网络与优化参数。
5. **组合参数**：TopK、buffer、换手、仓位、风险和成本。

如果把所有参数在同一个验证集上做大规模联合搜索，搜索自由度会迅速膨胀，验证集将被过拟合。推荐逐层冻结：先目标和数据，再时间框架，再模型，最后组合。

### 30.2 推荐调参顺序

#### 第一层：先固定研究口径

- 固定股票池、样本期、财务PIT规则和可交易过滤；
- 为5/10/20日标签分别生成或明确共享的因子清单；
- 固定评价主指标为验证 RankIC，同时保留 RankICIR、分组收益和换手作为约束指标；
- 测试集完全不可见。

#### 第二层：调整时间结构

优先比较：

- `train_len`: 500/750/1000/1500日；
- `valid_len`: 40/60/120日；
- `step`: 与5/10/20日标签匹配；
- rolling window 与 expanding window；
- 是否加入时间衰减权重。

时间结构决定模型看多远的历史和更新多快，通常比树叶数量更影响市场状态适应性。时间参数确定后再调模型容量。

#### 第三层：建立模型基线

先跑 Ridge alpha 网格，再跑 LightGBM默认参数，再跑MLP默认参数。若复杂模型没有稳定超过 Ridge，不应立刻扩大搜索空间，而应检查标签、样本权重、缺失值和特征周期。

#### 第四层：模型内部参数

LightGBM建议先调容量和最小叶样本，再调采样和正则，最后细调学习率。MLP建议先调学习率和batch size，再调正则和网络宽度。不要一开始同时搜索所有参数。

#### 第五层：组合参数

仅在模型和标签冻结后，用验证集调整 TopK、调仓周期、buffer、行业上限、换手预算和风险仓位。组合参数目标应是扣成本后的超额收益/信息比率，而不是模型 RankIC。

### 30.3 当前 Optuna 的真实搜索方法

Optuna不是在全部滚动窗口上评估每个trial，而是为了计算效率，从测试集之前取一个快速窗口：

```text
最近750日训练 -> h日purge -> 最近60日验证 -> h日purge -> 测试集起点
```

每个trial用TPE采样，LightGBM最多1000轮、早停50轮，然后在这60日验证窗口逐日计算 RankIC，最大化平均 RankIC。

当前搜索空间：

| 参数 | 范围 | 采样方式 |
|---|---:|---|
| learning_rate | 0.005–0.1 | 对数连续 |
| num_leaves | 8–256 | 对数整数 |
| max_depth | 3–12 | 整数 |
| min_data_in_leaf | 5–100 | 对数整数 |
| lambda_l1 | 1e-4–100 | 对数连续 |
| lambda_l2 | 1e-4–100 | 对数连续 |
| feature_fraction | 0.4–1.0 | 连续 |
| bagging_fraction | 0.4–1.0 | 连续 |

默认30次trial，`--quick`为3次，只适合检查流程。TPE种子为42。最优参数会把 `feature_fraction/bagging_fraction` 映射为训练配置使用的 `colsample_bytree/subsample`。

---

## 47. 文件级运行地图——从启动到最终回测

本节以当前仓库代码为准，回答“每个关键文件什么时候运行、由谁调用、读什么、写什么”。目录中的 `__pycache__`、日志、模型权重、临时诊断脚本和历史实验目录不是主流程入口，不应按文件名直接串行执行。

### A.1 总控层

| 文件 | 运行时机 | 机制与职责 |
|---|---|---|
| `main.py` | 所有正式阶段的统一入口 | 将阶段名映射到脚本或 Python 模块，用当前解释器通过子进程执行；`list` 只展示注册表，单阶段执行一个任务，`all` 按 `ALL_ORDER` 依次执行。 |
| `config/settings.py` | 各模块导入时 | 集中定义项目根目录、原始数据、处理数据、模型和报告路径，并读取 `.env` 中的配置。 |
| `requirements-ml.txt` | 建环境时 | 提供机器学习、Qlib、Optuna、MLflow 等依赖；`requirements-deploy.txt` 只用于部署环境。 |
| `.env` / `.env.example` | 启动前 | 提供 Tushare、DeepSeek 等密钥；密钥不应写入代码或提交到仓库。 |

当前 `main.py all` 的真实顺序是：

```text
fetch_daily -> fetch_financial -> merge_data -> fetch_stock_basic
-> clean_data -> fetch_daily_basic -> build_factors -> build_alpha158
-> preprocess_factors -> neutralize -> factor_quantile
-> optuna_search -> train_lgb -> lgb_baseline
```

这不是完整研究流程：它不会自动执行因子 RankIC 评估、冗余剔除、多模型比较、组合回测和最终测试。并且从空目录开始时，应手动把 `build_alpha158` 放在 `build_factors` 前面，以确保总因子表能够合并 Alpha158。`all` 当前即使某个阶段失败，也会继续尝试后续阶段，最后统一返回失败状态。

### A.2 数据采集与底表层

| 文件 | 前置文件/数据 | 核心机制 | 主要产物 |
|---|---|---|---|
| `scripts/fetch_daily.py` | Qlib `cn_data` | 按市场读取全量日线，统一股票代码、日期和字段后写 Parquet；支持限制样本量用于冒烟测试。 | `data/raw/daily/` |
| `scripts/fetch_financial.py` | Tushare、日线股票池 | 断点续传三大报表和指标表；按公告日构造 Point-in-Time 财务事件表，避免把未来公布的信息提前使用。 | `data/raw/financial/`、`financial_events.parquet` |
| `scripts/merge_data.py` | 日线、财务事件表 | 按股票和交易日做 as-of 时点合并，只连接当日可获得的最近财务信息。 | `merged_data.parquet` |
| `scripts/fetch_stock_basic.py` | Tushare | 获取上市日期、退市/名称变更等静态及历史信息，供股票池过滤和历史 ST 判断使用。 | `stock_basic.parquet`、`namechange.parquet` |
| `scripts/clean_data.py` | `merged_data.parquet`、股票基础信息 | 过滤指数、ST、次新股、重复记录和不可交易样本；对财务字段按规则前向填充，不对价格制造虚假成交。 | `basic_cleaned.parquet` |
| `scripts/fetch_daily_basic_by_date.py` | 清洗底表、Tushare交易日接口 | 按历史交易日和当时股票池拉取市值、估值、行业、涨跌停等数据，再合并回底表。 | `basic_cleaned_with_extra_by_date.parquet`、原始 daily_basic |
| `data_catalog/audit.py` | 原始数据目录 | 建立文件、日期、股票覆盖和字段契约，作为第 0 层审计。 | `data_catalog/raw_data_inventory.json` |

### A.3 因子与知识层

| 文件/目录 | 运行时机 | 机制 |
|---|---|---|
| `factors/registry.py` | 构建因子前 | 注册因子名称、分类、输入字段、计算函数和时间契约；注册表是因子计算的唯一目录。 |
| `factors/base/`、`factors/composite/` | `build_factors` 调用 | 分别实现基础技术/基本面因子和复合因子；每个因子接收按日期排序的横截面数据并返回同长度序列。 |
| `scripts/build_factors.py` | 数据准备完成后 | 读取增强底表，遍历注册表计算因子，生成标签并按主键合并为总表。 |
| `scripts/build_alpha158.py` | Qlib 行情准备后 | 通过 Qlib Alpha158 配置生成 158 个标准因子，单独落盘，避免和自研因子计算耦合。 |
| `factor_catalog/build.py`、`audit.py`、`sota.py` | 因子研究前 | 建立原子因子目录、审计来源和字段，并生成正式的人工优选因子清单；不等同于重新计算因子值。 |
| `quantmind_pipeline/` | QuantMind 因子扩展时 | 提供 DSL、财务报告和行业原子因子等受约束的候选表达式。 |
| `quantmind_integration/` | 外部因子接入或审计时 | 负责算子映射、字段白名单、策略约束和 RD-Agent 接口，不是日常行情主链。 |
| `knowledge/` | 研究设计和复核时 | 保存未来函数、市场状态、组合构建、微观结构和因子评价规范，是规则文档而非可执行阶段。 |

### A.4 分析、预处理与模型层

```text
factors.parquet
  -> analysis/evaluate_factors.py
  -> analysis/remove_redundant_factors.py
  -> analysis/factor_quantile.py
  -> preprocessing/preprocess_factors.py
  -> models/*/train.py
```

| 文件 | 机制与输出 |
|---|---|
| `analysis/evaluate_factors.py` | 按日期计算每个因子与未来收益的 RankIC，输出日序列和汇总统计。 |
| `analysis/remove_redundant_factors.py` | 根据 IC 序列相关性和质量阈值保留代表因子，输出保留/删除清单；它不修改原始因子表。 |
| `analysis/factor_quantile.py` | 将股票按因子分成五组，检查单调性、多空收益和训练/验证稳定性，生成 `passed_factor_cols.json`。 |
| `preprocessing/preprocess_factors.py` | 严格按训练窗口规则完成缩尾、行业/市值中性化、标准化和标签整理，输出 `features.parquet` 与 `labels.parquet`。 |
| `preprocessing/neutralize.py` | 独立的 OLS 残差分析工具；产物用于诊断或替代性实验，不会自动替换正式模型输入。 |
| `models/lightgbm/optuna_search.py` | 在验证窗口搜索 LightGBM 参数并保存 `best_params.json`。 |
| `models/lightgbm/train.py` | 使用 Purged Walk-Forward 滚动训练，逐窗口保存模型和预测，不把未来标签带入当前训练。 |
| `models/ridge/train.py`、`models/mlp/train.py` | 使用相同的时间边界、标签和特征生成线性基线与神经网络预测，便于公平比较。 |
| `models/baselines/train.py` | 生成等权、IC 加权等非机器学习基准。 |
| `analysis/compare_models.py` | 对不同模型的同日期预测计算 IC、分层和收益指标，输出模型选择依据。 |

### A.5 组合回测、文本和验证层

| 文件 | 运行逻辑 |
|---|---|
| `backtest/run_backtest.py` | 读取预测和交易底表，在 T 日收盘形成信号、T+1 日开盘执行，处理 TopK、Buffer、行业/市值约束、涨跌停、整手、成本和换手限制。 |
| `backtest/portfolio_optimizer.py`、`alpha_signal.py`、`engine.py` | 分别负责组合约束/优化、信号整理和逐日回测状态推进；`run_backtest.py` 是外部入口。 |
| `analysis/tune_portfolio.py` | 只在验证集内部做时间留出，选择组合参数，不能读取测试集。 |
| `textdata/collector.py`、`rss.py`、`json_source.py`、`tushare_source.py`、`storage.py`、`schema.py` | 采集、校验、哈希去重并保存新闻/政策/公告文本；`collect_text` 是与行情主链并行的扩展入口。 |
| `scripts/lgb_baseline.py` | 使用 Qlib 自有数据源完成独立 Alpha158 + LightGBM 基线，不依赖本项目 Parquet。 |
| `tests/` | 对时间切分、因子、数据契约、模型和回测约束做回归验证；修改核心逻辑后应优先运行相关测试。 |
| `reports/`、`mlruns/` | 保存实验、指标、回测和审计结果，是输出目录，不是输入脚本。 |

### A.6 从零开始的实际执行命令

```powershell
# 1. 审计与数据
python main.py audit_raw_data
python main.py fetch_daily
python main.py fetch_financial
python main.py merge_data
python main.py fetch_stock_basic
python main.py clean_data
python main.py fetch_daily_basic

# 2. 因子
python main.py audit_operators
python main.py audit_quantmind_integration
python main.py build_factor_catalog
python main.py audit_factor_catalog
python main.py build_alpha158
python main.py build_factors

# 3. 因子筛选与预处理
python main.py evaluate_factors
python main.py remove_redundant
python main.py factor_quantile
python main.py preprocess_factors

# 4. 模型比较
python main.py optuna_search --label label_ret_20
python main.py train_lgb --label label_ret_20 --eval-segments valid
python main.py train_ridge --label label_ret_20 --eval-segments valid
python main.py train_mlp --label label_ret_20 --eval-segments valid
python main.py compare_models --label label_ret_20 --segment valid

# 5. 组合验证与最终测试
python main.py tune_portfolio
python main.py portfolio_backtest --model auto --label label_ret_20 --segment valid
python main.py portfolio_backtest --model auto --label label_ret_20 --segment test --confirm-final-test
```

`label_ret_5`、`label_ret_10`、`label_ret_20` 应视为三套独立研究目标；更换标签后应重新训练、评估和比较，不能直接复用另一标签的预测结果。任何测试集结果只能在方案冻结后产生，不能再反向修改因子、模型或组合参数。

### 30.4 当前 Optuna 逻辑的局限

- 只优化一个末端60日窗口，容易选择恰好适合该市场状态的参数；注释所说“完整滚动回放最优trial”并未在 `optuna_search.py` 内自动执行，仍需另跑 `train_lgb`。
- 搜索目标是RankIC，但早停目标仍是MSE；一个trial可能因MSE不再改善而提前停止，即使RankIC还有变化。
- `--limit-filter` 参数传入 `_quick_split`，但当前内部始终按普通涨停/跌停过滤，没有根据 `lock/none` 分支执行。这是参数接口与实现不一致。
- 30次trial已经构成多重比较；`best_rank_ic` 是候选最大值，天然存在赢家偏差。
- 搜索空间允许 `num_leaves` 与 `max_depth` 组合不协调，例如叶数远大于深度可实现容量。
- 没有记录每个trial的完整日频IC、训练耗时、最佳迭代数和参数稳定性。

更严谨的调参应采用验证期内多折 Purged Walk-Forward：每组参数在多个验证窗口得到 RankIC，再优化均值减去不稳定性惩罚，例如：

\[
Score=mean(RankIC_{fold})-\lambda\,std(RankIC_{fold})
\]

也可以使用年度最差RankIC、正IC年份比例或多目标优化，避免单一市场状态支配选择。

### 30.5 LightGBM 参数之间的联动

- 较小 `learning_rate` 通常需要更多树；比较时应依赖早停后的有效树数，而不是固定树数。
- `num_leaves` 和 `max_depth` 共同控制容量。深度8的理论最大叶数为256，当前默认210接近高容量端。
- `min_data_in_leaf` 应结合样本数理解。百万级样本下20非常小，叶节点可能学到局部股票/日期噪声。
- L1/L2正则与叶节点容量联动；强正则可允许更多叶，弱正则应收紧深度和叶数。
- 特征只有25个时，`feature_fraction=0.4` 每棵树大约只看10个因子；这可去相关，也可能遗漏关键因子交互。
- 行采样改变不同日期和股票的权重。随机按行采样不会保持完整日截面，若目标是横截面排序，可研究按日期分层采样。

### 30.6 MLP 推荐搜索空间

当前没有自动化MLP调参器。合理的验证网格可从小规模开始：

| 参数 | 建议候选 | 主要影响 |
|---|---|---|
| learning_rate | 3e-4/1e-3/3e-3 | 收敛速度和稳定性 |
| weight_decay | 1e-5/1e-4/1e-3 | 参数收缩 |
| dropout | 0/0.1/0.2/0.3 | 随机正则 |
| batch_size | 2048/4096/8192 | 梯度噪声和显存 |
| hidden size | 32-16/64-32/128-64 | 网络容量 |
| Huber delta | 0.02/0.05/0.10 | 对极端标签敏感度 |
| patience | 3/4/6 | 早停容忍度 |

MLP每组参数应在相同抽样种子和窗口上比较，并至少用多个随机种子检查方差。只报告最好一次运行会低估神经网络的不稳定性。

### 30.7 组合参数怎样调才合理

建议做分层敏感性分析，而不是直接用优化器联合搜索：

- TopK：30/50/80/100，观察信号浓度、个股风险和容量；
- 调仓：5/10/20日，必须与标签周期和成本共同解释；
- buffer：0/10/30/50；
- max_drop：3/5/10；
- 行业上限：15%/20%/25%；
- 单次换手预算：10%/15%/25%；
- 目标波动：20%/25%/35%；
- 最低仓位：0%/30%/60%；
- 风险优化开关：等权与均值—方差严格并行比较。

每次只调整一组相关参数，输出验证集净收益、信息比率、最大回撤、平均/最大换手、平均仓位、不可成交比例和行业偏离。组合参数冻结后再生成配置哈希。

### 30.8 防止参数搜索污染的实验台账

每次正式实验至少记录：

- experiment id和Git commit；
- 数据文件版本/哈希；
- 因子清单及其生成标签；
- 模型标签、滚动参数和随机种子；
- 完整模型参数；
- 组合参数与成本版本；
- 使用的区间只能是train/valid；
- 主指标、辅助指标和预先声明的选择规则；
- 被放弃方案也要记录，避免重复试验后只保留赢家。

---

## 31. 回测风险控制：从信号风险到最终测试审计

### 31.1 风控不是一个止损参数

当前项目的风险控制可分为七层：

```text
数据风险
 -> 特征暴露风险
 -> 模型预测风险
 -> 股票选择风险
 -> 权重与仓位风险
 -> 交易执行与流动性风险
 -> 回测/测试集过拟合风险
```

每一层解决的问题不同。只加入回撤降仓，不能修复未来函数；只做行业中性化，也不能解决涨停买不进和容量问题。

### 31.2 数据和研究风险控制

已经实现：公告日PIT、历史ST、次新过滤、停牌NaN、标签purge、验证/测试分离和最终测试锁。

仍需补充：退市股覆盖审计、历史行业PIT、财务修订版本、数据异常告警、公司行动和真实未复权成交价账本。

### 31.3 模型输出风险控制

每个信号日对预测值执行：

1. 剔除预测、市值或行业无效的股票；有效股票少于20时返回空信号。
2. 按1%/99%分位缩尾模型分数。
3. 回归截距、`log(circ_mv)` 和行业哑变量。
4. 对残差做Z-Score，得到 `alpha`。
5. 按alpha降序排名。

这一步控制模型重新形成的行业和规模暴露，但没有直接控制 Beta、波动、价值、动量等其他风格，也没有对预测置信度或模型分歧做过滤。

### 31.4 股票名单风险控制

默认Top50，原持仓只有跌出Top80才进入主动淘汰候选，且一次最多主动换出5只。行业上限在名单阶段按股票数量近似为 `floor(50*20%)=10` 只。

名单机制降低集中和换手，但有以下边界：

- 被强制排除或不再有有效排名的旧持仓不受 `max_drop` 的主动换出定义保护；
- 行业限制按股票数，不是成交后实际权重；
- 无足够其他行业候选时，最终持股可能少于50；
- 不可卖持仓会导致实际行业和股票权重超过目标；
- 没有单独限制ST以外的风险警示、退市整理期或重大停牌预期。

### 31.5 波动率目标仓位

使用此前最多60日组合净值估计年化已实现波动：

\[
\hat\sigma_t=std(r_{p,t-59:t})\sqrt{252}
\]

波动缩放：

\[
s_{vol}=\min(1,0.35/\hat\sigma_t)
\]

如果历史不足或波动不可用，缩放默认为1。该方法只在实际波动超过35%时降仓，不在低波时加杠杆。

风险点：用策略自身净值估计波动会有滞后；60日窗口可能对突发风险响应较慢；未使用指数或个股前瞻波动；仓位调整只在调仓日成交。

### 31.6 回撤分级仓位

交易前权益相对历史峰值回撤：

\[
DD_t=EquityOpen_t/PeakEquity_{t-1}-1
\]

分级上限：

- 回撤达到-20%：最高90%仓位；
- 达到-30%：最高75%；
- 达到-40%：最高60%。

最终目标仓位：

\[
Exposure_t=\max(0.60,\min(1,s_{vol},s_{dd}))
\]

这意味着无论风险多高，当前配置最低仍保留60%仓位。它属于风险缓释，不是止损清仓。回撤降仓可能在底部卖出并造成路径依赖，因此阈值只能在验证集研究，且应与再入场规则共同评估。

### 31.7 换手预算

首次建仓不设换手预算；后续调仓的成交名义金额预算为：

\[
Budget=EquityOpen\times 15\%
\]

代码把卖出和买入名义金额都累计进 `traded_notional`，因此是双边总成交预算，不是单边换手。若目标仓位比上次降低，卖出风险仓位时可突破预算，避免换手约束阻止必要降险；卖出后买入仍会受到剩余预算和现金限制。

这会导致实际目标组合无法一次到位，是有意的交易平滑。风险是旧持仓残留、Alpha兑现变慢和实际行业暴露偏离目标。

### 31.8 开盘涨跌停与可交易判断

回测入口读取 Qlib复权 `open/close/factor`，计算：

\[
ActualOpen=Open/Factor,\quad ActualClose=Close/Factor
\]

再用上一交易日真实收盘和板块涨跌停比例计算四舍五入到0.01元的理论开盘涨跌停价。主板10%、科创板20%、创业板改革后20%、北交所30%。开盘价达到理论涨停价则不能买，达到跌停价则不能卖。

这种处理比使用全天收盘涨跌幅更符合T+1开盘执行时点，但仍是保守简化：开盘封板后可能盘中打开，回测一律视为相应方向无法成交；ST股票已在上游过滤，因此没有5%规则。

### 31.9 成交、现金和停牌风险

- 无有效开盘价或成交量非正数：不可交易；
- 先卖后买，降低现金不足；
- 目标股数按100股向下取整；
- 买入量同时受目标、现金和换手预算约束；
- 费用按 `max(最低5元, 名义金额×费率)`；
- 不可卖股票继续持有；
- 当日无有效收盘价时使用最近有效收盘估值。

当前没有成交量参与率、订单冲击、滑点、排队成交概率、印花税明细、分红和公司行动账本。对低流动性因子较多的25因子组合，这些缺口尤其重要。

### 31.10 默认等权和可选风险优化

默认 `use_mean_variance_optimizer=False`，选中股票在目标风险仓位内等权：

\[
w_i=Exposure/N
\]

等权对高噪声Alpha更稳健，参数少、可解释。`max_stock_weight` 和基于权重的行业约束在默认等权路径中不直接生效；行业控制已在股票名单阶段按数量执行。

开启优化器后，目标函数为：

\[
\max_w \quad \tilde\alpha^Tw
{}-0.5w^T\Sigma w
{}-0.003\left(\|w-w_{prev}\|_1+w_{outside}\right)
\]

其中 Alpha 先裁剪到[-3,3]再乘0.025。约束包括：

- 只做多，\(w_i\ge0\)；
- 单票不超过4%；
- 权重和等于目标仓位；
- 单行业权重不超过20%；
- 有旧持仓时总换手不超过15%。

### 31.11 协方差估计

优化器使用信号日及以前最多120日收盘价。收益缺失先用当日横截面中位数填充，再填0。若有效观察不少于40日，使用 Ledoit–Wolf 收缩协方差并年化；不足时退化为对角方差矩阵。

Ledoit–Wolf比样本协方差在股票数接近观察数时更稳定。但仍存在：

- Top50、120日样本下估计误差仍大；
- 横截面中位数填充可能低估停牌股风险；
- 没有显式因子风险模型；
- 股票名单每期变化，协方差不连续；
- 历史波动未必代表未来风险。

求解器优先CLARABEL，失败后尝试SCS；若换手硬约束造成不可行，会放宽最后一个换手约束；仍无解则退化等权。回测报告目前没有单独记录每次是否触发求解器fallback，这是应补的审计字段。

### 31.12 绩效与风险指标

已实现：

- 年化收益和基准年化收益；
- 年化超额收益；
- 年化波动率；
- Sharpe；
- 信息比率；
- 最大回撤；
- 平均日换手；
- 平均目标仓位；
- 最大再平衡换手；
- 总成本和交易日数。

`nav.parquet` 每日保存 `target_exposure/realized_volatility/pretrade_drawdown`，便于检查降仓触发。

建议补充 Beta、跟踪误差、Calmar、Sortino、月度胜率、回撤持续期、行业/风格暴露、容量、成交失败率和滚动一年指标。还应区分毛收益与净收益贡献，把交易成本、仓位控制、名单变化分别归因。

### 31.13 测试集和配置审计

统一组合回测默认只允许验证集。测试集必须同时满足：

1. 显式 `--segment test`；
2. 显式 `--confirm-final-test`；
3. 不存在既有 `final_test_audit.json`。

输出 `summary.json` 保存模型、标签、完整组合配置、配置SHA-256和指标。测试成功后写入审计锁，记录UTC时间、模型、标签、配置哈希和报告路径。

配置哈希能识别组合配置变更，但当前不包含：Git commit、数据文件哈希、预测文件哈希、因子清单内容和模型文件哈希。因此它还不是完整可复现实验指纹。正式最终评估前应把这些内容纳入manifest。

### 31.14 风控参数的推荐验证矩阵

| 风控层 | 核心参数 | 验证观察 |
|---|---|---|
| 信号暴露 | 输出缩尾0.5%/1%/2% | RankIC、行业/市值残余暴露 |
| 名单分散 | Top30/50/80，行业15%/20%/25% | 超额、个股贡献、行业偏离 |
| 换手 | buffer、max_drop、10%/15%/25%预算 | 净收益、信号延迟、成本 |
| 波动目标 | 20%/25%/35% | 年化收益、波动、Sharpe、平均仓位 |
| 回撤仓位 | 不降仓/当前阶梯/更激进阶梯 | 最大回撤、恢复期、底部卖出损失 |
| 权重方法 | 等权/优化器 | 净IR、集中度、求解失败率 |
| 成本 | 基础/双倍费率/冲击模型 | 盈亏平衡成本、容量 |

风险参数也会过拟合。应预先限定少量经济合理方案，按验证集综合指标选择，冻结后一次性进入测试，而不是看到测试回撤后再调整阶梯。

---

## 32. 项目真实历程：从“能训练”到“能经受质疑”

这一节不是理想化流程，而是本项目实际发生过的研究和工程演进。面试时如果被问“你具体做了什么、遇到什么问题、怎么做决策”，应以这条时间线回答。

### 32.1 第一阶段：浏览、运行和理解原项目

最初工作不是立刻改模型，而是梳理仓库的数据流、运行环境和已有产物：

- 确认项目是A股日频横截面选股，而不是时间序列预测单只股票；
- 确认数据规模达到约1043万股票—交易日行、270个候选因子；
- 识别训练/验证/测试三段及已有Qlib、LightGBM链路；
- 检查后台训练日志、模型输出和环境兼容性；
- 明确模型输出是复合Alpha分数，不是可直接交易的目标持仓。

这一阶段形成的核心认识是：模型预测只是项目中间环节，最终是否有价值取决于信号中性化、股票选择、交易约束、成本和风险控制。

### 32.2 第二阶段：严格化时间验证

项目采用全局时间划分：训练期2016-01至2023-04，验证期2023-05至2025-01，测试期2025-01以后。模型内部进一步采用Purged Walk-Forward：

```text
750日训练 -> 20日purge -> 60日局部验证 -> 20日purge -> 20日预测
```

为什么不是随机K折：同一天股票共享市场状态，相邻日期标签窗口重叠；随机打散既泄露未来市场状态，也让未来20日标签跨集合重叠。测试增加了两个明确断言：任何训练样本的标签终点必须早于局部验证开始，任何局部验证样本的标签终点必须早于预测开始。

### 32.3 第三阶段：补充Ridge与PyTorch MLP

为了判断LightGBM的非线性增益是否真实，补充两类有明确角色的模型：

- Ridge：低方差线性基线，固定L2正则；
- MLP：`25 -> 64 -> 32 -> 1`，ReLU、Dropout、AdamW、HuberLoss和早停；
- LightGBM：表格数据主模型，使用局部验证早停和Optuna参数。

PyTorch环境安装完成后，三个模型统一使用相同因子清单、`label_ret_20`、滚动日期和全局验证区间。没有继续对5/10/20日标签做大规模联合网格搜索，原因是搜索自由度已经很大，继续扩展会更快消耗验证集。

### 32.4 第四阶段：三模型比较与主模型选择

三模型结果说明“每个指标第一”并不属于同一个模型：

- Ridge RankICIR最高，说明线性关系最稳定；
- MLP RankIC最高，但只比LightGBM高0.001582，且多空收益最低；
- LightGBM IC最高、Top-Bottom收益最高，统一组合收益转化最好。

因此没有按一个事后拼凑的总分选模型，也没有因为MLP是深度学习就优先采用。最终选择LightGBM，Ridge和MLP分别作为线性与神经网络对照。

如果面试官追问评分公式，应说明：`MeanRankIC + a*RankICIR - b*IC波动 - c*复杂度`只是一种概念表达，RankICIR本身已经包含均值和波动，直接重复相加会双重计量稳定性；三种模型复杂度也没有统一量纲。本项目真实选择依据是多指标评分卡和相同组合规则下的收益转化，而非任意加权总分。

### 32.5 第五阶段：从模型预测连接组合回测

模型分数每天再次进行缩尾、行业/市值中性化和Z-Score，然后执行：

```text
Top50 + Buffer30 + 每次最多主动换出5只
+ 单行业最多20%
+ 20日调仓
+ T日收盘信号、T+1开盘成交
+ 双边总成交预算
+ 费用与100股整手
```

输出端再次中性化不是重复劳动：输入端控制单因子暴露，输出端控制非线性模型重新组合出来的行业和规模暴露。

### 32.6 第六阶段：尝试风险覆盖层

原始LightGBM等权组合一度得到：年化8.54%、超额10.02%、波动34.37%、Sharpe 0.410、最大回撤-37.97%、日均换手1.32%。风险明显过高，因此最初加入较激进的覆盖层：20%目标波动、最低25%仓位，并在-10%/-20%/-30%回撤时将仓位降到75%/50%/25%。

结果变为：年化1.88%、超额3.36%、波动22.04%、Sharpe 0.195、最大回撤-29.85%。风险下降，但收益被过度压缩。这个实验说明：回撤控制是滞后的，过早降仓会在底部卖出；风险目标不能只看回撤改善，也必须检查收益和恢复期。

随后采用更温和的35%目标波动、最低60%仓位和-20%/-30%/-40%回撤阶梯。测试期平均仓位仍为100%，说明测试期的好结果主要来自选股，而不是风险择时。

### 32.7 第七阶段：尝试均值—方差优化，但主动放弃

参考增强指数和交易成本组合优化，项目实现了：

\[
\max_w\;\alpha^Tw-\lambda w^T\Sigma w-\eta\|w-w_{old}\|_1
\]

其中协方差使用120日收益和Ledoit–Wolf收缩，约束只做多、单票4%、行业20%和换手预算。为了防止在整个验证集上寻找最好参数，又将验证集拆为：

- 组合调优段：2023-05-04至2024-03-29；
- 组合留出段：2024-04-01至2024-12-31。

第一轮对比中，均值—方差的`balanced`方案在调优段年化-5.36%、波动9.14%、最大回撤-10.65%，但留出段年化-21.73%、超额-34.95%、IR -1.46。它虽然显著压低风险，却破坏了Alpha收益转化。

放弃原因不是优化理论错误，而是当前条件下估计误差过大：Top50、120日样本、股票名单不断变化，协方差对个股权重的影响压过了噪声较高但有效的排序信号。最终将优化器保留为可复现实验，默认关闭，采用更稳健的等权方案。

### 32.8 第八阶段：选择低换手稳健等权

随后预先声明四个少量方案：稳健等权、不降仓等权、低换手等权、均值—方差。调优段综合考虑Sharpe、IR、超额、回撤、波动和换手，低换手等权领先；其核心变化是把单次双边成交预算限制到15%。

它没有同时最大化所有指标，而是在收益、风险和交易摩擦之间取折中。项目没有切换到Backtrader，因为当前问题是几千只股票的横截面信号和组合约束，现有向量化引擎/Qlib式组合研究更直接；Backtrader更适合事件驱动执行、订单生命周期和经纪商仿真，不能自动解决Alpha、风险模型或过拟合。

### 32.9 第九阶段：首次最终测试与异常警觉

参数冻结后执行了第一次测试集回测，旧成交规则得到年化21.49%、Sharpe 1.446、最大回撤-13.77%。由于结果显著高于验证集，没有直接当作成功，而是主动检查：

- 测试预测是否使用当前或未来标签；
- 20日purge是否真正隔离；
- 测试是静态模型还是滚动再训练；
- 月度收益是否由少数异常日主导；
- 涨跌停和价格口径是否包含未来信息。

审计发现模型没有把未来标签放入特征，20日purge也满足边界。但测试属于每20日更新的滚动再训练样本外模拟，不是冻结一个模型到期末；这一点必须在报告中披露。

### 32.10 第十阶段：发现并修复开盘成交未来信息

旧回测在T+1开盘成交，却使用当日完整`pct_chg`生成的`limit_up/down`判断能否成交，相当于开盘时提前知道收盘是否涨跌停。这是明确的日内时序错误。

修正方式：

1. 用Qlib `factor` 把前复权OHLC还原为真实价格；
2. 按股票取得上一交易日真实收盘价；
3. 按主板10%、科创板20%、创业板改革后20%、北交所30%计算理论涨跌停价；
4. 价格按0.01元四舍五入；
5. 只有开盘价已经触及涨停时禁止买入，触及跌停时禁止卖出；
6. 不使用当天收盘涨跌幅和全天最高最低价。

新增测试覆盖两种情形：开盘未涨停但收盘涨停应允许开盘买入；昨收10元、主板开盘11元应禁止买入。

### 32.11 第十一阶段：顺带发现非确定性Bug

纠错复跑时相同参数出现不同结果。原因是旧持仓以Python `set` 传入，行业名额分配受集合遍历顺序影响，而Python不同进程的哈希随机化会改变顺序。

修正为：保留持仓先按模型排名、再按股票代码稳定排序。新增测试验证正序和逆序构造集合得到完全相同名单。修复后验证集连续运行两次，指标完全一致。

这是一个重要工程教训：固定随机种子并不能保证所有代码可复现；无序容器参与交易决策也会造成隐含随机性。

### 32.12 第十二阶段：测试集纠错复算

首次测试已经写入审计锁，因此没有删除锁或覆盖报告。项目新增一次性纠错入口：

```text
backtest/run_test_correction.py
```

该入口要求原审计锁存在、新旧配置哈希不同、同一纠错ID从未运行；新结果写入独立目录，并记录`parameters_retuned=false`。修正测试得到年化21.70%、Sharpe 1.461、最大回撤-13.73%，与旧结果接近，说明全天涨跌停错误不是测试高收益的主要来源。

---

## 33. 实验结果台账：成功、失败和口径变化

### 33.1 模型层正式结果

| 模型 | 角色 | IC | RankIC | RankICIR | 多空差 | 结论 |
|---|---|---:|---:|---:|---:|---|
| Ridge | 线性基线 | 0.0821 | 0.0914 | **18.19** | 0.0239 | 稳定、可解释，但非线性不足 |
| LightGBM | 主模型 | **0.0930** | 0.0967 | 16.95 | **0.0270** | 综合收益转化最好，最终采用 |
| MLP | 神经网络对照 | 0.0817 | **0.0982** | 15.78 | 0.0228 | 排序略高但稳定性和组合转化不足 |

### 33.2 组合层演进

| 阶段 | 年化 | 波动 | Sharpe | 最大回撤 | 日均换手 | 状态 |
|---|---:|---:|---:|---:|---:|---|
| LGB原始等权、旧成交口径 | 8.54% | 34.37% | 0.410 | -37.97% | 1.32% | 风险过高，废弃 |
| 激进风险覆盖、旧成交口径 | 1.88% | 22.04% | 0.195 | -29.85% | 1.18% | 收益压缩过度，废弃 |
| 低换手方案、旧成交口径 | 5.78% | 17.23% | 0.412 | -21.64% | 0.99% | 后发现成交未来信息，不再引用 |
| `open_limit_v2` + 稳定排序，验证集 | **2.27%** | **16.39%** | **0.219** | **-21.34%** | **0.98%** | 当前有效验证结果 |
| `open_limit_v2`，最终测试纠错复算 | **21.70%** | **14.15%** | **1.461** | **-13.73%** | **1.01%** | 当前测试结果，禁止调参 |

不要把这些阶段当作同一口径下的参数曲线：前三行使用了后来确认有问题的旧成交规则，只用于展示研究演进和错误影响；正式结果只引用最后两行。

### 33.3 为什么测试远好于验证

测试期模型RankIC约0.0841、IC约0.0567，均低于验证期，并不存在预测指标异常暴增。高组合收益更可能来自：

- 2025年上半年市场环境与模型偏好的低换手、流动性/量价、价值信号较匹配；
- Top50头部收益转化可能好于全截面平均IC；
- 测试窗口仅383个交易日，年化估计误差较大；
- 基准是沪深300，组合并非严格指数增强，仍可能有中小盘和其他风格暴露；
- 滚动再训练能够逐步吸收较早测试期已经实现的信息。

测试月度并非持续占优：2025年8月、2026年4至6月出现明显负超额。这支持“阶段适配”而不是“稳定套利”的解释。

---

## 34. 面试官高压追问：建议回答框架

### 34.1 “测试这么好，是不是未来函数？”

建议回答：

> 我第一反应也是先怀疑回测。审计后没有发现标签直接进入特征，滚动窗口在训练—验证和验证—预测之间都按20日标签设置purge；测试RankIC反而低于验证期。但我确实发现成交层曾用全天涨跌停状态判断次日开盘成交，这是日内未来信息。我修正后保留原审计报告，通过独立纠错入口复算；测试仍约21.7%，所以该错误不是高收益主因。不过测试只有约1.5年，且组合不是严格沪深300增强，我不会把这个年化外推为长期预期。

### 34.2 “你都看过测试集了，修正后为什么还能再跑？”

> 重新运行只允许用于实现错误纠正，不能用于参数选择。我没有删除原锁、没有覆盖旧结果，也没有改变TopK、模型、换手或风控参数。新入口要求旧锁存在和新旧哈希不同，并写独立纠错审计，明确`parameters_retuned=false`。修正后不再根据测试结果做任何优化。

### 34.3 “为什么验证Sharpe只有0.22，还敢说策略有效？”

> 我不会把它描述成已经稳定有效。验证期有正超额和正IR，但绝对收益和Sharpe偏弱；测试期强很多，说明明显存在状态依赖。项目价值更多在完整研究闭环和对错误、过拟合、成交约束的处理。若作为投资结论，需要更长的冻结前向期、风格归因和冲击成本压力测试。

### 34.4 “为什么最终不选RankIC最高的MLP？”

> MLP RankIC只比LightGBM高约0.0016，但IC、RankICIR和多空收益都更弱，原统一组合表现也明显较差。全截面排序略微改善可能发生在中部股票，未必改善Top50。LightGBM在结构化因子数据上训练更稳定，头尾收益分离最好，因此按组合转化而不是单一RankIC选择。

### 34.5 “为什么不做模型融合？”

> 融合只有在模型误差互补时才有效。当前没有先验冻结的融合权重实验，看到三个结果后再搜索权重会继续消耗验证集。Ridge可作为稳定锚，但直接等权也可能稀释LightGBM头部信号，因此将融合保留为未来研究，而不是为了增加复杂度强行加入。

### 34.6 “为什么不用LSTM？”

> 当前输入是每日横截面表格因子，主要问题是跨股票排序和可交易组合，而不是单股票长序列表示。LSTM需要重新定义序列样本、掩码、财务低频对齐和滚动训练，计算成本显著提高，也未必超过LightGBM。先用Ridge、树模型和MLP证明基线更合理；只有在引入明确的序列状态、订单流或多频数据后，LSTM/Transformer才更有必要。

### 34.7 “为什么不用Backtrader？”

> Backtrader擅长事件驱动订单仿真、经纪商规则和指标策略，但不会自动解决几千只股票的横截面中性化、风险模型和组合优化。当前向量化引擎更适合快速研究TopK和滚动预测。未来进入纸面交易和订单生命周期仿真时，可以把冻结信号接入Backtrader或RQAlpha，而不是为了框架名称重写研究层。

### 34.8 “均值—方差是业界方法，为什么反而更差？”

> 方法正确不代表输入估计可靠。只有120个时间观察、50只股票、名单不断变化时，个股协方差估计噪声很大；优化器容易把估计误差当成精确信息。Ledoit–Wolf只能缓解，不能消除。留出期明显失败后，我保留实现但关闭默认开关，选择参数更少的等权方案。这是复杂模型不如稳健基线的典型情况。

### 34.9 “你说行业中性，为什么基准还是沪深300？”

> 当前输出端控制行业和市值信号暴露，名单层限制单行业股票数，但没有约束相对沪深300的行业权重、Beta和Barra风格暴露，因此它不是严格的沪深300增强。沪深300只是绩效参照。更严谨的指数增强需要成分权重、相对行业/风格约束和跟踪误差目标。

### 34.10 “回测最薄弱的地方是什么？”

> 当前最薄弱的是交易和风险口径：虽然开盘涨跌停未来信息已经修复，但仍需完整未复权价格/公司行动账本、成交量参与率、滑点和冲击成本；行业历史PIT和指数成分PIT也不完整。其次测试期较短，长期稳定性证据不足。

---

## 35. 当前结果的标准对外表述

### 35.1 30秒项目介绍（更新版）

> 我搭建了一个A股日频机器学习选股管线，处理约1043万行股票—交易日数据和270个量价、基本面及Alpha158因子，通过公告日PIT、历史ST、横截面中性化和20日purge控制未来信息。统一比较Ridge、LightGBM和PyTorch MLP后，选择IC和多空收益转化更好的LightGBM。组合采用Top50、缓冲区、行业上限和15%单次换手预算，并按T+1开盘成交。修正成交时序错误后，验证集年化超额3.75%，约1.5年的滚动样本外测试年化超额9.59%、Sharpe 1.46，但我会明确说明测试较短、模型滚动再训练且尚未加入冲击成本。

### 35.2 简历结果表述

> 构建A股日频多因子选股系统，处理1043万级股票—交易日样本与270个候选因子，采用PIT财务对齐、Purged Walk-Forward及行业/市值中性化防止信息泄漏；统一对比Ridge、LightGBM和PyTorch MLP，LightGBM验证期IC 0.093、RankIC 0.097并取得最高多空收益；实现Top50缓冲换仓、行业上限、涨跌停、交易成本及换手约束回测，修正成交时序后滚动样本外测试年化21.7%、超额9.6%、Sharpe 1.46、最大回撤13.7%，同时保留审计锁与纠错记录。

建议口头补充：测试只有383个交易日，不能把21.7%视作长期预期。

### 35.3 不允许使用的宣传性表述

- “稳定年化21.7%”；
- “实盘Sharpe 1.46”；
- “完全消除未来函数”；
- “严格沪深300指数增强”；
- “模型在所有市场状态都有效”；
- “深度学习不如LightGBM”——这里只能说当前MLP配置和数据表示下不如LightGBM；
- “风险优化无效”——这里只能说当前估计窗口和约束下留出期失败。

---

## 36. 代码与证据索引：面试前应能现场打开

| 面试问题 | 主要文件/产物 |
|---|---|
| 时间划分在哪里 | `config/settings.py` |
| purge如何实现 | `models/lightgbm/data.py::build_rolling_windows` |
| 标签为何不会进特征 | `models/lightgbm/data.py::get_feature_cols` |
| Ridge/MLP怎么训练 | `models/ridge/train.py`、`models/mlp/train.py`、`models/rolling.py` |
| 三模型结果 | `reports/model_comparison/valid_label_ret_20.csv` |
| 为什么选LGB | `data/processed/selected_model.json` |
| 输出中性化 | `backtest/alpha_signal.py` |
| TopK/Buffer/Dropout | `backtest/alpha_signal.py::select_with_buffer` |
| 风控和成交 | `backtest/engine.py` |
| 开盘涨跌停修复 | `backtest/run_backtest.py::_attach_open_limit_state` |
| 组合优化失败实验 | `backtest/portfolio_optimizer.py`、`reports/portfolio_tuning/` |
| 当前验证结果 | `reports/portfolio/valid/lgb/label_ret_20/summary.json` |
| 原最终测试审计 | `reports/portfolio/final_test_audit.json` |
| 纠错测试结果 | `reports/portfolio/test_corrections/open_limit_v2/lgb/label_ret_20/summary.json` |
| 纠错审计 | `reports/portfolio/test_corrections/open_limit_v2/audit.json` |
| 自动化测试 | `tests/test_time_split_and_portfolio.py`、`tests/test_additional_models.py` |

---

## 37. 从这个项目真正学到的研究原则

1. **先怀疑过好的结果。** 测试集显著好于验证集时，应先审计时序、成交和数据口径，而不是先做展示图。
2. **复杂不等于有效。** MLP的RankIC略高不代表组合更好；均值—方差理论更完整也可能因估计误差输给等权。
3. **模型和组合必须分开评价。** IC衡量全截面预测，Top50收益取决于头部信号、约束、成本和成交。
4. **失败实验也要保存。** 激进风险覆盖和协方差优化失败，帮助解释最终为何采用温和低换手等权。
5. **测试集纠错和调参必须区分。** 实现错误可以修正复算，但要保留原报告、独立审计，并禁止利用新结果重新选参数。
6. **可复现性不只靠随机种子。** 无序集合、文件顺序、浮点求解器和数据版本都可能改变结果。
7. **指标必须带口径。** RankICIR是否年化、Sharpe是否扣无风险利率、换手是单边还是双边、测试是否滚动再训练，都应明确。
8. **诚实披露限制比最好看的数字更重要。** 对初级量化岗位，能识别并修复研究错误通常比多做一个模型更有说服力。

---

## 38. 面试准备：经济思维、量化思维与市场认知

### 38.1 面试官想听的不是口号

面试官问“你有什么经济思维和量化思维”，通常不是要求背诵宏观理论，而是想确认三件事：你是否理解收益从哪里来，是否知道研究结果为什么可能失效，以及你能否把抽象判断落实成数据、模型、交易规则和风险控制。

我的核心认识是：**股票收益不是模型凭空创造出来的，而是企业基本面、投资者预期、资金流动、风险补偿和交易约束共同作用的结果。量化的作用，是把这些可能影响收益的经济假设转成可计算的变量，用严格的时间划分和交易成本检验它们是否真的具有稳定的增量信息。**

### 38.2 我理解的经济思维

#### （1）价值最终来自现金流，但价格由预期变化驱动

企业长期价值与盈利、现金流、资产质量有关，但短期股价不会机械等于基本面。市场交易的是“未来相对于当前预期的变化”。因此，本项目同时构造估值、盈利质量、成长、动量、波动、流动性和交易行为因子，而不是只依赖某一类指标。

对应到项目：质量和估值因子尝试描述企业内在价值，动量和价格结构因子描述预期和趋势，换手率、成交量和波动因子描述资金行为与风险补偿。多因子模型的目的不是堆变量，而是从不同经济来源获取尽可能互补的信息。

#### （2）风险与收益相伴，不能把高收益直接当作能力

高收益可能来自承担了小市值、低流动性、高波动、行业集中或市场下跌风险。一个策略只有在控制这些暴露后仍有稳定的相对收益，才更接近 Alpha。因此项目不仅看收益和 IC，还检查行业、市值、波动、换手、最大回撤和成本后的表现。

对应到项目：模型输出端再次做行业和市值中性化，组合层限制行业权重，并通过 TopK、Buffer、max_drop、仓位控制和回撤降仓降低非预期风险。

#### （3）市场有效但不完全有效

A股市场有大量公开信息，但投资者反应速度、资金约束、机构行为、交易制度和情绪并不完全相同，所以短期可能存在可重复的相对定价偏差。但这种偏差会被交易、拥挤和套利逐渐削弱，不能假设一个历史规律永久有效。

对应到项目：使用滚动训练而非一次性训练，保存因子失败记忆，检查不同年份和市场状态，并用验证集观察规律是否衰减。DeepSeek只能提出新假设，不能因为公式复杂就自动获得经济解释或入选资格。

#### （4）流动性是收益实现的前提

回测中的价格不等于真实可成交价格。涨跌停、停牌、成交量不足、买卖价差、冲击成本和整手交易都会影响收益能否实现。因此“纸面上有效”的信号，可能在高换手后失效。

对应到项目：采用次日开盘成交，处理涨跌停、停牌、资金、100股整手和交易费用；用低频调仓、Buffer和Dropout减少无效交易，并对成本和容量做压力测试。

### 38.3 我理解的量化思维

#### （1）先提出假设，再设计检验

不能先看到收益结果再编故事。正确顺序是：先提出经济机制，例如“低拥挤和低特质波动可能代表更稳健的风险收益结构”，再定义因子公式、预测期限、样本区间、筛选标准和交易规则，最后看数据是否支持。

#### （2）时间顺序比模型复杂度更重要

只要使用了未来信息，再复杂的模型也没有研究价值。因此项目按公告日对齐财务数据，使用未来收益生成标签，排除标签列，设置 purge/embargo，并采用滚动样本外预测。

#### （3）预测能力不等于投资收益

RankIC回答“股票排序是否正确”，但组合收益还取决于 TopK、行业暴露、换手、成交限制和成本。因此项目采用三层评价：模型预测能力、因子增量与稳定性、扣成本后的组合表现。

#### （4）基线比复杂模型更重要

Ridge 用来回答“简单线性关系是否已经足够”，LightGBM 用来捕捉非线性和因子交互，MLP 用来检验更复杂的函数形式。最终选择 LightGBM，不是因为模型越复杂越好，而是因为其验证期 IC、头尾区分和组合转化在当前数据上综合更好。

#### （5）失败结果也是信息

缺失率过高、RankIC弱、与25因子重复、成本后不赚钱的因子都应保留失败原因。失败能告诉我哪些经济逻辑在当前市场、当前期限或当前成本下不成立，避免下一轮重复试错。

### 38.4 我对股市的看法

我认为股市是一个由基本面、预期、资金、制度和情绪共同驱动的复杂适应系统。它既不是完全随机，也不是存在一个简单公式可以永久套利。量化策略的优势不在于准确预测每一天的涨跌，而在于：

- 在大量股票中寻找概率上更有利的相对排序；
- 用分散化降低单只股票判断错误的影响；
- 用规则代替临场情绪，保持决策一致；
- 用成本、容量和风险约束把理论信号变成可执行组合；
- 持续验证因子在不同市场环境下是否仍然有效。

我不会把历史回测收益理解为未来承诺，也不会把一次高 RankIC 理解为永久 Alpha。对量化研究来说，能够解释收益来源、识别隐性风险、审计未来函数和承认结果边界，和追求更高收益同样重要。

### 38.5 这些认识如何帮助我的项目

| 认识 | 项目中的落实 | 带来的帮助 |
|---|---|---|
| 收益来自不同经济来源 | 构建估值、质量、成长、动量、风险、流动性等270候选因子 | 避免只押注单一市场风格 |
| 预期会变化 | 使用滚动训练和5/10/20日期限分析 | 检查规律是否随市场状态改变 |
| 风险可能伪装成Alpha | 行业/市值中性化、回撤和暴露分析 | 区分真实增量信息与风格收益 |
| 信号必须能够交易 | TopK、Buffer、Dropout、涨跌停和成本模型 | 避免纸面收益无法实现 |
| 新因子必须有增量 | DeepSeek候选与人工25因子去重、增量IC检查 | 防止重复包装旧因子 |
| 模型不应替代经济判断 | Ridge/LightGBM/MLP统一比较和消融分析 | 防止盲目追求复杂模型 |
| 测试集应保持不可见 | 训练/验证/测试隔离和测试审计锁 | 降低数据挖掘和后见偏差 |

### 38.6 对后续工作的帮助

这些认识会影响我下一步的研究方式：先从经济机制出发，再决定是否需要新数据或新模型；先检查数据时点和交易可行性，再扩大参数搜索；先做基线、消融和稳健性分析，再谈策略升级。

后续重点包括：分市场状态和行业分析因子稳定性；增加成交量参与率、滑点和冲击成本；做不同 TopK、调仓周期和容量的有效前沿；采用嵌套滚动验证减少验证集过拟合；检验25个人工因子和5个增量因子的真实消融贡献；用 SHAP、部分依赖和因子暴露解释模型，但不把解释工具误认为因果证明；在参数冻结后使用新数据做真正的前向验证。

### 38.7 面试时的完整回答

> 我的经济思维是先问收益来源和风险来源：企业基本面决定长期价值，但短期收益还受到预期变化、资金行为、流动性和市场情绪影响；高收益也可能只是承担了行业、市值或流动性风险。我的量化思维是把这些经济假设转成可计算因子，再用严格的时间切分、PIT财务数据、purge、滚动样本外预测和交易成本回测检验。项目中先构建270个候选因子，筛选25个人工核心因子，再让DeepSeek提出增量假设，经过合法性、增量IC、冗余和10bps成本准入，最终用25+5个因子训练LightGBM。周频或低频调仓是为了在信号衰减、噪声、换手成本和执行容量之间取得平衡。最后我不会只看最高收益或最高RankIC，而会综合看跨时期稳定性、扣成本超额、Sharpe、回撤、换手和风险暴露。这些认识帮助我把项目从‘能跑出回测数字’提升为‘能解释、能审计、能交易、能持续验证’的研究系统。

## 48. 后续工作边界：测试已查看后还能做什么

可以继续做，但不能再以当前测试收益为选择依据：

- 修复明确实现错误并做带审计的纠错复算；
- 增加单元测试、数据校验和结果manifest；
- 做不改变策略的归因分析和可视化；
- 增加成本压力测试，作为风险披露而不是挑选最优成本；
- 进入新的、尚未产生的数据做纸面前向验证；
- 另起研究版本探索新模型，但必须重新定义未来不可见评估期。

不应继续做：

- 根据当前测试期重新选择TopK、调仓频率、回撤阈值或模型；
- 删除表现不好的测试月份或股票；
- 在测试期搜索模型融合权重；
- 只展示测试结果而隐藏修正后验证结果；
- 把纠错复算描述成一次全新的独立测试。

当前项目最合理的下一验证方式不是继续榨取2025—2026测试集，而是冻结`open_limit_v2`及全部模型/组合参数，等待新的真实行情进行纸面前向跟踪。

---

## 39. 因子挖掘运行索引（对应主流程第四阶段）

本节内容属于主流程第四阶段的运行说明，按候选生成、校验、筛选、比较和准入顺序组织。

因子挖掘不是“生成一个公式后看 IC”这一单步操作，而是一个有准入门槛的闭环：

```text
原始数据与算子审计
  -> 生成候选公式
  -> DSL/字段/时间边界校验
  -> 近两年 quick 粗筛
  -> 训练期全量 RankIC 与质量检查
  -> 与 core25、已准入注册表去重
  -> 10bps 周频训练期组合回测
  -> 通过后才注册为候选因子
  -> 重建挖掘记忆，指导下一轮生成
```

### B.1 候选公式从哪里来

有三条来源：

1. `factors/base/` 中的人工设计因子，按价值、质量、成长、流动性、波动、技术、量价、反转动量等类别实现；
2. `scripts/run_deepseek_factor_trial.py` 通过 DeepSeek 根据算子白名单、已有因子记忆和研究约束生成一个候选公式；
3. `vendor/quantmind` / RD-Agent 产生的 Qlib 因子假设，通过 `quantmind_integration/rdagent_hook.py` 接入本项目校验流程。

`scripts/build_factor_mining_memory.py` 汇总当前注册因子、历史候选、分类、窗口、失败原因和方向信息，输出 `reports/factor_mining_memory.json`。下一轮生成器读取这份记忆，减少重复探索，而不是盲目随机生成公式。

### B.2 算子和公式执行机制

| 文件 | 机制 |
|---|---|
| `factors/operators.py` | 早期基础时间序列算子，如收益、均线、波动、排名、相关性、斜率、衰减和滞后。主要供已有人工因子使用。 |
| `factors/operators_v2.py` | 当前更严格的安全算子层，增加财务报告滞后/差分、稳健除法、安全对数、横截面 rank/z-score、连续计数和窗口统计；统一处理缺失、无穷和窗口合法性。 |
| `quantmind_pipeline/dsl.py` | 解析候选字符串公式，识别时间序列算子和财务报告算子，在 DataFrame 上计算 Series；公式只能使用批准的字段和算子。 |
| `quantmind_pipeline/financial_reports.py` | 将财务报告快照按公告时点接入候选公式，保证“报告期”不被误当成“可见日期”。 |
| `quantmind_pipeline/industry_atoms.py` | 构造带历史区间的行业原子数据，并按有效起止日期挂接到日线。 |
| `quantmind_integration/policy.py` | 读取 `config/quantmind_candidate_policy.json`、算子映射和准入注册表，检查公式、字段、窗口、选择期和训练期边界。 |
| `quantmind_integration/sota_loader.py` | 加载最终人工 SOTA 因子清单，供候选比较和正式实验使用。 |
| `quantmind_integration/text_readiness.py` | 检查文本数据源是否具备时间戳、来源和去重信息；文本因子未满足条件时不能进入正式研究。 |

### B.3 单候选因子的真实准入逻辑

入口是 `scripts/mine_one_factor.py`。它串联以下动作：

1. 调用候选生成器取得一个公式和元数据；
2. 检查 JSON 结构、公式语法、字段白名单、算子白名单和时间窗口；
3. 在最近约两年数据上进行 quick 计算，快速淘汰常数、极端缺失、无穷、覆盖率不足和明显无效候选；
4. 对通过粗筛的候选执行训练期 5 日标签的全量评估，记录 RankIC、ICIR、t 值、覆盖率、分位数组合和换手；
5. 与 `config/human_core_25_v1.json` 及 `config/quantmind_admitted_registry.json` 比较表达式和收益序列，防止重复暴露；
6. 用 `backtest/weekly_factor_engine.py` 做含 10bps 交易成本的周频训练期回测，检查可交易性和净收益；
7. 只有所有门槛通过，才调用注册逻辑写入准入记录；失败候选保留报告和失败原因，但不能进入正式因子库。

### B.4 批量候选流水线

| 入口 | 用途 |
|---|---|
| `scripts/run_quantmind_candidate_pipeline.py` | 对候选执行训练期计算、质量校验、去重和准入流程；任何子步骤失败即停止该候选。 |
| `scripts/run_quantmind_weekly_candidate.py` | 对指定候选执行通用周频训练期回测，验证信号到组合的可实现性。 |
| `scripts/evaluate_quantmind_candidate.py` | 计算候选 IC、覆盖率、中性化后表现，并和 core25/准入注册表比较。 |
| `scripts/compare_candidate_to_core.py` | 读取候选日度结果，量化它与核心因子的相关性、增量信息和重复程度。 |
| `scripts/evaluate_rdagent_h5_candidate.py` | 评估 RD-Agent H5 候选的统一指标，属于外部候选适配入口。 |
| `analysis/audit_quantmind_candidate_sota.py` | 审计候选、正式 SOTA 清单和准入表之间的数量、来源、重复及时间边界。 |
| `analysis/compare_quantmind_liquidity_variants.py` | 比较流动性候选的不同实现，防止把同一经济含义的变体误当作独立信号。 |
| `analysis/evaluate_quantmind_experimental_factor.py` | 对实验因子做分布、相关性、IC 和质量摘要，不自动晋级。 |
| `scripts/apply_consec_fixes_to_library.py` | 将已验证的连续计数/反转动量修复应用到因子库；属于明确变更工具，运行后必须重新评估。 |
| `analysis/validate_reversal_momentum_fixes.py` | 独立重算修复前后因子，比较质量和稳定性，不覆盖正式结果。 |
| `scripts/build_industry_atoms.py`、`scripts/audit_industry_atoms.py` | 构建并审计行业历史原子数据，供行业相关候选使用。 |

### B.5 因子从候选到正式模型的两级筛选

候选准入与模型特征筛选是两件事：

```text
候选公式
  -> QuantMind 准入：合法、覆盖、IC、去重、周频回测
  -> 注册因子库
  -> build_factors 生成数值
  -> evaluate_factors 评估全体因子
  -> remove_redundant 去冗余
  -> factor_quantile 分层把关
  -> features.parquet
  -> 模型训练
```

不能因为候选在 quick 窗口表现好就直接加入模型，也不能因为某个因子入选模型就反过来宣称它已经通过独立候选准入。

---

## 40. 源文件全量索引与阅读顺序

下表覆盖当前仓库中的可执行源文件。`__init__.py` 只承担包标识或导出；`tmp_*.py` 是一次性诊断脚本；模型 `step_*`、`reports/`、`mlruns/`、`log/` 和 `pickle_cache/` 是运行产物，不是源代码运行步骤。

### C.1 因子、目录和策略契约

```text
factors/operators.py              旧版基础算子
factors/operators_v2.py           安全算子与财务时点算子
factors/registry.py               因子注册、查询、分类统计
factors/base/utils.py             因子通用清洗与滚动工具
factors/base/value.py             价值因子
factors/base/quality.py           质量因子
factors/base/growth.py            成长因子
factors/base/liquidity.py         流动性因子
factors/base/volatility.py        波动率因子
factors/base/technical.py         技术分析因子
factors/base/volume_price.py      量价因子
factors/base/price_structure.py   价格结构因子
factors/base/reversal_momentum.py 反转与动量因子
factors/base/gp_huatai_replication.py 华泰遗传规划复现因子
factor_catalog/build.py           从注册表和 Alpha158 建目录
factor_catalog/audit.py           审计目录与物理列一致性
factor_catalog/sota.py            生成/校验最终 SOTA 清单
research/protocol.py              PIT 合约、文件指纹、实验 manifest
```

### C.2 数据与文本

```text
scripts/fetch_daily.py                 Qlib 日线导出
scripts/fetch_financial.py             财务数据与 PIT 事件表
scripts/fetch_stock_basic.py           股票静态信息与历史名称
scripts/fetch_industry_history.py     历史行业数据
scripts/fetch_daily_basic_by_date.py   按交易日拉取估值/市值/涨跌停
scripts/merge_data.py                  日线与财务时点合并
scripts/clean_data.py                  样本清洗与交易状态标记
data_catalog/catalog.py                数据目录契约读取/校验
data_catalog/audit.py                  原始数据盘点
data_catalog/operator_audit.py         算子白名单审计
textdata/schema.py                     文本标准结构与规范化
textdata/rss.py                        RSS 解析与抓取
textdata/json_source.py                JSON 源解析与抓取
textdata/tushare_source.py             Tushare 文本转文档
textdata/storage.py                    文档哈希去重与落盘
textdata/collector.py                  按配置调度全部文本源
```

### C.3 分析、预处理、模型与回测

```text
preprocessing/preprocess_factors.py    正式特征矩阵
preprocessing/neutralize.py            独立中性化实验
preprocessing/build_features.py        旧版特征流程
analysis/evaluate_factors.py           当前因子 RankIC 评估
analysis/factor_evaluation.py          旧版评估实现
analysis/remove_redundant_factors.py  相关性去冗余
analysis/factor_quantile.py             五分组分层
analysis/factor_extra_checks.py        单调性/换手/相关性检查
analysis/select_stable_factors_v2.py  稳定性与衰减筛选
analysis/replicate_gp_huatai.py       华泰 GP 因子复现审计
analysis/compare_labels.py             5/10/20日标签对比
analysis/compare_models.py             模型公平比较
analysis/compare_rebalance_frequency.py 调仓频率比较
analysis/diagnose_signal_to_portfolio.py 信号到组合诊断
analysis/tune_portfolio.py             验证集组合参数调优
analysis/visualize_factors.py          因子图表
models/rolling.py                      共享滚动窗口/样本/预测收尾
models/lightgbm/data.py                LightGBM 输入和标签
models/lightgbm/config.py              LightGBM 配置
models/lightgbm/optuna_search.py      V1 超参搜索
models/lightgbm/optuna_search_v2.py   多折 Purged 搜索
models/lightgbm/train.py               LightGBM 滚动训练
models/lightgbm/evaluate.py            预测指标评估
models/ridge/train.py                  Ridge 滚动基线
models/mlp/train.py                    MLP 滚动训练
models/baselines/train.py              等权/IC加权基线
backtest/config.py                     回测参数
backtest/alpha_signal.py               信号整理
backtest/portfolio_optimizer.py       组合约束/优化
backtest/engine.py                     逐日状态推进
backtest/weekly_factor_engine.py       因子周频回测
backtest/run_backtest.py               正式组合回测入口
backtest/run_test_correction.py        测试纠错复算入口
```

### C.4 脚本编排、基线和测试

```text
scripts/run_deepseek_factor_trial.py  DeepSeek 候选生成
scripts/mine_one_factor.py             单候选准入闭环
scripts/build_factor_mining_memory.py  挖掘记忆重建
scripts/run_quantmind_candidate_pipeline.py 候选批处理
scripts/run_quantmind_weekly_candidate.py 周频候选回测
scripts/evaluate_quantmind_candidate.py 候选评估
scripts/compare_candidate_to_core.py   候选与核心因子比较
scripts/evaluate_rdagent_h5_candidate.py RD-Agent候选评估
scripts/run_model_comparison.py        多模型串行比较
scripts/lgb_baseline.py                Qlib独立基线
scripts/backtest_walkforward.py        独立滚动回测脚本
scripts/verify_daily.py                日线完整性检查
scripts/smoke_test_factors.py          因子冒烟测试
scripts/smoke_test_rdagent_hook.py     RD-Agent接口冒烟测试
scripts/preflight_qlib_operator_mapping.py Qlib算子预检
scripts/visualize_baseline.py          基线结果可视化
scripts/run_lgbm_human25_qm5_weekly.py 人工25+QuantMind5周频实验
scripts/run_lgbm_human25_qm5_contaminated_test.py 污染测试审计实验
scripts/run_lgbm_topkdropout_50_30_10.py TopK/Dropout实验
tests/                                  全部回归测试，按模块对应运行
```

旧实现 `preprocessing/build_features.py`、`analysis/factor_evaluation.py` 仍保留用于历史复现，但不应和当前 `preprocess_factors.py`、`evaluate_factors.py` 混用。真正执行前先运行 `python main.py list`，因为只有注册到 `main.py` 的阶段才能被统一入口调用。

---

## 41. 数据准备详解（面试版，对应主流程第一阶段）

### E.1 先用一句话理解数据准备

数据准备不是“把数据下载下来”，而是把行情、财务、股票状态、估值和行业信息，按照“当时真正能知道、当时真正能交易”的原则，整理成后续因子计算的统一股票—交易日面板。

面试时可以先这样回答：

> 我先从 Qlib 获取复权日线，从 Tushare 获取财务报表、财务指标、历史名称、每日估值和行业信息；然后以股票和交易日为主键合并，用公告日做 Point-in-Time 对齐，过滤指数、历史 ST、次新股和重复记录，保留停牌缺失，并标记涨跌停和一字板，最后形成因子计算底表。

### E.2 数据准备涉及的文件

| 文件 | 作用 |
|---|---|
| `main.py` | 统一调度数据准备阶段 |
| `config/settings.py` | 集中管理路径、日期、Tushare token 和财务配置 |
| `data_catalog/catalog.py` | 定义数据目录契约 |
| `data_catalog/audit.py` | 审计原始数据文件、日期和股票覆盖 |
| `scripts/fetch_daily.py` | 从 Qlib 导出日线行情 |
| `scripts/fetch_financial.py` | 拉取财务报表、财务指标并生成 PIT 事件表 |
| `scripts/merge_data.py` | 合并日线和财务事件表 |
| `scripts/fetch_stock_basic.py` | 拉取上市日期和历史名称变更 |
| `scripts/clean_data.py` | 清洗股票池、保留缺失、标记涨跌停 |
| `scripts/fetch_daily_basic_by_date.py` | 拉取每日估值、市值、换手和官方涨跌幅 |
| `scripts/verify_daily.py` | 检查日线完整性 |
| `analysis/check_fin_coverage.py` | 检查财务字段覆盖率 |
| `research/protocol.py` | 记录时点契约、文件指纹和实验 manifest |

### E.3 第一步：获取日线行情

文件：`scripts/fetch_daily.py`；配置：`config/settings.py`。

数据来自本地 Qlib，按股票读取日频 OHLCV 数据，字段包括 `open`、`high`、`low`、`close`、`volume`、`amount`、`vwap`、`factor` 和 `adjclose`。代码会按沪市、深市、北交所分组，再按批次读取，统一成 `symbol/date` 主键，输出到 `data/raw/daily/`。

经济含义：价格描述收益和价格行为，成交量与成交额描述交易活跃度和流动性，VWAP描述平均成交价格，复权字段用于消除分红拆股造成的非经济价格跳变。面试官追问“为什么用复权价”时，可以回答：因子研究需要连续收益；但执行和交易约束仍要单独考虑真实开盘价、停牌和涨跌停，不能把复权价直接等同于成交价。

### E.4 第二步：获取财务数据

文件：`scripts/fetch_financial.py`；配置：`config/settings.py`。

项目从 Tushare 获取四类数据：利润表 `income`、资产负债表 `balancesheet`、现金流量表 `cashflow`、财务指标 `fina_indicator`。原始数据保存到 `data/raw/financial/`。脚本支持逐股票拉取、失败重试、指数退避、定期 checkpoint、断点续传和按股票/报告期/公告日去重。

经济含义：利润表反映盈利能力，资产负债表反映规模、杠杆和偿债能力，现金流量表反映利润质量和现金创造能力，财务指标表把这些原始项目转成 ROE、ROA、ROIC、利润率、成长率、周转率等可用于因子研究的指标。

### E.5 最重要的财务处理：公告日对齐

文件：`scripts/fetch_financial.py` 中的 `build_financial_events()`；输出：`data/processed/financial/financial_events.parquet` 和 `report_period_index.parquet`。

财务表有两个日期：`end_date` 是报告描述的期间结束日，`ann_date` 是公司真正向市场公告的日期。模型只能在公告后使用这份信息。因此项目使用 `ann_date` 建立 Point-in-Time 事件表，排除公告日缺失、报告期缺失、报告期晚于公告日、同日重复和迟到的旧报告修订。

面试标准回答：

> 不能按报告期末对齐，因为报告期末时市场可能还不知道这份财报。按公告日对齐可以避免把未来信息提前带入历史样本，是防止财务未来函数的核心措施。

### E.6 第三步：合并行情和财务

文件：`scripts/merge_data.py`；输入：日线 Parquet 和 `financial_events.parquet`；输出：`data/processed/merged_data.parquet`。

代码使用按股票分组的 `merge_asof(direction="backward")`：对每个股票的每个交易日，只寻找“公告日小于等于当前交易日”的最近财务事件。日线停牌缺失保持 NaN，不把停牌价格前向填充。

为什么停牌不填充：如果把停牌日价格填成上一日价格，就会产生虚假的零收益，复牌跳空会被错误摊平，也会污染波动、流动性和未来收益标签。这里体现的是“缺失值有经济含义”，缺失不等于 0。

### E.7 第四步：获取上市日期和历史名称

文件：`scripts/fetch_stock_basic.py`；输出：`stock_basic.parquet` 和 `namechange.parquet`。

项目获取股票上市日期、历史名称区间、当前名称和当前 ST 诊断状态。上市日期优先使用 Tushare `namechange` 的最早生效日期，Qlib instruments 作为兜底。

历史名称采用 `[start_date, end_date)` 区间。这样 `clean_data.py` 可以对每个历史交易日判断当时的名称是否含 ST 或退市标记，而不是拿今天的名称回看过去。经济逻辑是 ST 状态会变化，当前状态不能代表历史状态。

### E.8 第五步：基础清洗

文件：`scripts/clean_data.py`；输入：`merged_data.parquet`、`stock_basic.parquet`、`namechange.parquet`；输出：`data/processed/basic_cleaned.parquet`。

实际处理顺序是：删除 `(symbol,date)` 重复记录，剔除指数，按历史名称过滤 ST/退市股，过滤上市不足 60 天的新股，保留停牌 OHLCV 的 NaN，对财务字段按股票过去值前向填充，最后标记涨跌停和一字板。

这些规则背后的经济思想是构造“可投资股票池”：指数不是个股，ST股票存在交易和风险制度差异，新股历史太短且价格发现过程特殊，停牌股票不能假设能正常交易。

财务字段可以按过去值前向填充，是因为财务信息低频发布；但只能延续已经公告的信息，不能用未来值填充，也不能把完全没有财务记录的股票填成 0。

### E.9 第六步：涨跌停和一字板

仍由 `scripts/clean_data.py` 完成，生成 `limit_up`、`limit_down`、`lock_limit_up`、`lock_limit_down`。代码按股票板块和历史日期区分主板、科创板、创业板和北交所的涨跌停幅度，再用前收和当日 OHLC 判断是否封板。

经济逻辑：涨停股票可能买不进去，跌停股票可能卖不出去。一字涨停和一字跌停的可交易性更差。如果回测只看收盘收益而不处理这些状态，结果会偏乐观。

### E.10 第七步：每日估值、规模、行业和官方涨跌幅

文件：`scripts/fetch_daily_basic_by_date.py`；输入：`basic_cleaned.parquet`；输出：`daily_basic_raw.parquet`、`daily_basic_ckpt.json` 和 `basic_cleaned_with_extra_by_date.parquet`。

获取字段包括 `total_mv`、`circ_mv`、`pe_ttm`、`pb`、`ps_ttm`、`turnover_rate`、`industry` 和 `pct_chg`。脚本按交易日获取全市场数据，有交易日历、重试、指数退避、checkpoint 和定期落盘机制。

`pct_chg` 单独通过 Tushare `daily` 接口获取，因为 `daily_basic` 的该字段可能受积分权限影响。估值缺失当前填 0，但 `pct_chg` 缺失保持 NaN；因为缺失涨跌幅不能被解释为平盘。

经济含义：市值和流通市值提供规模与容量信息，估值字段提供价值信息，换手率提供交易拥挤和流动性信息，行业字段支持行业中性化，官方涨跌幅支持更可靠的交易状态判断。

### E.11 数据准备最终产物

数据准备最终应形成：`data/processed/basic_cleaned_with_extra_by_date.parquet`。它由以下链路得到：日线原始文件加财务原始文件，生成 PIT 财务事件表；行情与事件表合并为 `merged_data.parquet`；结合股票基础信息清洗为 `basic_cleaned.parquet`；再合并每日估值、行业和官方涨跌幅形成增强底表。

这张增强底表才是后续 `scripts/build_factors.py` 构建 270 因子库的正式输入。

### E.12 面试官可能追问的问题

| 追问 | 回答重点 |
|---|---|
| 为什么不用当前股票名称过滤历史 ST？ | 会把历史状态误判为当前状态，产生时间穿越；使用历史名称区间。 |
| 为什么财务数据按公告日？ | 报告期末不等于市场可见日，公告日才能防未来函数。 |
| 为什么停牌日不填充？ | 填充会制造零收益，掩盖复牌跳空并污染标签。 |
| 为什么要获取官方 `pct_chg`？ | 复权价格适合研究收益，但涨跌停交易判断需要官方涨跌幅口径。 |
| 为什么过滤新股？ | 新股历史不足、价格发现和涨跌停制度特殊，因子统计不稳定。 |
| 为什么要保存原始层和处理层？ | 原始层可追溯，处理层可重建，出现错误时不必重新下载全部数据。 |

### E.13 当前不足与优化方向

1. `fetch_daily.py` 已导出北交所文件，但 `merge_data.py` 当前只合并沪深，需要明确是否纳入北交所并统一交易规则。
2. 上市不足 60 天当前按自然日近似，建议改为按交易日历计算。
3. 财务 FFill 建议增加最大有效期限，避免多年没有新公告时旧值无限延续。
4. daily_basic 最终失败日期应自动重试并输出缺失清单，不能把跳过当成完整。
5. 应补充价格关系、成交量非负、主键唯一、公告时点和复权一致性检查。
6. 应保存下载时间、接口参数、字段版本、行数、覆盖率和文件哈希，形成可复现实验 manifest。
7. 行业分类最好使用带生效日期的历史行业数据，避免用当前行业回看历史。
8. 回测阶段还应增加滑点、冲击成本、成交量参与率和涨跌停排队机制。

### E.14 数据准备阶段的验收标准

进入 270 因子构建前，必须确认：行情覆盖已审计，财务事件表存在且公告日合法，`(symbol,date)` 无重复，历史 ST 状态可追溯，新股过滤口径明确，停牌价格没有伪造填充，daily_basic 失败日期有清单，行业/市值/估值/涨跌幅覆盖率已报告，并且训练、验证、测试时间边界没有被数据处理步骤混用。

---

## 42. 本项目最终实验分类与标准顺序

本项目的实验分为一条主流程和一个额外分析分支。主流程负责从数据生成最终可交易组合；额外分析不改变主流程结论，只用于比较模型、预测周期和研究稳健性。

### D.1 主流程总览

```text
一、数据准备
  -> 二、270因子库构建
  -> 三、25个人工因子筛选
  -> 四、DeepSeek候选因子挖掘
  -> 五、30因子LightGBM组合
  -> 六、周频TopK/Dropout调仓回测
```

其中“30因子”固定指最终模型输入：25 个人工核心因子，加上 5 个通过准入、去重和验证的 QuantMind/DeepSeek 增量因子。30 因子的最终身份以实验 manifest、因子清单和 `features.parquet` 实际特征列共同确认。

### D.2 第一阶段：数据准备

目标是构造一份按交易日、股票和可见时间对齐的基础数据集。

```text
Qlib日线
 + Tushare财务报表
 + 股票基础信息/历史名称
 + daily_basic/行业/市值/涨跌停
       ↓
basic_cleaned_with_extra_by_date.parquet
```

标准顺序：

```powershell
python main.py fetch_daily
python main.py fetch_financial
python main.py merge_data
python main.py fetch_stock_basic
python main.py clean_data
python main.py fetch_daily_basic
```

这一阶段还包括 `audit_raw_data`、`verify_daily.py` 和财务覆盖检查。数据准备没有完成时，后续因子数值、IC 和回测结果都没有解释意义。

### D.3 第二阶段：270因子库构建

270 因子由两部分构成：

```text
自研注册因子
  + Qlib Alpha158
  = factors.parquet 中的候选因子集合
```

正确顺序是先生成 Alpha158，再构建合并总表：

```powershell
python main.py build_alpha158
python main.py build_factors
python main.py build_factor_catalog
python main.py audit_factor_catalog
```

这里的“270”是研究时的候选数量，不代表 270 个因子最终全部进入模型。每个因子还必须经过缺失率、有效样本、未来函数和时间可用性检查。

### D.4 第三阶段：25个人工因子筛选

这一步是人工研究因子从候选库进入核心因子集合的过程，不是简单地从 270 列中随机取 25 列。

```text
270候选因子
  -> 训练期RankIC/ICIR评估
  -> 因子经济含义和时间契约检查
  -> 因子间相关性去冗余
  -> 五分组单调性与多空检验
  -> 稳定性、换手和中性化检查
  -> 人工确定 human_core_25_v1
```

推荐命令：

```powershell
python main.py evaluate_factors
python main.py remove_redundant
python main.py factor_quantile
python main.py build_final_sota
```

这里的核心产物是：

```text
config/human_core_25_v1.json
config/human_final_sota_v1.json
data/processed/selected_factor_cols.json
data/processed/passed_factor_cols.json
```

完成 25 个人工核心因子确定后，才进入下一阶段的 DeepSeek 增量因子挖掘。DeepSeek 不参与 270→25 的人工核心筛选，也不能修改 `human_core_25_v1.json`；它以这 25 个因子作为去重基准，在其之外寻找新的经济逻辑。随后通过准入的候选，才有资格与人工 25 因子组成下一阶段的约 30 因子 LightGBM 输入。

### D.5 第四阶段：DeepSeek挖掘新因子（25因子确定之后）

DeepSeek 阶段是增量因子研究，不负责重建 270 因子库，也不替代人工核心 25 因子。

```text
核心25因子和历史实验记忆
  -> DeepSeek生成一个新公式
  -> DSL/字段/算子/时间约束校验
  -> 近两年quick筛选
  -> 训练期label_ret_5评估
  -> 与核心25及已准入QM因子去重
  -> 10bps训练期周频回测
  -> 通过者进入准入注册表
```

标准入口：

```powershell
python main.py qm_build_mining_memory
python main.py qm_mine_one_factor
```

注意：DeepSeek 生成的候选只有通过本地评估和周频准入，才可以作为人工 25 因子之外的增量因子。它不能直接写入最终 SOTA 清单，也不能读取或使用最终测试期来决定是否录用。

#### D.5.1 挖掘因子的入选标准、处理流程与比较方法（主流程口径）

这一环节的核心不是“让 DeepSeek 生成一个公式，然后直接加入模型”，而是一个**候选生成→合法性检查→统计筛选→经济逻辑检查→去重比较→交易回测→注册冻结**的准入漏斗。DeepSeek 只负责提出研究假设，最终是否入选由本地数据、统一标签、统一交易成本和统一回测规则决定。

涉及的主要文件如下：

| 环节 | 主要文件 | 作用 |
|---|---|---|
| 挖掘记忆 | `scripts/build_factor_mining_memory.py`、`reports/factor_mining_memory.json` | 汇总历史候选、失败原因、已准入因子和禁用方向，避免重复试错 |
| 候选生成 | `scripts/run_deepseek_factor_trial.py`、`.env.deepseek` | 调用 DeepSeek API，按提示词生成一个结构化因子公式及经济解释 |
| 单因子总入口 | `scripts/mine_one_factor.py`、`main.py` | 串联筛选、比较、回测和注册，不是只执行模型训练 |
| 因子计算 | `quantmind_integration/`、`features/`、`config/quantmind_candidate_policy.json` | 解析公式、检查字段和时间约束、计算候选因子 |
| 训练期评估 | `models/`、`reports/quantmind_trials/` | 以 `label_ret_5` 为主标签，计算 RankIC、增量IC、缺失率等 |
| 周频准入 | `quantmind_integration/`、`backtest/engine.py` | 在 10bps 成本下检查收益、Sharpe、超额和回撤 |
| 结果注册 | `config/quantmind_admitted_registry.json`、`pipeline_manifest_v2.json` | 记录通过者、公式、方向、指标和版本，供最终30因子模型冻结使用 |

**第一关：公式和数据可用性。** 候选必须输出合法 JSON，名称通常使用 `QM_` 前缀；字段必须来自项目白名单，算子必须属于项目 DSL，窗口长度必须有效，且只能使用当时已经公开的信息。财务数据必须遵守 point-in-time 原则，不能把未来修订值或未来收益泄漏到当前特征中。因子还要满足 `after_close -> next trade day` 的时间契约：收盘后形成信号，最早在下一交易日执行。

**第二关：数据质量和预测能力。** 以 5 日未来收益 `label_ret_5` 作为主评估目标，候选需要满足：最大缺失率不超过 **15%**；训练期绝对 RankIC 均值至少 **0.01**；相对于人工25因子的增量绝对 RankIC 至少 **0.005**。这里的“绝对”是为了同时保留正向和反向预测因子，实际进入模型前还会记录并固定 `learned_direction`，不能事后随意翻转方向。

**第三关：与已有因子去重。** 候选不能只是人工25因子的改写、同义变换或高度相关版本。当前规则以日度 RankIC 序列做比较，候选与人工25及已准入 QM 因子的绝对相关系数达到 **0.70** 即视为冗余风险；同时还检查公式是否完全重复、字段/概念是否重复以及经济类别是否过度集中。注意：这里的 ICIR 阈值是前面 270 因子库筛选的口径，DeepSeek 增量准入的主要硬门槛是增量 RankIC、冗余相关性和交易回测门槛，不能把两套规则混为一谈。

**第四关：经济解释和研究方向。** 每个候选都必须回答“它反映了什么经济行为、为什么可能预测未来5日收益、为什么与25因子不同”。例如，低换手/流动性放大可能反映交易拥挤和价格冲击；特质波动率可能反映个股风险补偿或信息不确定性；质量与低拥挤、低波动的组合可能反映稳健经营和风险偏好切换。历史记忆中已被证明无效的方向会降低或关闭继续生成的优先级；短窗口、高换手、依赖噪声字段的公式即使训练期 IC 看起来不错，也可能在成本后回测中被淘汰。

**第五关：候选与基准的比较。** 每个候选都要同时和三类对象比较，而不是只看自己的收益：

| 比较对象 | 主要比较信息 | 目的 |
|---|---|---|
| 人工25因子基线 | RankIC、增量RankIC、IC稳定性、因子方向、缺失率 | 判断候选是否提供了25因子没有的新信息 |
| 已准入QM因子 | 日度IC相关性、概念类别、重叠度、换手率 | 防止多个 DeepSeek 因子只是同一个交易逻辑的重复 |
| 25因子模型/30因子模型 | 验证期预测RankIC、Top/Bottom收益、净收益、年化超额、Sharpe、最大回撤、换手率 | 判断候选加入 LightGBM 后是否真正改善组合，而不是只改善单因子指标 |

**第六关：交易层准入。** 只有前面条件通过，才运行周频 TopK/Dropout 组合，并按统一 **10bps** 成本计算净绩效。当前准入门槛为：净 Sharpe 不低于 **0.5**、年化净超额不低于 **0**、最大回撤绝对值不超过 **0.5**。年化超额以沪深300（`SH000300`）为基准，定义为策略年化收益减去基准年化收益。未通过者会记录具体失败原因，例如 `weak_train_rank_ic`、`weak_incremental_ic`、`rejected_excessive_missing_rate`、`duplicate_core25` 或 `weekly_failed`，并写回挖掘记忆，供下一轮提示词避开。

**第七关：注册和最终冻结。** 通过者写入 `quantmind_admitted_registry.json`，同时保留公式、方向、来源、评估区间、成本、Sharpe、年化超额、回撤和换手率。注册不等于立刻进入测试集；在最终30因子模型中，应从准入候选中冻结5个增量因子，与人工25因子合并，形成唯一的 `25 + 5 = 30` 输入。冻结后不得根据测试期结果反向挑因子，否则会产生测试集选择偏差。

因此，面试时可以概括为：**DeepSeek 提出因子，代码负责可计算性和防泄漏检查，统计规则负责预测能力，相关性负责增量性，10bps 周频回测负责可交易性，注册表负责版本和可复现性；只有完整通过这条链路的候选，才有资格进入最终30因子 LightGBM。**

### D.6 第五阶段：最终30因子LightGBM模型

该阶段的“组合”是模型特征组合，不是股票投资组合。它把 25 个人工核心因子和 5 个通过筛选的增量因子放入同一个最终 30 因子 LightGBM 横截面预测器。

```text
human_core_25
  + 通过DeepSeek/QuantMind准入的增量因子
  -> 30个最终特征
  -> features.parquet / labels.parquet
  -> LightGBM Purged Walk-Forward训练
  -> 每个滚动窗口生成预测分数
```

标准顺序：

```powershell
python main.py preprocess_factors
python main.py optuna_search --label label_ret_5
python main.py train_lgb --label label_ret_5 --eval-segments valid
```

这里必须记录实际使用的因子清单、标签、训练窗口、purge/embargo、模型参数和预测文件。若因子数量不是 30，应以实际 manifest 为准，并说明是 25、30、58 还是其他版本。

### D.7 第六阶段：周频TopK/Dropout调仓回测

本阶段的经济逻辑、参数含义、交易约束、面试回答和优化方向统一见第9.5节；本处只保留主流程入口，避免重复维护两套说明。

这一阶段才把 LightGBM 的预测分数转成股票组合。

```text
LightGBM预测分数
  -> T日收盘排序
  -> TopK选股
  -> Buffer/Dropout减少无效换手
  -> 行业、涨跌停、停牌和整手约束
  -> T+1日开盘执行
  -> 周频调仓
  -> 扣除交易成本后统计净值和风险
```

标准命令：

```powershell
python main.py tune_portfolio
python main.py portfolio_backtest --model lgb --label label_ret_5 --segment valid
python scripts/run_lgbm_topkdropout_50_30_10.py
```

`run_lgbm_topkdropout_50_30_10.py` 是 TopK、持仓缓冲和 Dropout 方案的实验脚本；`backtest/run_backtest.py` 是正式组合回测入口。两者不能混称：前者比较策略参数，后者负责正式、可审计的组合回测。

### D.8 额外分析分支一：Ridge / LightGBM / MLP 模型对比

你所说的“LM”按当前仓库代码应统一写作 `MLP`，即多层感知机。额外模型比较不改变主流程的 30 因子定义，原则上复用相同的特征、标签、时间窗口和验证集。

```text
同一 features/labels
  ├─ 等权基线
  ├─ IC加权基线
  ├─ Ridge
  ├─ LightGBM
  └─ MLP
        ↓
同一验证集比较 RankIC、ICIR、分层收益和稳定性
```

```powershell
python main.py train_ridge --label label_ret_5 --eval-segments valid
python main.py train_lgb --label label_ret_5 --eval-segments valid
python main.py train_mlp --label label_ret_5 --eval-segments valid
python main.py compare_models --label label_ret_5 --segment valid
```

该分支回答的是“哪种模型更适合当前特征和时间结构”，不是重新挖掘因子。模型对比完成后，只有选定模型才进入正式组合回测。

### D.9 额外分析分支二：5日/10日/20日未来收益预测对比

这是预测目标周期对比，分别对应：

```text
label_ret_5  -> 未来5个交易日收益
label_ret_10 -> 未来10个交易日收益
label_ret_20 -> 未来20个交易日收益
```

正确做法是每个标签分别完成：

```text
标签生成
  -> 因子评估
  -> 特征/标签对齐
  -> 模型训练
  -> 验证集预测
  -> IC、ICIR、分层和稳定性比较
```

```powershell
python main.py train_lgb --label label_ret_5 --eval-segments valid
python main.py train_lgb --label label_ret_10 --eval-segments valid
python main.py train_lgb --label label_ret_20 --eval-segments valid
python analysis/compare_labels.py --seg valid
```

标签周期比较属于额外研究，不能把 5 日模型的预测文件直接当成 10 日或 20 日模型使用。若最终采用周频调仓，`label_ret_5` 是最自然的主标签；10 日和 20 日结果主要用于研究周期敏感性、稳定性和替代方案。

#### D.8.1 三模型为什么最后选择 LightGBM

三模型对比的目的不是证明“越复杂越好”，而是判断当前30个结构化因子更适合哪种函数形式：

| 模型 | 经济/统计含义 | 优点 | 局限 |
|---|---|---|---|
| Ridge | 假设因子与未来收益大体是线性、可加的 | 稳定、快速、系数容易解释，是可靠基线 | 难以捕捉阈值、非线性和因子交互 |
| LightGBM | 用树分裂学习非线性和因子之间的条件关系 | 适合表格型因子数据，能识别“某因子在特定状态下才有效” | 参数较多，可能过拟合，特征重要性不等于因果贡献 |
| MLP | 用神经网络学习更平滑的复杂非线性 | 表达能力强，理论上能学习高阶组合关系 | 对样本、归一化、随机种子和超参数更敏感，解释和稳定性较弱 |

公平比较必须固定相同的30个因子、标签、股票池、训练验证区间、purge规则和组合规则。当前验证结果显示，MLP 的 RankIC 略高，但差距很小；LightGBM 的 Pearson IC、Top-Bottom收益和实际组合转化更好；Ridge 的 RankICIR较高，说明线性关系稳定，但排序和头部收益不如 LightGBM。因此最终选择 LightGBM，不是因为它每个指标都第一，而是因为它在“非线性表达、头尾区分、组合转化和工程稳定性”之间取得了更好的平衡。

面试时可以这样回答：**Ridge 是可解释的线性基准，用来判断因子是否本身有效；MLP 是高复杂度非线性对照，用来检查是否存在更复杂关系；LightGBM 在结构化因子数据上的验证IC和多空区分更好，且训练稳定、成本低于MLP，预测优势能转化为扣成本组合表现，所以被选为主模型。**

#### D.8.2 5日、10日、20日标签为什么最后以5日为主

三个标签代表三种不同的投资持有假设，而不是同一问题的三个写法：

```text
label_ret_5  = 未来5个交易日收益
label_ret_10 = 未来10个交易日收益
label_ret_20 = 未来20个交易日收益
```

最终以5日为主，主要有四个原因：

1. **与研究目标一致**：人工25因子筛选和 DeepSeek 增量因子准入都以 `label_ret_5` 为主要口径，继续使用5日可以保持因子、模型和组合的一致性。
2. **与周频信号逻辑更接近**：因子每天更新，但交易不是每天完全换仓。5日标签能描述较短期的相对强弱，又不会像1日收益那样被噪声和交易价格影响过度。
3. **避免目标与调仓严重错位**：如果用20日收益训练，却频繁按短周期换仓，模型预测的持有期限和实际交易期限不一致；5日标签更适合解释短周期周频信号。
4. **信息更新和成本之间折中**：5日能较快响应新信息，10日和20日更平滑但可能稀释短期信号，并且重叠标签带来更强自相关。最终仍要以验证集和扣成本组合结果确认，不能只凭直觉。

这不是说10日、20日一定更差。它们保留为敏感性分析，用来回答“因子是否只在某一个预测期限有效”“长期标签是否更稳定”“较低换手是否能抵消信号变弱”。每个标签都必须重新生成训练样本、重新设置 purge、重新训练和重新评估，不能复用5日预测文件。当前模型实际默认值仍可能是20日，因此最终报告必须以实际运行的标签和 manifest 为准，不能仅凭文档口头宣称。

#### D.8.3 还应该做哪些额外分析

除模型和标签周期外，建议把分析分成四类：

| 分析方向 | 要回答的问题 | 主要指标 |
|---|---|---|
| 稳定性分析 | 模型是否只在某几年有效 | 分年度/季度RankIC、正IC比例、滚动ICIR、不同市场状态表现 |
| 增量分析 | DeepSeek因子是否真的贡献新信息 | 25因子模型 vs 25+5模型的IC、Top组收益、净超额、消融结果 |
| 交易分析 | 预测优势是否能扣成本实现 | TopK、调仓频率、Buffer、换手、10/20/30bps成本压力测试 |
| 风险归因 | 收益来自Alpha还是隐藏风格暴露 | 行业、市值、价值、动量、波动率、流动性暴露和回撤期间表现 |

还可以做模型消融：只用人工25因子、只用5个增量因子、25+5完整模型；做特征重要性和 SHAP 解释，但必须说明它们是模型使用证据，不是经济因果证明；做不同 TopK 和持有期的有效前沿；做不同随机种子、不同训练窗口和不同成本假设的稳健性检验；增加 Qlib Alpha158 或简单等权/IC加权策略作为外部基线。

#### D.8.4 当前结论的边界与后续改进

当前“选 LightGBM、主用5日”是基于统一验证框架下的综合判断，不应表述为在所有市场和所有期限都最优。后续可以：

- 统一三模型的样本上限、封板过滤和早停指标，进一步提高比较公平性；
- 增加多随机种子和多窗口重复实验，报告均值、标准差和最差窗口；
- 用排序损失或加权损失，使训练目标更贴近最终 RankIC 和 TopK；
- 对5/10/20日标签分别采用匹配的持有期和调仓频率，避免目标错位；
- 用嵌套滚动验证选择模型和标签，减少验证集反复试验造成的选择偏差；
- 把成本、容量、行业暴露和极端行情表现纳入最终决策，而不是只追求最高IC；
- 在全局测试集冻结后只做一次最终报告，并将未来新增数据作为真正的前向验证。

因此，额外分析的核心经济思想是：**模型要能解释因子关系，标签要匹配投资持有期限，预测优势要能转化为扣成本后的组合收益，并且这种优势要跨时间、跨市场状态和跨成本假设保持稳定。**

### D.10 最终版分类结论

因此，你的实验框架是完整的，但应写成下面这个更严格的版本：

```text
主流程：
数据准备
→ 270候选因子库构建
→ 25个人工因子筛选
→ DeepSeek增量因子挖掘与准入
→ 约30因子LightGBM预测
→ 周频TopK/Dropout组合回测

额外分析：
1. Ridge / LightGBM / MLP 模型对比
2. 5日 / 10日 / 20日未来收益标签对比
3. 因子稳定性、换手、调仓频率、信号到组合诊断
4. 独立Qlib Alpha158基线和华泰GP因子复现
```

其中最后一组是辅助研究，不应被误写成主流程步骤。最终测试集只能在主流程的因子、模型和组合参数全部冻结后执行一次。

---

## 43. LightGBM运行机制与超参数优化索引（对应主流程第五阶段）

详细内容已前移至正文第 8 章 8.7—8.13。本节只保留补充索引，避免与正文形成重复口径。

### I.1 模型在项目中的任务

LightGBM 的任务不是直接预测某只股票的精确价格，而是在每个交易日对股票进行横截面排序：哪些股票未来一段时间相对更强。输入是经过筛选的约 30 个因子，输出是 `pred` 分数，随后由组合模块选 TopK。

涉及文件：`models/lightgbm/train.py`、`models/lightgbm/config.py`、`models/lightgbm/data.py`、`models/lightgbm/evaluate.py`、`models/rolling.py`、`models/lightgbm/optuna_search.py`、`models/lightgbm/optuna_search_v2.py`、`data/processed/features.parquet`、`data/processed/labels.parquet`、`data/processed/passed_factor_cols.json`。

### I.2 输入和样本处理

`train.py` 读取 `features.parquet` 和 `labels.parquet`，剔除 symbol/date、行业、规模、涨跌停状态和所有标签列；如果存在 `passed_factor_cols.json`，优先使用其中通过分层检验的因子。标签可选 `label_ret_5`、`label_ret_10`、`label_ret_20`。

默认训练时剔除涨停和跌停样本，也可设置为只剔除一字板或不过滤。原因是封板股票可能买不进或卖不出，把它们当普通可交易标签会产生虚假训练信号。

### I.3 为什么采用滚动训练

文件：`models/rolling.py`、`models/lightgbm/train.py`。

市场不是静止的。滚动训练每次只使用当时之前最近 750 个交易日，留出 60 个交易日做局部早停验证，再预测接下来的 20 个交易日：

```text
750日训练 → purge → 60日局部验证 → purge → 20日样本外预测
```

每一步都会重新训练，预测窗口向前移动。这样可以适应市场风格变化，避免把很久以前的规律永久当成有效规律。注意：模型滚动步长 20 日与组合周频调仓不是一回事，前者是模型更新频率，后者是交易频率。

### I.4 Purge/Embargo 如何防止标签泄漏

例如使用 5 日标签时，t 日标签会用到 t+5 的价格。如果训练样本和验证/预测样本太靠近，两个区间的未来收益窗口会重叠。代码通过 `label_horizon()` 按标签周期隔离 5/10/20 个交易日，避免同一段未来收益同时影响训练和验证。

面试回答：Purged Walk-Forward 不是普通随机切分，而是保持时间顺序，并在边界删除可能共享未来收益的样本；这样评估更接近真实上线。

### I.5 LightGBM 每一步如何训练

文件：`models/lightgbm/train.py`、`models/lightgbm/config.py`。

每个滚动窗口中：

1. 取训练、局部验证和预测日期；
2. 删除标签为空、非有限值和不可交易封板样本；
3. 用训练特征和未来收益构造 LightGBM Dataset；
4. 训练回归树模型，最多 1000 轮；
5. 根据局部验证集早停，连续 50 轮没有改善就停止；
6. 用最佳迭代轮数预测下一段日期；
7. 保存模型、预测分数和训练/验证边界。

模型保存到 `models/lightgbm/<label>/step_xxx/lgb_model.txt`，预测保存到 `data/processed/predictions/lgb_pred_<label>.parquet`。预测文件只保存 symbol/date/pred/segment 和模型边界，不携带真实未来收益，避免把答案泄漏给下游。

### I.6 超参数分别控制什么

默认配置在 `models/lightgbm/config.py`：

| 参数 | 小白理解 | 经济/统计作用 |
|---|---|---|
| `learning_rate` | 每棵树走多小一步 | 小步学习通常更稳，但需要更多树 |
| `num_leaves` | 一棵树能分多少个区域 | 越大越能拟合复杂非线性，也越容易过拟合 |
| `max_depth` | 树最多有多深 | 限制复杂度和极端分裂 |
| `min_data_in_leaf` | 每个叶子至少多少样本 | 防止模型为少数股票噪声建立规则 |
| `lambda_l1` | L1 正则 | 压制不重要的复杂结构 |
| `lambda_l2` | L2 正则 | 让树模型更平滑、更稳定 |
| `feature_fraction` | 每棵树抽多少因子 | 降低因子之间的共线和过拟合 |
| `bagging_fraction` | 每棵树抽多少样本 | 增加随机性和泛化能力 |
| `num_boost_round` | 最多建多少棵树 | 控制模型容量，上限 1000 |
| `early_stopping` | 何时停止继续训练 | 防止继续拟合局部验证噪声 |

当前默认使用回归目标和 MSE 训练，但最终更关心横截面 RankIC、头部收益、分层收益和组合净值，而不是单纯 MSE。

### I.7 Optuna 如何优化参数

文件：`models/lightgbm/optuna_search.py`。

Optuna 不是在测试集上搜索，也不是把所有滚动窗口都用于每次试验。它在测试集之前切出一个快速窗口：最近 750 日训练、隔离标签周期、最近 60 日验证。每个 trial 使用 TPE 采样一组参数，LightGBM 早停后在验证窗口计算平均 RankIC，目标是最大化验证 RankIC。

主要搜索范围：

```text
learning_rate: 0.005~0.1
num_leaves: 8~256
max_depth: 3~12
min_data_in_leaf: 5~100
lambda_l1/lambda_l2: 1e-4~100
feature_fraction/bagging_fraction: 0.4~1.0
```

默认 30 次 trial，`--quick` 只跑 3 次用于检查流程，不用于正式结论。最优参数按标签保存到 `models/lightgbm/best_params.json`，`train.py` 再读取并覆盖默认参数。

### I.8 这里体现的经济思想

1. **排序优先**：选股只需要把相对更好的股票排在前面，不要求收益点预测完全准确；
2. **市场状态变化**：滚动训练承认因子关系会变，定期更新模型；
3. **风险控制优先于复杂度**：叶子数、深度、最小叶样本和正则共同限制过拟合；
4. **交易可实现性**：封板样本不作为普通训练样本，避免模型学到无法成交的收益；
5. **标签与调仓匹配**：周频组合更适合先以 5 日收益作为主标签，10 日和 20 日作为敏感性分析；
6. **验证集服务于选择**：验证集可以挑参数，但测试集必须保持最后一次、不可反向使用。

### I.9 面试官常见追问

**为什么不用随机 K 折？**

股票数据有时间顺序，随机切分会让未来市场状态进入训练集，也会让重叠收益泄漏；所以使用 Purged Walk-Forward。

**为什么 LightGBM 适合这个项目？**

因子和收益关系通常非线性，LightGBM 能自动学习阈值和交互，对缺失值也较友好，同时训练速度和解释性优于复杂深度模型。

**为什么训练用 MSE，评价用 RankIC？**

MSE 是树模型的优化目标，RankIC 是选股排序的研究目标。二者不完全相同，因此还要看 TopK、分层和真实组合表现。

**为什么 Optuna 最大化 RankIC 而不是收益？**

先用稳定、可比较的信号指标选择模型，避免把组合 TopK、成本和多个参数一次性混入模型搜索；组合参数留到后续回测阶段。

**滚动步长 20 日是不是每 20 日调仓？**

不是。20 日是模型重新训练和生成预测的窗口步长，周频调仓由 `backtest/config.py` 和组合回测参数单独控制。

### I.10 当前不足与优化

1. 当前 Optuna 主要使用单个快速验证窗口，应升级为多折 Purged Walk-Forward，避免参数只适应一个阶段。
2. 目标可从单一平均 RankIC 扩展为 RankIC、TopK收益、稳定性、换手和成本的约束式目标。
3. 应比较固定窗口与扩张窗口，并按市场状态评估训练长度 500/750/1000/1500 的差异。
4. 应记录每个 trial 的完整数据版本、因子清单、随机种子和验证边界，避免不可复现。
5. 应对 `num_leaves`、深度和正则做更严格的容量约束，避免高维因子库下树模型记忆股票或日期模式。
6. 可以加入时间衰减权重，让较近样本对变化中的市场更重要，但必须只用当时可得信息确定权重。
7. 应增加按行业、规模、牛熊阶段和换手分层的预测诊断，确认模型不是只押注某一种暴露。
8. 最终模型选择不能只看验证 RankIC，还要连接周频 TopK 回测，检查成本后超额收益、回撤和容量。

### I.11 进入组合回测前的验收标准

```text
因子列和标签列没有混淆
训练、验证、预测严格按时间排序
purge长度与标签周期一致
封板样本过滤口径已记录
每个滚动模型的边界已保存
Optuna没有读取最终测试集
最优参数按标签独立保存
验证RankIC、RankICIR、Top组收益和多空已报告
预测文件不包含真实未来收益
最终模型需经过实际TopK和成本回测
```

---

## 44. 因子挖掘准备与隐形机制（对应主流程第四阶段）

### H.1 先理解这个环节

DeepSeek 因子挖掘不是让模型自由生成公式，而是在人工核心 25 因子、允许字段、允许算子、时间边界和历史实验记忆的约束下，提出新的增量研究假设。DeepSeek 负责“提出想法”，本地程序负责“计算、证伪和准入”。

涉及文件：`scripts/mine_one_factor.py`、`scripts/run_deepseek_factor_trial.py`、`scripts/build_factor_mining_memory.py`、`scripts/evaluate_quantmind_candidate.py`、`scripts/compare_candidate_to_core.py`、`scripts/run_quantmind_weekly_candidate.py`、`quantmind_integration/policy.py`、`quantmind_integration/rdagent_hook.py`、`quantmind_pipeline/dsl.py`、`factors/operators_v2.py`、`config/quantmind_candidate_policy.json`、`config/quantmind_admitted_registry.json`、`config/human_core_25_v1.json`、`reports/factor_mining_memory.json`。

### H.2 挖掘前准备工作

每轮真正生成前，`mine_one_factor.py` 会先运行 `build_factor_mining_memory.py`，将历史候选整理为结构化记忆，包括试验次数、成功/失败次数、字段使用次数、类别状态、已尝试公式、最近实验结果和生成规则。

这一步的隐形作用是把过去实验变成下一轮提示词：成功方向可以继续探索，连续失败的大类会被关闭，高换手方向会被惩罚，已经尝试过的公式和简单代数变形会被禁止重复。它不是模型参数训练，而是“实验结果 → JSON记忆 → 下一轮提示词”的外部反馈机制。

### H.3 DeepSeek 实际收到的依据

`run_deepseek_factor_trial.py` 使用 DeepSeek API，模型为 `deepseek-v4-flash`。提示词包含六类内容：

1. 角色：A 股量化研究员，研究周频、long-only、前 10% 分位组合；
2. 目标：围绕固定的 `label_ret_5` 生成新候选；
3. 字段白名单：行情、成交、估值、财务和行业残差字段；
4. 算子白名单：TS_MEAN、TS_STD、TS_SLOPE、SAFE_DIV、FIN_DELTA_REPORT 等；
5. 禁区：测试期、未批准字段、重复公式、核心因子红海、财务错误时点和高换手概念；
6. 历史记忆：候选结果、类别方向和失败经验。

因此它生成的是“受规则限制的经济假设”，不是直接看行情后自动发现规律。

### H.4 当前实际构建过的 DeepSeek/QuantMind 因子

历史实验中实际尝试过的候选包括：

```text
QM_DS_IND_RESID_VOL_20       TS_STD($IND_RESID_RET_1D,20)
QM_DS_IND_RESID_VOL_60       TS_STD($IND_RESID_RET_1D,60)
QM_DS_IND_RESID_VOL_120      TS_STD($IND_RESID_RET_1D,120)
QM_DS_IND_RESID_MEAN_60      TS_MEAN($IND_RESID_RET_1D,60)
QM_DS_IND_RESID_MOM_20       $IND_RESID_MOM_20
QM_DS_IND_REL_RET_20         $IND_REL_RET_20
QM_DS_IND_RESID_SKEW_20      TS_SKEW($IND_RESID_RET_1D,20)
QM_DS_IND_RESID_WIN_RATE     TS_MEAN(WHERE($IND_RESID_RET_1D>0,1,0),20)
QM_DS_AMIHUD_20              TS_MEAN(SAFE_DIV(ABS($pct_chg),$amount),20)
QM_DS_FLOAT_SHARE             SAFE_DIV($circ_mv,$total_mv)
QM_DS_PROFIT_CONVERSION       SAFE_DIV($netprofit_margin,$grossprofit_margin)
QM_DS_CF_NP_GROWTH_GAP        $ocf_yoy-$netprofit_yoy
QM_DS_PROFIT_GROWTH_OVER_SALES $op_yoy-$tr_yoy
QM_DS_SALES_GROWTH_MARGIN     $q_sales_yoy*$netprofit_margin
QM_DS_ROIC_DELTA_1Q           FIN_DELTA_REPORT($roic,1)
QM_DS_LEVERAGE_CHG_1Q         FIN_DELTA_REPORT($debt_to_assets,1)
QM_DS_GROSS_MARGIN_YOY_CHG    FIN_DELTA_REPORT($grossprofit_margin,4)
QM_DS_VWAP_DRIFT_20           TS_MEAN(SAFE_DIV($close-$vwap,$vwap),20)
QM_DS_DOWN_SESSION_VOLUME_SHARE_20  SAFE_DIV(TS_MEAN(WHERE($close<$vwap,$volume,0),20),TS_MEAN($volume,20))
QM_DS_LIQUIDITY_TREND_20_60   SAFE_DIV(TS_MEAN($amount,20),TS_MEAN($amount,60))-1
QM_DS_NOISE_AMPLIFY_20        TS_CORR(ABS($IND_RESID_RET_1D),$turnover_rate,20)
QM_DS_RET_AUTOCORR_20         TS_CORR($pct_chg,DELAY($pct_chg,1),20)
QM_DS_TURNOVER_SLOPE_20       TS_SLOPE($turnover_rate,20)
QM_LOCAL_REV5                  TS_PCT_CHANGE($close,5)
QM_LOCAL_ROE_LEVEL             $roe
QM_COMPOSITE_QUALITY_LOW_CROWDING SIGN($roe)*LOG1P(ABS($roe))-LOG1P($turnover_rate)
QM_COMPOSITE_QUALITY_LOW_CROWDING_LOWVOL30 同上并减30倍20日波动
QM_DS_LIQUIDITY_AMPLIFICATION_TURNOVER_RETVOL 流动性/波动复合指标
```

当前准入注册表中记录的主要通过候选包括：`QM_DS_IDIOVOL_60D`、`QM_COMPOSITE_QUALITY_LOW_CROWDING_LOWVOL30` 和 `QM_DS_LIQUIDITY_AMPLIFICATION_TURNOVER_RETVOL`。注意：尝试过不等于通过，写入 `reports/quantmind_trials/` 也不等于进入正式模型。

### H.5 候选筛选的隐藏门槛

配置文件：`config/quantmind_candidate_policy.json`。候选必须满足：

```text
缺失率 <= 15%
|训练期RankIC| >= 0.01
|增量RankIC| >= 0.005
与核心25因子日秩相关绝对值 < 0.7
```

只有通过这些门槛，才会进入 10bps 周频回测。准入回测的主要门槛是：净 Sharpe 至少 0.5、年化超额收益不低于 0、最大回撤绝对值不超过 0.5。通过者才写入 `config/quantmind_admitted_registry.json`，作为下一轮候选的去重参照。

### H.6 因子挖掘中的经济逻辑

- 行业残差波动：剥离行业共同涨跌后，观察个股特异性风险；负方向表示高特异风险可能对应拥挤、彩票偏好或风险厌恶补偿不足。
- 低换手/低拥挤：交易不拥挤的股票可能有更低成本和更稳定的预期收益；高换手虽然可能有信息，但交易成本会侵蚀收益。
- 质量 + 低拥挤 + 低波动：把盈利能力、投资者关注度和风险结合，寻找基本面较好、交易不拥挤且风险较低的股票。
- 财务变化：理论上经营改善应带来预期修正，但公告频率低、数据缺失和 5 日预测周期错配，导致许多财务候选没有增量信息。
- VWAP/量价背离：试图识别收盘价与平均成交价格、价格与成交量之间的不一致，反映供需和资金行为。

### H.7 面试官追问与回答

**问：DeepSeek 是怎么学习的？** 答：不是重新训练模型，而是把本地试验结果写成结构化记忆，下一轮作为提示词输入；真正的数值计算和回测始终由本地代码完成。

**问：为什么不让 DeepSeek 直接看全部行情？** 答：这样会使数据口径、未来函数和计算过程不可控。项目让模型生成公式，再由本地统一数据和回测框架验证。

**问：为什么很多候选 IC 不低仍被拒？** 答：单因子 IC 只是入场券，还要看增量 IC、与核心因子的重复性、缺失率、换手、成本和周频组合表现。

**问：为什么高换手因子经常失败？** 答：理论信号收益未必能覆盖交易成本；周频策略每周调仓，高换手会把纸面 alpha 转化为成本和冲击损失。

**问：通过的候选能否立即替换人工25？** 答：不能。通过的候选只是进入 QM 准入注册表，还要经过冻结验证期检查，并明确它是增量因子而不是修改人工核心25。

### H.8 当前不足与优化

1. 当前候选数量、通过率和“准入”状态应由统一 manifest 管理，避免人工统计。
2. 提示词中的成功标准、配置文件门槛和实际回测参数应由同一配置生成，避免三套口径。
3. 需要给候选建立不可变实验 ID、输入数据哈希、代码版本、API 请求 ID 和完整失败原因。
4. 可将“新经济逻辑探索”和“同一逻辑窗口优化”拆成两个阶段，减少把窗口微调误当新发现。
5. 应增加跨市场状态、滚动子样本、验证期一次性检查和多重检验，防止只在训练期挖出偶然规律。
6. 对通过候选进行组合增量实验：比较加入前后 RankIC、特征重要性、持仓重合、换手、行业暴露和净超额收益。
7. 对财务因子加入公告延迟、报告类型、最大有效期和财务字段覆盖门槛。
8. 当前 RD-Agent Hook 已能校验候选转换，但主挖掘入口仍是 DeepSeek 直连脚本；若要宣称“完整 RD-Agent 自动进化”，还需实际启动并审计 RD-Agent 主循环。

### H.9 本阶段验收

```text
生成前已重建 factor_mining_memory
候选只使用允许字段和算子
候选没有使用测试期
候选有明确经济逻辑
缺失率、RankIC、增量RankIC已计算
核心25和已准入QM因子去重已完成
10bps周频回测只对通过前置门槛的候选执行
通过和失败候选均保存报告
只有周频通过者进入准入注册表
准入因子仍需验证期检查，不能直接称为最终alpha
```

---

## 45. 270 因子筛选为 25 个人工核心因子（面试版，对应主流程第三阶段）

270 因子不是直接按排名取前 25，而是经过训练期统计筛选、去冗余、五分组检验和人工经济逻辑复核。涉及文件：`analysis/evaluate_factors.py`、`analysis/remove_redundant_factors.py`、`analysis/factor_quantile.py`、`analysis/factor_extra_checks.py`、`config/human_core_25_v1.json`、`config/human_final_sota_v1.json`。

真实漏斗是：

```text
270候选因子
→ 训练期RankIC/ICIR评估
→ 低信号和不稳定信号过滤
→ 日频IC序列相关性去冗余
→ 约58个历史优选因子
→ 5日五分组收益与单调性检验
→ 25个人工核心因子
```

### F.1 训练期 RankIC 评估

`analysis/evaluate_factors.py` 只使用训练期 `2016-01-01 ~ 2023-04-30`。每天把所有股票的因子排名与未来收益排名做 Spearman 相关，得到日频 RankIC，再计算 IC 均值、标准差、ICIR、t 值、p 值和有效天数。至少有 30 个有效交易日才纳入汇总。

经济含义是：横截面选股关心因子能否把未来收益较高的股票排在前面，因此 RankIC 比单纯看收益均值更适合评价排序能力。只使用训练期是为了让验证集和测试集保持真正样本外。

输出：`data/processed/factor_evaluation.parquet`、`data/processed/daily_rankic.parquet`。

### F.2 低信号和不稳定因子过滤

`analysis/remove_redundant_factors.py` 的当前默认门槛是：`|ic_mean| >= 0.01` 且 `|icir| > 0.05`。负 IC 不会被误删，因为负 IC 可能代表稳定的反向关系，LightGBM 可以学习负向关系，分组检验也会按 IC 符号对齐。

这一步体现“先排除噪声，再讨论组合”的思想：平均 IC 接近 0 的因子缺乏排序能力，ICIR 很低则说明方向不稳定。但门槛只是研究筛选规则，不等于统计学上的最终证明。

### F.3 日频 IC 序列去冗余

同一个文件中的 `greedy_prune()` 先按 IC 均值排序，再计算因子日频 RankIC 序列之间的 Pearson 相关。若新因子与已保留因子的相关性大于 `0.70`，就剔除新因子，并写明 `redundant_with`、相关系数和双方 IC。

输出：`selected_factor_cols.json`、`factor_kept.parquet`、`factor_removed.parquet`、`reports/factor_redundancy_report.txt`。

为什么比较 IC 序列而不是原始因子值：两个因子的数值尺度可能不同，但如果它们每天的预测能力同步变化，说明它们承担了相同或相近的经济暴露。去冗余的目标不是让公式长得不同，而是减少重复信息。

### F.4 58 到 25 的五分组检验

`analysis/factor_quantile.py` 在每个调仓日按因子值把股票等分为 G1 至 G5，观察各组未来收益、多空收益、收益单调性、夏普、最大回撤和换手。训练集通过条件是：IC 符号对齐后的年化多空收益大于 0，且组收益单调性绝对值大于 0.8；验证集只观察，不用于反复修改清单。

这一步比单看 IC 更接近投资组合：IC 只说明全截面排序相关性，五分组能看出信号是否集中在头部、是否单调、是否有可交易的多空差异。

### F.5 当前 25 个人工核心因子

清单来自 `config/human_core_25_v1.json`：

```text
LOW_TURNOVER, LN_TURNOVER, CORR20, VOL_CV20,
RET_AMT_CORR20, KLOW, CONSEC_UP5, CORD60,
RET_VOL_CORR5, OBV_CHG_20, CORD5, AMIHUD_20,
VOL_OF_VOL_20, CORR60, VOL_PRICE_DIVERGENCE,
BP, EP, CNTD10, IMAX60, WVMA30, MAX_DD60_V2,
CNTD60, Q_SALES_YOY, VSTD5, NEG_SKEW_20
```

这些因子覆盖流动性/拥挤、量价关系、波动和尾部风险、价格结构和趋势、价值、资金流以及基本面成长。人工复核的作用是避免 25 个因子全部只是“成交量的 25 种变形”，让最终核心集合在统计有效性、经济解释、风险暴露和交易可行性之间平衡。

### F.6 面试官常见追问

**为什么不直接选 IC 最高的 25 个？**

因为最高 IC 可能集中在同一个风险暴露上。我先去弱、再用日频 IC 相关性去冗余，最后用五分组收益、单调性和换手筛选。

**负 IC 因子为什么保留？**

负 IC 不代表无效，可能是稳定的反向信号。筛选使用绝对值，分组回测使用 IC 符号对齐。

**为什么只用训练集？**

如果用验证集挑因子，验证集就被反复试错，不能再客观评价最终方案；测试集更不能参与筛选。

**58 和 25 是什么关系？**

58 是 IC/ICIR 和日频相关性去冗余后的历史优选集合；25 是 58 再经过 5 日五分组收益和单调性检验后的当前人工核心集合。`human_final_sota_v1.json` 是历史参考，`human_core_25_v1.json` 才是当前核心清单。

### F.7 当前不足与后续优化

1. 将 IC、ICIR 和相关性阈值全部移入统一配置，消除代码注释与历史报告的参数漂移。
2. 用聚类、最大独立集或组合边际贡献替代单一贪心去冗余，减少排序顺序影响。
3. 对 270 次检验增加 FDR、置换检验或 block bootstrap，控制多重检验造成的假显著。
4. 增加早期/中期/近期、牛熊和行业分层稳定性，而不是只看全训练期平均值。
5. 把五分组结果连接到真实 TopK、成本、涨跌停、停牌和容量约束，避免分层收益高估可实现收益。
6. 记录每个因子的公式版本、输入、窗口、方向、数据文件、筛选原因和复算日期。
7. 对 25 因子的组合暴露做进一步检查，避免单因子筛选通过但组合后仍然集中在同一风险来源。

### F.8 进入 DeepSeek 和 LightGBM 前的验收

必须能从 `factors.parquet` 找到 25 个因子列；每个因子都有类别、公式和经济解释；弱信号、冗余和五分组结果都有记录；验证集只观察；测试集未参与筛选。完成后，25 个因子才可以作为 DeepSeek 去重基准和 LightGBM 的人工核心输入。

---

## 46. 270 因子库构建详解（面试版，对应主流程第二阶段）

### F.1 这 270 个因子是什么

270 是候选因子池，不是最终持仓数量，也不是 270 个完全独立的 alpha。当前口径是：约 112 个自研注册因子，加上 Qlib Alpha158 的 158 个标准量价因子。自研因子体现项目的经济假设，Alpha158 提供统一、可复现的外部基准。

涉及文件：`scripts/build_alpha158.py`、`scripts/build_factors.py`、`factors/registry.py`、`factors/operators.py`、`factors/operators_v2.py`、`factors/base/` 下的各因子模块、`factor_catalog/build.py`、`factor_catalog/audit.py`、`config/factor_catalog.json`、`data/processed/alpha158.parquet` 和 `data/processed/factors.parquet`。

### F.2 正确运行顺序

```text
basic_cleaned_with_extra_by_date.parquet
    ↓
build_alpha158.py → alpha158.parquet
    ↓
build_factors.py → factors.parquet
    ↓
build_factor_catalog.py → audit_factor_catalog.py
```

命令：

```powershell
python main.py build_alpha158
python main.py build_factors
python main.py build_factor_catalog
python main.py audit_factor_catalog
```

必须先生成 Alpha158 再构建总因子表，否则 `build_factors.py` 找不到 `alpha158.parquet`，只会生成自研因子。

### F.3 自研因子类别和经济问题

| 类别 | 文件 | 研究问题 | 经济逻辑 |
|---|---|---|---|
| 反转/动量 | `reversal_momentum.py` | 过去强弱是否延续或反转 | 信息扩散、趋势跟随、过度反应 |
| 波动率 | `volatility.py` | 风险和尾部波动是否被定价 | 风险补偿、低波动异象、极端损失 |
| 流动性 | `liquidity.py` | 交易难度和拥挤是否影响收益 | 流动性溢价、交易成本、拥挤风险 |
| 技术 | `technical.py` | 价格形态是否可重复 | 趋势、超买超卖、价格偏离 |
| 价值 | `value.py` | 便宜股票是否长期回归 | 价值回归和风险补偿 |
| 质量 | `quality.py` | 盈利和资产负债表是否持续 | 盈利持续性、经营稳健性 |
| 成长 | `growth.py` | 经营改善是否被低估 | 预期修正和基本面趋势 |
| 价格结构 | `price_structure.py` | 高低点、回撤和位置是否有信息 | 供需失衡、支撑阻力、行为偏差 |
| 量价 | `volume_price.py` | 成交量是否确认价格变化 | 信息交易、资金流、量价背离 |
| 华泰GP复现 | `gp_huatai_replication.py` | 文献中的遗传规划因子是否可复现 | 外部研究假设的独立验证 |

当前 `factors/registry.py` 允许的类别还包括预留的 `composite`。一个文件可以注册多个因子，因此文件数量不能代表因子数量。

### F.4 具体因子如何理解

反转/动量因子使用过去收益、隔夜收益、连续上涨、跳空和最大回撤；波动率因子使用收益标准差、下行波动、ATR、偏度和回撤；流动性因子使用换手率、成交额、Amihud 类价格冲击和量价相关；技术因子使用均线、RSI、MACD、KDJ、CCI 和布林带；价值因子使用 EP、BP、SP；质量因子使用 ROE、ROA、ROIC、利润率、流动性、杠杆和周转；成长因子使用收入、利润、现金流同比以及单季变化。

面试时不要说“这些因子天然有效”，应说：每一类因子对应一个可检验的市场行为或公司基本面假设，最终要看训练期稳定性、验证期表现、交易成本和增量信息。

### F.5 Alpha158 做了什么

文件：`scripts/build_alpha158.py`。它调用 Qlib 的 `Alpha158.get_feature_config()`，批量计算：9 个 K 线结构因子、4 个价格类因子、29 个滚动算子乘 5 个窗口（5/10/20/30/60），合计 158 个。

Alpha158 的优势是标准、成熟、可复现；不足是多个窗口之间可能高度相关，部分表达式经济解释较弱，不能因为数量多就认为信息更多。它必须和自研因子一样经过后续 IC、去冗余、分组和样本外评估。

### F.6 `build_factors.py` 的真实机制

文件：`scripts/build_factors.py`。它优先读取增强底表，只投影因子需要的字段；按股票和日期排序；生成未来 5、10、20 个交易日收益标签；导入所有 `factors/base/*.py`，通过模块导入自动注册；遍历注册表计算自研因子；删除不再需要的原始列；分批读取 Alpha158，并按 `(symbol,date)` 对齐；同名时保留自研版本；最后保存 `factors.parquet`。

标签定义是：`label_ret_h(t) = close(t+h) / close(t) - 1`。标签不是因子，不能计入 270 个特征。最后 h 个交易日没有未来价格，因此标签为空，训练时应剔除。

### F.7 因子计算中的安全处理

涉及 `factors/operators_v2.py` 和各 `factors/base/*.py`：时间序列必须按股票分组并先排序；除法使用 `SAFE_DIV`；对数和平方根使用安全版本；财务跨期变化使用报告期序号；横截面排名按日期计算；缺失值不随意填 0；因子原值先落盘，缩尾、标准化和正式中性化放到 `preprocess_factors.py`，避免用全样本统计量泄漏未来。

### F.8 面试官会问什么

**问：为什么做 270 个？**

答：为了覆盖技术、量价、流动性、价值、质量、成长和风险等不同经济机制，同时保留 Alpha158 作为标准基准；270 只是候选池，之后要筛选。

**问：270 个是不是 270 个独立信号？**

答：不是。多个因子可能使用相同字段或不同窗口表达相同概念，所以必须做相关性去冗余和增量检验。

**问：为什么不直接把 270 个全喂给模型？**

答：会扩大多重检验、冗余和过拟合风险，也会降低解释性。先在训练期做质量和冗余筛选，再进行模型验证。

**问：因子和标签有什么区别？**

答：因子只用 t 日及以前的信息，标签用 t+h 的未来收益，只能作为监督学习目标，不能作为输入特征。

### F.9 后续优化

1. 让构建脚本自动输出自研数量、Alpha158 数量、重复列、失败因子、缺失率和最终总数，避免“文档写 270、实际数量变化”。
2. 给每个因子保存公式、输入、窗口、类别、方向、可用时间、缺失率和版本号。
3. 在落盘前阻断常数列、无穷值过多、覆盖率过低和疑似未来依赖的因子。
4. 明确北交所口径：`fetch_daily.py` 导出 `daily_bj`，但 `merge_data.py` 当前主链主要合并沪深，因子库必须统一股票范围。
5. 统一 `operators.py` 和 `operators_v2.py` 的正式使用边界，旧算子只用于历史复现。
6. 对 5/10/20/30/60 多窗口进行多重检验校正和稳定性筛选，减少窗口挖掘偏差。
7. 用历史行业和动态股票池，避免当前行业或当前股票状态回看历史。
8. 对复权价格、除权日、停牌日和涨跌停日增加单元测试。
9. 为因子库、底表、配置和代码提交生成统一 manifest，确保模型和回测结果可追溯。

270 因子库验收完成后，才进入“因子评估、去冗余、分组检验和 25 个人工因子确定”，不能直接进入 LightGBM。
